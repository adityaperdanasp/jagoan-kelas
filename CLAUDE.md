# Jagoan Kelas — Project Notes

Aplikasi edukasi buat anak SD Indonesia kelas 1-6, ngikutin Kurikulum Merdeka. Tagline: "Belajar, tapi Gak Ngebosenin". Dibuat buat kelas anaknya Adit (sama konteksnya kayak BrainBox), tapi **infrastruktur 100% terpisah** dari BrainBox (`~/Documents/al-idrisi-games`) — repo GitHub beda, Firebase project beda, Vercel project beda, domain beda. BrainBox cuma dipakai sebagai REFERENSI POLA KODE (baca-only, gak pernah diedit dari sesi kerja project ini).

## Infra

- **Repo**: [github.com/adityaperdanasp/jagoan-kelas](https://github.com/adityaperdanasp/jagoan-kelas)
- **Hosting**: Vercel project `ellilo/jagoan-kelas` → [jagoan-kelas.vercel.app](https://jagoan-kelas.vercel.app), `vercel --prod --yes` buat deploy manual (belum ada custom domain)
- **Backend**: Firebase project `jagoan-kelas` (Firestore Database, region `asia-southeast2`/Jakarta, mode test). Config di `src/firebase.js` — `apiKey` itu public client identifier, aman di-commit (security lewat Firestore Rules, bukan kerahasiaan key), sama kayak pola `firebase.js` BrainBox.
- **Stack**: Vite + React + React Router (SPA, bukan Next.js). BrainBox pakai vanilla HTML/CSS/JS; project ini sengaja pakai React karena butuh handle 42 kombinasi kelas×pelajaran dengan komponen reusable, bukan reuse langsung kode BrainBox.
- **Auth ke GitHub**: SSH key (`~/.ssh/id_ed25519`) udah terhubung ke akun `adityaperdanasp`, dipakai buat push — bukan `gh auth login` (belum pernah login, `gh` CLI ke-install tapi nganggur).
- **Deploy history penting**: sesi kerja 2026-08-04 s/d 2026-08-05 numpuk 30+ file perubahan (konten 3 subject baru, semua fitur di CLAUDE.md ini) TANPA di-commit sama sekali sampai akhirnya di-commit+push+`vercel --prod --yes` sekaligus di penghujung 2026-08-05 — kalau nemu commit gede "Add Bahasa Inggris/Indonesia/PAI content, Drive/Plane advanced features, BGM, encourage messages, AI Tutor", itu sebabnya. **Pelajaran buat serverless function baru**: `api/generate-hint.js` sempet gagal deploy (`FUNCTION_INVOCATION_FAILED`) gara-gara pakai `module.exports` (CommonJS) padahal `package.json` app ini `"type":"module"` — WAJIB pakai `export default` (ESM) buat file apapun di `api/`.

## Desain — WAJIB ikutin wireframe persis, jangan improvisasi

Sumber: `~/Documents/jagoan-kelas/design_handoff_kids_game_wireframe/` (`README.md` + `Kids Game Wireframe.dc.html` + `_ds/` design tokens). Ini SPEC, bukan inspirasi.

- Token warna/font/radius/shadow di `src/styles/tokens.css` — nilai persis dari `_ds/tokens/*.css`, **jangan approximate**.
- Komponen di `src/components/ds/` (Avatar, Badge, Chip, Button, GameCard, ProgressXP, Input, SegmentedToggle) — di-port **1:1** dari source JSX yang ada di `_ds_bundle.js` (bundle itu nyimpen source component asli BrainBox design system, bukan cuma compiled CSS).
- Awalnya 4 layar wireframe: Landing → Pilih Kelas (`PickGrade.jsx`, path 6 node winding) → Pilih Pelajaran (`PickSubject.jsx`, 7→6 card) → Detail Pelajaran (`SubjectDetail.jsx`, list topik + status). **`PickGrade.jsx` DIGABUNG ke `Landing.jsx` 2026-08-06** (lihat bagian "Kiko — maskot resmi" buat detail) — SEKARANG 3 layar: Landing (skalian jadi Pilih Kelas) → Pilih Pelajaran → Detail Pelajaran.
- **Deviasi yang UDAH disepakati sama user** (jangan revert tanpa nanya): wireframe aslinya nampilin **7 mata pelajaran** (Matematika/IPA/IPS/PPKn/PAI/B.Indo/B.Inggris terpisah), tapi diubah jadi **6** — IPA+IPS digabung jadi satu card "IPAS" (`src/data/content.js`) karena Kurikulum Merdeka asli emang udah merger keduanya jadi satu buku sejak kelas 3 (kelas 1-2 tematik). Kalau nanti ada permintaan "kok cuma 6 pelajaran" — ini bukan bug, ini keputusan sadar.

## Kiko — maskot resmi (desain final 2026-08-05, di-pasang ke seluruh app 2026-08-06)

`src/components/ds/Kiko.jsx` — SVG hand-coded, badan biru (`--pastel-blue`), telinga ungu (`--pastel-purple`), antena gold (`--pastel-gold`), pipi pink (`--pastel-pink`), badge dada hijau (`--pastel-green`), panel muka **cream terang** (BUKAN gelap — user eksplisit nolak versi awal solid ink-900 karena "kegelapan", lihat komentar di file). Nama final "Kiko" (versi sebelumnya sempet dipanggil "Robi" pas eksplorasi awal). **Desain FINAL, jangan diubah lagi tanpa diminta eksplisit** (warning ini ada di komentar file-nya sendiri, bukan cuma di sini).

Batch kerjaan 2026-08-06 masangin Kiko ke 4 titik di app (semua pakai komponen `Kiko.jsx` yang sama, `size` beda-beda per konteks, gak ada varian/pose baru):

- **Landing hero, versi 1** (`Landing.jsx`) — `<Kiko size={92}/>` GANTIIN `<HeroIllustration/>` yang lama (komponennya udah dihapus, lihat bagian "Hero Illustration" di bawah buat histori). `useSecretTap` easter egg gak kesenggol (quadrant dihitung dari div pembungkus, bukan isinya).
- **Pilih Kelas, versi 1** (`WanderingKiko.jsx`, dulu `WanderingDino.jsx`) — Kiko patroli bolak-balik horizontal di atas lingkaran pilih kelas, MURNI DEKORATIF (gak ada tap-to-navigate, beda dari versi dino lama). Entry ke DinoRace DIPISAH ke tombol pill sendiri. Logic patroli (interval 2.6s, posisi 6%-82%, bounce animation) SAMA PERSIS kayak `WanderingDino.jsx` lama, cuma visual + interaktivitasnya yang beda.
  - **`PickGrade.jsx` DIGABUNG ke `Landing.jsx` (2026-08-06, permintaan user "langsung pemilihan kelas, gak usah ada 'Ayo Main' lagi")** — file `PickGrade.jsx` DIHAPUS, semua isinya (heading "Pilih Kelas Kamu!", tombol pill DinoRace, `WanderingKiko`, lingkaran 6 kelas dari `GRADES`) dipindah jadi bagian dari `Landing.jsx`, di bawah hero+badge (yang di-kecilin biar muat), GANTIIN tombol "Ayo Main! 🚀" yang dihapus total. Route `/kelas` (tanpa `:grade`) DIHAPUS dari `App.jsx` — `PickSubject.jsx` back-button diarahin ke `/` (bukan `/kelas` lagi). Header/avatar/XP/mute/parent-link Landing yang lama TETEP ADA gak berubah, cuma badan bawahnya yang diganti.
  - **REVAMP TOTAL (2026-08-06, sore, sesi yang sama)** — user kasih 4 feedback abis liat versi gabungan di atas: (1) warna kartu kelas ("ink-on-*" gelap yang dipake sementara buat mockup review) kegelapan, (2) desain masih "kebanyakan card, buang-buang space", (3) Kiko harus ada animasi + tas yang bisa di-klik jadi entry chatbot, (4) tombol pill "🦕 DinoRace" dihapus dari sini, jadi easter egg lagi. **Proses**: bikin 3 mockup (A/B/C) LANGSUNG di `Landing.jsx` sungguhan (bukan artifact terpisah, lihat `feedback_mockup_in_real_file`) via query param sementara `?mockup=a|b|c` (Mockup component-nya nempel sementara di bawah file, dicabut abis user milih), screenshot pakai Playwright headless (localStorage di-seed manual biar lolos `RequireAuth` tanpa login asli) karena screenshot dari `mcp__Claude_Browser__computer` CUMA keliatan di sesi asisten, GAK otomatis ke-forward ke user (baru ketauan pas user bilang "mana mockupnya ga kliatan") — kirim ulang pake `SendUserFile`. User pilih **Mockup B** (Kiko ngambang gak dibungkus card + grid 3×2 padat) tapi minta warnanya diganti dari `ink-on-*` gelap ke **"pastel lucu"**.
  - **Implementasi final** (`Landing.jsx`, scaffold mockup dicabut total): `GRADE_CARD_COLORS` — palet BARU (bukan `--pastel-*` yang kelewat pucat, BUKAN JUGA `--ink-on-*` yang kelewat gelap dari mockup) — 6 hex custom cukup saturated buat nge-pop kayak card game (mirip Duolingo/Kahoot) tapi tetep ringan/ceria, di grid `1fr 1fr 1fr` + `aspectRatio:"1"` (WAJIB biar sel grid gak ke-stretch jadi persegi panjang tinggi — bug yang kejadian di draft mockup B pertama, `alignItems:stretch` default CSS Grid + `flex:1` parent bikin baris grid stretch ngisi sisa ruang; fix-nya `aspectRatio:"1"` + `alignContent:"start"`). Teks "Jagoan Kelas" jadi gradient text (`WebkitBackgroundClip:"text"`), Kiko `size={112}` + `animation: jkKikoWiggle` (rotate ±4deg terus-terusan) + badge tas 🎒 kecil nempel (`jkBagPulse`, scale pulse). `WanderingKiko.jsx` DIHAPUS (gak dipake lagi — Kiko besar udah jadi hero statis, bukan yang "jalan-jalan" lagi) GANTIIN `WalkingDino.jsx` (component baru) di bagian bawah grid.
  - **Ronde revisi ke-2 (2026-08-06, malam, 3 feedback abis liat implementasi final di atas)**:
    1. **Warna kelas 3 & 6 "terlalu gonjreng"** — `GRADE_CARD_COLORS[2]` (`#6FCF7C` neon-ish green) → `#8FCB9B` (sage lembut), `[5]` (`#F06BA8` hot pink) → `#E888B4` (dusty rose). 4 warna lain gak disentuh (gak dikomplain).
    2. **Backpack 🎒 udah jadi tap-target BENERAN** (bukan placeholder lagi) — lihat detail lengkap di bagian "AI Tutor" → "Kiko chat umum". Konsekuensi: `handleSecretTap` (easter egg DinoRace) DIPINDAH dari div Kiko ke teks "Jagoan Kelas" di bawahnya (dulu numpang di elemen yang sama) — 2 fitur beda gak bisa lagi berbagi 1 tap-target begitu Kiko sendiri jadi tombol chat sungguhan (React `onClick` biasa, bukan lagi div dekoratif). Ditambah **sticker speech-bubble** "Hai, ini Kiko! 👋" (div putih rotate 4deg + segitiga kecil `::after`-style pakai `<span>` rotate 45deg sebagai "ekor" bubble) nempel di pojok kanan-atas Kiko, permanen (bukan muncul-hilang), nunjukkin ke anak itu Kiko & bisa di-ajak ngobrol.
- **Drive Mode windshield** (`DriveMode.jsx`) — wajah Kiko kecil (`size={13}`) nempel di kaca depan mobil, ikut posisi mobil tapi counter-rotate biar tetep tegak pas mobil belok. Detail teknis lengkap di bagian "Games" → Drive Mode.
- **AI Tutor v2 "Tanya Kiko"** (`KikoTutorChat.jsx` + `api/kiko-chat.js`) — upgrade AI Tutor dari tombol on-demand jadi chat interaktif pakai wajah Kiko sebagai avatar. Detail lengkap di bagian "AI Tutor".

Yang **SENGAJA BELUM disentuh** (bukan lupa, di luar scope batch ini): `RoamingCarDino.jsx` (mobil+dino roaming di bawah teks intro Landing) masih pakai emoji dino 🦕, BUKAN Kiko — karena itu representasi kendaraan+musuh DinoRace (dino yang ngejar mobil, bukan maskot ramah), ganti ke Kiko di situ malah bikin bingung (Kiko jadi seolah-olah "musuh" yang ngejar). `GlassBridge.jsx`/`BoBridgeBanner.jsx` (Bo Bridge) juga masih pakai dino sebagai walker character, alasan sama.

## Auth — pola disamain persis sama BrainBox

- **Sign Up / Sign In** pakai nama + PIN 4 digit (`src/screens/Auth.jsx`, `SegmentedToggle`), BUKAN email/password formal — sama filosofi kayak BrainBox (`al-idrisi-games/index.html` layar `#sc-screen-select`).
- Skema data: `players/{nameKey}` → `{name, pin, xp, createdAt}` (`src/data/authService.js`). `nameKey` = nama di-lowercase + non-alfanumerik jadi `-`, identik sama fungsi `nameKeyOf`/sanitasi BrainBox punya `testerAccounts/{nameKey}`.
  - Sign Up: tolak kalau `nameKey` udah ada dokumennya, langsung login abis create.
  - Sign In: cocokin `pin` plaintext ke yang tersimpan. **Plaintext PIN itu SENGAJA** (bukan lupa hash) — sama persis pola BrainBox, karena threat model-nya rendah (device fisik anak SD di kelas, bukan aplikasi publik dengan data sensitif).
- Avatar warna: `hashColor(seed ?? name)` di `Avatar.jsx` — fungsi hash-nya **disalin persis** dari BrainBox punya (PALETTE array yang sama, algoritma hash yang sama), bukan reimplementasi.
- Session: `localStorage` key `jagoan_kelas_player` nyimpen `{id, name, pin, xp, createdAt}` (`src/data/PlayerContext.jsx`) — analog `player.js` BrainBox (`aig_player`), tapi lebih simpel (BrainBox punya konsep `role: parent/child` yang gak dipakai di sini, gak ada kebutuhan itu).
- Proteksi rute: `RequireAuth` wrapper di `App.jsx` — semua rute selain `/masuk` redirect ke situ kalau `player` null.

### Beda sengaja dari BrainBox (dicatat biar transparan, bukan silent divergence)
- **Firestore buat data utama, Realtime Database (RTDB) khusus buat DinoRace.** Progress/XP/akun tetap di Firestore (model per-dokumen, udah kepasang dari awal). RTDB ditambahin ke project Firebase `jagoan-kelas` yang sama (2026-08-05, dibuat manual via Firebase Console oleh user, region asia-southeast1) khusus buat DinoRace (`public/dinorace/`, lihat bagian Games) yang butuh sinkronisasi real-time buat 2-player race — satu Firebase project bisa punya Firestore DAN RTDB sekaligus, gak perlu project terpisah. **DinoRace TIDAK numpang ke project al-idrisi-games** — infra tetap 100% terpisah sesuai prinsip project ini.
- Skema progress (`players/{id}.progress.{subject}.{grade}.{babKey}`) **flat nested-map di 1 dokumen**, BUKAN subcollection kayak BrainBox punya `players/{id}/badges/{gameId}/...`/`topicStats/{gameId}/{topicKey}/...` (2 path terpisah). Digabung jadi satu struktur di sini karena scope-nya lebih kecil per topik (status+stars+xp+correct+wrong sekaligus, bukan 2 concern terpisah) — lihat bagian "Progress Tracking + Focus Round" di bawah buat detail lengkap.

## Konten soal (content-pipeline)

Folder terpisah `~/Documents/jagoan-kelas/content-pipeline/` (di luar `app/`, cuma script Python + output JSON, gak ikut ke-deploy). Restrukturisasi bank soal mentah (`soal_matematika_1000.json`, `soal_ipas_1_6.json`) jadi format per-bab sesuai struktur Kurikulum Merdeka resmi (`input/struktur_bab_kemdikbud.md`, hasil riset web + verifikasi ulang).

- `bab_struktur.py` — struktur bab resmi per kelas per mapel (hardcoded dari riset, termasuk 8-bab tematik IPAS kelas 1 dari modul ajar Kemdikbud, BUKAN Buku Siswa resmi karena emang gak ada buku IPAS buat kelas 1-2).
- `classify_matematika.py` / `classify_ipas.py` — mapping soal existing ke bab pakai kombinasi topic-name langsung + keyword-scoring (buat topik generik kayak "Pengetahuan Alam & Sosial Tambahan"). Soal yang gak match apapun masuk `belum_terpetakan`, TIDAK dipaksa ke bab yang salah.
- `generators_matematika.py` — generator soal prosedural (gaya MathVille `generators.js`, jawaban dihitung via kode) buat ngisi bab yang kosong/tipis. Target 15 soal/bab.
- `draft_ipas.py` — soal IPAS ditulis manual (BUKAN generate acak, karena harus faktual akurat) buat 14 bab yang kosong, ditandai `"sumber": "draft_ai_perlu_review"` — **WAJIB direview manusia sebelum dianggap final**, khususnya sains (cahaya/bunyi, tubuh manusia, bumi) dan sejarah/geografi. Kelas 6 Bab 8 "Proyek Akhir IPAS" SENGAJA gak dibikinin soal (itu bab proyek, bukan materi kuis).
- Output di `content-pipeline/output/{matematika,ipas}/kelas_{1-6}.json`, di-copy manual ke `app/src/data/content/{subject}/kelas_{n}.json` (bukan symlink/build-step — kalau content-pipeline di-rerun, HARUS re-copy manual ke app).
- **PPKn**: soal per-bab (`soal_ppkn_per_bab.json`) dikasih user langsung udah dalam format final, di-split jadi per-kelas via script inline (bukan file terpisah, one-off) ke `app/src/data/content/ppkn/kelas_{n}.json`.
- **PAI**: konten paling sensitif, WAJIB direview ustadz/guru agama sebelum di-generate ulang/ditambah — udah SELESAI & AKTIF (lihat bagian "PAI" di bawah), dicatat di sini biar gak ada yang asal nge-generate soal baru tanpa research yang sama ketatnya. IPS udah gabung ke IPAS (bukan gap).

### Mismatch kurikulum — SUDAH diselesaikan (2026-08-04)
Semua 421 soal `belum_terpetakan` (341 Matematika + 80 IPAS) udah direklasifikasi berdasarkan riset ulang struktur bab resmi Kemdikbud (web search, cross-check beberapa sumber independen). Keputusan detail + alasan ada di docstring `content-pipeline/reclassify_cross_grade.py` (Matematika) dan `content-pipeline/reclassify_ipas.py` (IPAS) — **baca itu sebelum re-run pipeline dari awal**, karena `restructure_*.py` bakal reset ke state belum-direklasifikasi (reclassify script harus dijalanin lagi setelahnya, urutannya: restructure → fill → reclassify).

### Bahasa Inggris & Bahasa Indonesia — konten baru (2026-08-04)
Beda dari Matematika/IPAS: gak ada bank soal existing buat direstrukturisasi, jadi dibangun langsung dari nol (struktur bab + soal, keduanya ORIGINAL, sumber `"draft_ai_perlu_review"` — **WAJIB direview guru** sebelum dianggap final, terutama akurasi kosakata Inggris & tata bahasa Indonesia).

- **`INGGRIS_BAB`/`BINDO_BAB`** di `bab_struktur.py` — struktur bab DIRANCANG SENDIRI (bukan port literal buku resmi), lihat komentar panjang di atas tiap dict buat alasannya: B.Inggris statusnya mulok pilihan Kurikulum Merdeka (gak ada 1 buku baku nasional), B.Indonesia strukturnya per-SKILL bukan per-cerita (app belum punya komponen buat nampilin bacaan panjang).
- **`draft_inggris.py`** — soal 2 arah (EN→ID, ID→EN) di-assemble programatis dari bank kosakata tematik per bab (`vocab_pair_questions()`), bukan generator prosedural kayak Matematika (kosakata tetep ditulis manual).
- **`draft_bindo.py`** — soal tata bahasa/kosakata Indonesia (jenis kalimat, sinonim-antonim, kata baku, jenis teks, dll), ditulis manual per bab.
- **`build_inggris.py`/`build_bindo.py`** — assemble langsung dari struktur+draft (gak ada tahap restructure/classify kayak Matematika/IPAS, karena emang gak ada bank buat diklasifikasi).
- Output di `content-pipeline/output/{inggris,bindo}/kelas_{1-6}.json`, **di-copy manual ke folder app yang NAMANYA BEDA**: `inggris` → `app/src/data/content/binggris/`, `bindo` → `app/src/data/content/bindo/` (harus cocok sama `SUBJECTS` id di `content.js` / `SUBJECTS_WITH_CONTENT` di `contentLoader.js`, JANGAN asal copy nama folder pipeline).
- 526 soal B.Inggris, 282 soal B.Indonesia. Diverifikasi jalan end-to-end di browser (register → pilih kelas 4 → buka kedua subject → jawab soal → feedback benar muncul).

Ringkasan keputusan kunci:
- Matematika: perkalian/pembagian ternyata materi resmi **kelas 3** (bukan kelas 2 — Kurikulum Merdeka nunda ke kelas 3). Keliling bangun datar → kelas 5. Volume bangun ruang (rumus kubus/balok) → kelas 6 bab "Kubus dan Balok". Perbandingan&skala → kelas 6 bab "Rasio". Statistika (modus) → kelas 5 bab "Data".
- **1 topik dibuang** (bukan dipindah): "Bilangan Bulat" kelas 6 (28 soal) ternyata cuma soal penjumlahan/pengurangan level kelas 1 yang salah label generik, gak ada nilai pedagogis di kelas 6 — diputuskan dibuang.
- IPAS: semua 80 soal dapet rumah (gak ada yang dibuang), kebanyakan soal generik "Pengetahuan Alam & Sosial (Tambahan)" yang gagal keyword-matching ternyata cuma butuh assignment manual per-soal (bukan per-topik) karena 1 topik generik nyampur beberapa tema beda kelas.
- Kelas 6 Matematika dikonfirmasi CUMA 4 bab resmi (Pecahan&Desimal/Rasio/Kubus&Balok/Peluang) via 2 sumber independen — bukan struktur yang salah/kurang lengkap.

## BGM + Encourage Message (2026-08-05)

**Musik latar per pelajaran** (`src/data/bgm.js`) — di-port dari pola BrainBox `mathville/bgm.js` APA ADANYA (baca komentar di file itu buat detail iOS Safari workarounds yang disalin, bukan preferensi gaya): volume lewat Web Audio `GainNode` (BUKAN `<audio>.volume` — iOS Safari ngabaikan itu total), unlock-on-gesture yang RETRY tiap gesture (AudioContext bisa nyangkut "suspended" di iOS walau `resume()` udah dipanggil), `switchTrack()` ganti `.src` di `<audio>` element yang SAMA dengan crossfade 400ms, no-op kalau src-nya sama (biar gak glitch restart).

- **File audio REUSE dari BrainBox** (`al-idrisi-games`), bukan bikin baru — di-copy manual ke `public/audio/bgm/` (matematika.mp3 dari mathville, ipas.mp3 dari azkauniverse, bindo.mp3/binggris.mp3 dari azkacraft menu/game, drive.mp3 dari multipleazka, plane.mp3 dari mathville plane-bgm, hub.mp3 buat landing/ppkn/pai). **Kalau BrainBox nambah/ganti track-nya, gak otomatis kesinkron ke sini** — harus di-copy ulang manual, sama kayak pola copy konten soal di `content-pipeline`.
- **`TRACK_BY_SUBJECT`** map subject id → path, dipake `SubjectDetail.jsx` (`useBgmTrack(TRACK_BY_SUBJECT[subject])`). PPKn & PAI numpang track yang sama kayak Drive/hub (gak ada track BrainBox yang tematik pas buat itu) — bukan bug, keputusan sadar karena keterbatasan aset yang di-reuse.
- **`useBgmTrack(src)`** — hook kecil, `useEffect` yang manggil `switchTrack()` tiap `src` berubah. Dipasang di `Landing.jsx` (hub, sekarang juga tempat pilih kelas — lihat bagian "Kiko"), `SubjectDetail.jsx` (per-subject), `DriveMode.jsx`, `PlaneMode.jsx`. `PickSubject` gak perlu pasang sendiri — track hub dari Landing otomatis kebawa lewat (gak ada yang override sampai `SubjectDetail` mount).
- **`initBgmUnlock()`** dipanggil sekali di `App.jsx` (`useEffect` deps `[]`) — daftarin listener gesture (`pointerdown`/`touchend`/`click`/`keydown`) di `document`.
- **Tombol mute** (🔊/🔇) di header `Landing.jsx`, persisten `localStorage` (`jk_bgm_muted`) — BrainBox sendiri gak punya UI mute eksplisit, ini tambahan sadar karena konteks kelas (26 anak main bareng, guru mungkin butuh matiin).
- **Verifikasi**: `new Audio()` gak nempel ke DOM (kayak `new Image()`), jadi `document.querySelectorAll('audio')` gak nemuin apa-apa — dites via `window.__jkBgmDebug` (expose dev-only, `import.meta.env.DEV` guard, aman ada di kode produksi karena gak pernah ke-set di build). Diverifikasi genuinely: `paused:false`, `currentTime` maju beneran, `src` ganti bener dari hub.mp3 → matematika.mp3 pas masuk `/kelas/4/matematika`.
- **Fix `TRACK_DRIVE` ketuker (2026-08-07)** — user minta "musiknya samain dengan drive mode di alidrisi". Investigasi `md5` file: al-idrisi `mathville/bgm.js` CUMA punya 2 track total (`DEFAULT_SRC` "bgm.mp3" dipake DI MANA-MANA termasuk Drive Mode, gak ada track drive terpisah, + "plane-bgm.mp3" khusus Plane Mode). File `drive.mp3` di jagoan-kelas KEBALIK pas porting 2026-08-05 -- byte-identik sama `plane-bgm.mp3` (bukan `bgm.mp3`), jadi Drive Mode malah muter musik Plane Mode al-idrisi selama ini. Sementara musik Drive Mode al-idrisi yang bener (`bgm.mp3`) ternyata udah ke-copy tapi ke-mapping ke `matematika.mp3` (byte-identik, diverifikasi md5). Fix: `TRACK_DRIVE` (`src/data/bgm.js`) diarahin ke `/audio/bgm/matematika.mp3` (file yang udah bener isinya, gak bikin file baru) -- sekarang Drive Mode muter track yang SAMA PERSIS byte-nya kayak Drive Mode al-idrisi. `TRACK_BY_SUBJECT.ppkn` (masih nunjuk ke `drive.mp3` fisik, isinya plane-track) SENGAJA gak disentuh, di luar scope laporan user.

**Encourage message tanpa nama** (`src/data/encouragement.js`) — pool pesan semangat random (bukan AI-generated), dipilih berdasar accuracy tier (HIGH ≥80%/MID ≥50%/LOW di bawah itu), SENGAJA gak pakai nama anak (beda dari "Kirim Pesan" Parent Portal yang personal). Dipasang di layar hasil `TopicQuiz.jsx` dan `FocusRoundQuiz.jsx`, di-generate SEKALI pas `setResult()` (bukan tiap render) biar gak berubah-ubah pas user liat hasil.

## AI Tutor (v1 2026-08-05, upgrade ke chat interaktif "Tanya Kiko" 2026-08-06) — AKTIF, v1 & v2 VERIFIED DI PRODUCTION

**v1 (single-shot, DIHAPUS 2026-08-06)** — `api/generate-hint.js`, di-port dari BrainBox, tombol "🤖 Jelasin Lagi" ON-DEMAND CUMA pas jawaban salah. Dihapus total begitu `QuizRunner.jsx` pindah manggil endpoint v2 (gak ada pemanggil lain yang nyisa, jadi bukan dead-code-dibiarin). Detail histori (bug ESM/CommonJS yang sempet bikin `FUNCTION_INVOCATION_FAILED`, cara verifikasi production) ada di git history file itu kalau perlu ditengok lagi.

**v2 "Tanya Kiko" (2026-08-06)** — upgrade ke chat interaktif multi-turn pakai wajah [[Kiko]] (maskot resmi), bagian dari batch kerjaan Kiko 2026-08-06 (lihat bagian "Kiko — maskot resmi").

- **`api/kiko-chat.js`** (Vercel serverless, ESM `export default` — WAJIB, sama alasan kayak v1). Model tetep `claude-haiku-4-5`, system prompt Bahasa Indonesia dengan persona eksplisit "Kiko" (bukan lagi "tutor AI generik"). Nerima `messages` (riwayat percakapan `[{role,text}]`) + facts soal (`subjectName`/`gradeLabel`/`topic`/`question`/`correctAnswer`/`kidAnswer`/`explanation`/`sudahJawab`), balikin `{reply}`.
- **Aturan anti-bocor-jawaban**: field `sudahJawab` (dikirim dari `answered` state QuizRunner) dipakai system prompt buat larang AI kasih jawaban final SEBELUM anak submit — cuma boleh kasih petunjuk/cara mikir. Ini krusial karena beda dari v1: v2 BISA diajak ngobrol SEBELUM jawab (bukan cuma pas salah), jadi tanpa aturan ini anak bisa "curang" nanya jawaban langsung ke Kiko.
- **Aturan on-topic-only (2026-08-06)**: karena input teks bebas (anak bisa ngetik apa aja, gak cuma quick-reply), system prompt eksplisit larang Kiko ngikutin topik di luar soal yang lagi dibahas (obrolan random, curhat, cerita, apalagi konten gak pantas buat anak SD) — instruksinya: tolak ramah 1 kalimat, arahin balik ke soal, JANGAN ceramah panjang soal aturannya. Ini guardrail prompt-level doang (gak ada filter/moderation layer terpisah) — cukup buat use-case sekarang (anak SD kelas 1-6, exposure rendah), tapi kalau nanti pola pemakaian keliatan aneh, prompt guardrail doang mungkin gak cukup.
- **`src/games/quiz/KikoTutorChat.jsx`** — komponen chat: tombol pill "Tanya Kiko" (wajah Kiko kecil) SELALU muncul di header soal `QuizRunner.jsx` (baris "Soal X/Y"), bukan lagi cuma-muncul-pas-salah kayak v1. Tap buka panel chat (bottom-sheet style, `position:fixed` full-viewport overlay, konsisten sama pola `OverlayCard` lain di app tapi custom karena butuh bentuk chat bukan card statis). Quick-reply chip (3 default + 1 tambahan "Kenapa jawabanku salah?" muncul kalau `answered && !isCorrect`) + input teks bebas. Riwayat percakapan di-reset tiap ganti soal (`resetKey={q.id}`).
  - **Kiko chat umum, di-refactor jadi reusable (2026-08-06, malam)** — feedback user "Kiko pastikan bisa dipencet jadi AI chat bot" (backpack 🎒 di hero Landing awalnya cuma PLACEHOLDER visual, lihat bagian "Kiko" → "Ronde revisi ke-2"). File di-split: `export function KikoChatPanel(...)` (CONTROLLED via prop `open`/`onClose`, isi UI panel + fetch logic doang, GAK ada tombol trigger sendiri) + `export default function KikoTutorChat(...)` yang bungkus `KikoChatPanel` + nambahin tombol pill "Tanya Kiko" internal (`QuizRunner.jsx` GAK PERLU ubah apa-apa, masih import default kayak biasa). `Landing.jsx` import `{ KikoChatPanel }` (named export) langsung, state `chatOpen` di-kontrol dari tap Kiko sendiri (bukan tombol pill terpisah).
  - **Mode `"quiz"` (default) vs `"general"`** — prop baru `mode` di `KikoChatPanel`. Bedanya: (1) quick-replies beda (`QUICK_REPLIES_GENERAL` vs `QUICK_REPLIES_QUIZ`), (2) pesan pembuka beda, (3) body yang dikirim ke `/api/kiko-chat` beda -- mode `"general"` CUMA ngirim `{messages}` (gak ada `subjectName`/`question`/`correctAnswer`/dst sama sekali, karena Landing gak lagi dalam konteks 1 soal). `api/kiko-chat.js` DETEK mode dari ADA-GAKNYA field `question` di body (bukan flag terpisah) — kalau gak ada, pake `GENERAL_SYSTEM_PROMPT` (guardrail konten tetep ada -- aman buat anak SD, tolak topik gak pantas -- tapi gak ada aturan "jangan bocorin jawaban" karena emang gak ada 1 soal spesifik yang dibahas).
  - Diverifikasi di browser: tap Kiko beneran buka panel mode general (quick-reply beda dari mode quiz kebukti bener), kirim 1 quick-reply = TEPAT 1 network request ke `/api/kiko-chat` (sempet keliatan "4 request" tapi itu network-log numpuk dari klik-klik test sebelumnya, bukan bug asli -- diverifikasi ulang abis reload bersih, 1 klik = 1 request persis).
- **Gagal manggil API** (network/`ANTHROPIC_API_KEY` belum ke-set/dev lokal yang emang gak punya route `/api/*`) = bubble balasan ramah dari Kiko ("Kiko lagi istirahat sebentar...") langsung di chat, BUKAN silent-fail-ilang kayak v1 (karena UI-nya persistent chat, bukan tombol yang bisa disembunyiin) — kuis TETEP jalan normal, filosofi "never blocks" tetep dipegang.
- **Verifikasi dev**: dites di dev lokal (`localhost:5173`, network request `/api/kiko-chat` beneran 404 karena Vite dev gak serve folder `api/` Vercel — EXPECTED, bukan bug) → konfirmasi fallback bubble error muncul dan kuis tetep jalan (jawab soal, quick-reply "Kenapa jawabanku salah?" muncul abis jawaban salah, riwayat chat gak ke-reset selama masih di soal yang sama).
- **Verifikasi production (2026-08-06, abis deploy)**: `curl` langsung ke `https://jagoan-kelas.vercel.app/api/kiko-chat`, DUA mode: (1) mode general (body cuma `{messages}`, gak ada `question`) — tes "kamu suka pelajaran apa?" balesnya masuk akal in-character, tes "ceritain film horor yang serem banget dong" nolak ramah + arahin balik, gak ngelanjutin topiknya; (2) mode quiz (body lengkap ada `question`/`correctAnswer`/dst) — tes pertanyaan off-topic ("siapa presiden Indonesia sekarang?") nolak 1 kalimat + arahin balik ke soal (bukti guardrail on-topic-only jalan beneran di production, bukan cuma di prompt doang). Kedua mode CONFIRMED jalan di production, bukan cuma dev.

## Games (Drive Mode / Plane Mode / DinoRace / Bo Bridge / Roaming car+dino / Math Race)

Folder `src/games/` — `drive/`, `plane/`, `dinorace/`, `shared/`. Drive & Plane Mode sekarang punya **hampir semua fitur lanjutan BrainBox** (di-port 2026-08-05, baca kode aslinya di `al-idrisi-games/mathville/script.js`, cari komentar "Drive Mode"/"PLANE MODE") — Drive: nitro ✅, water gun ✅, vehicle skin ✅; Plane: power-up ✅, boss ✅, respawn gauntlet ✅, endless+ramp ✅, high score ✅. Pola porting yang dipake: baca constant/logic asli di BrainBox, adaptasi value-nya APA ADANYA ke React (ref buat data per-frame, state React cuma buat hal yang jarang berubah) — jangan re-desain dari nol.
DinoRace (2-player racing) **BEDA POLA** dari Drive/Plane — bukan di-port ke React, tapi **file statis di-reuse LANGSUNG** (lihat detail di bawah), sesuai instruksi eksplisit user ("ngapain lo ngoding lagi... bisa ambil dari sana").

- **`shared/quickQuestion.js`** — generator soal kilat grade-aware (beda dari `generators_matematika.py` yang per-bab kurikulum). Rentang angka per kelas 1-6, dipakai Drive Mode & Plane Mode dua-duanya. BrainBox punya bank soal FIXED grade-4 (dari MathVille) karena mathville cuma 1 kelas; kita generate langsung per grade karena app-nya 6 kelas.
- **`shared/useJoystick.js` + `Joystick.jsx`** — analog stick, pola sama kayak BrainBox (drag dari tengah, vector -1..1). Posisi visual nub lewat React state, tapi `vecRef` yang dibaca game loop tiap frame TIDAK lewat re-render (biar gak lag) — persis alasan BrainBox manipulasi DOM langsung.
- **`drive/DriveMode.jsx`** — mobil dodge-obstacle + dino chase + quiz kilat pas nabrak obstacle. Entry: tombol "🚗 Drive" di `SubjectDetail.jsx` (khusus subject Matematika), route `/kelas/:grade/matematika/drive`.
  - Ada: 1 dino ngejar (bukan 2 di Hard), obstacle dodge, quiz-on-hit, lives/bite-immunity, difficulty picker, win/lose, **nitro boost + water gun + vehicle skin picker (2026-08-05, lihat di bawah)**.
  - **Immune 2 detik abis jawab soal (2026-08-07)** — request user: dino tetep gerak/ngejar posisi terakhir selama popup soal muncul (`gameRef.current.paused=true` selama itu), jadi begitu anak jawab dan game resume, kalau dino kebetulan udah deket banget mobil (atau pas bareng nabrak obstacle deket dino), bisa langsung digigit instan tanpa sempet ngapa-ngapain — kerasa gak adil. Fix: `POST_QUESTION_IMMUNE_MS=2000`, di-set ke `gameRef.current.immuneUntil` di dalem `setTimeout` yang sama pas `answerQuestion()` resume game (bareng `paused=false`) — REUSE mekanisme `immuneUntil` yang udah ada buat bite-immunity abis digigit (`BITE_IMMUNE_MS=3000`), collision-check-nya (`now >= g.immuneUntil`) gak berubah sama sekali, cuma nambah 1 tempat yang nge-set field itu. Diverifikasi lewat `gameRef` asli (React fiber scan, pola yang sama kayak verifikasi nitro/water-gun): simulasiin dino nempel PERSIS di posisi mobil abis resume — kondisi bite REAL dari kode produksi ngasih `false` (gak digigit) selama immune window, balik `true` (bite normal) begitu window abis.
  - **Nitro boost** — di-port dari BrainBox (`DRIVE_NITRO_*` constants di `mathville/script.js`), nilai SAMA PERSIS: hold buat 1.6x speed (`NITRO_MULT`), abis dalam 2.5 detik boost terus-terusan, isi ulang penuh 6 detik pas gak dipake. Tombol ⚡ + fuel bar **dipindah ke header atas** (2026-08-05, sebelumnya kanan-bawah) buat ngasih tempat aim-joystick water gun. Frame-based drain/regen (asumsi ~60fps, bukan real delta-time) — sama simplifikasi kayak `CAR_SPEED`/`DINO_SPEED` yang emang udah gitu dari awal di file ini. State-nya nempel di `gameRef.current.nitroFuel` (bukan React state, biar gak re-render tiap frame) + `boostingRef` buat status tombol lagi ditekan/enggak.
  - **Water gun (2026-08-05)** — di-port dari BrainBox (`DRIVE_WATER_*`), joystick kanan-bawah terpisah (`aimJoystick`, komponen `Joystick` yang sama di-reuse) buat aim 360°, di luar deadzone (`WATER_AIM_DEADZONE=0.3`) = nembak jet 💦. Range 128px, cone ±18° (`WATER_CONE_RAD`), tangki abis 1.5 detik nonstop lalu cooldown 1.5 detik. Kena dino TOTAL 3 detik (akumulatif lintas semburan, `dinoWetFrames`) trigger slow 2 detik -30% speed (`dinoSlowFrames`/`WATER_SLOW_MULT`) yang di-apply ke `DINO_SPEED` bareng `dinoDiffMult`. Visual jet (`waterJetRef`) di-rotate ke arah aim tiap frame, sama pola ref-DOM-langsung kayak mobil/dino.
  - **Vehicle skin picker (2026-08-05, SVG top-down real 2026-08-06)** — awalnya cuma swap emoji (`Blaze🚗/Comet🚕/Turbo🚙/Sunburst🚓/Nova🏎️`) karena app ini emoji-based dari awal, bukan custom art kayak BrainBox. **Diganti total 2026-08-06** (bug report user: "mobil dan dinonya miring") — `src/games/drive/vehicleArt.jsx` (file baru) nyimpen SVG top-down asli DI-PORT PERSIS dari al-idrisi-games `mathville/index.html` + `VEHICLE_SKINS.car` (`script.js`), id skin juga disamain (`blaze/comet/turbo/sunburst/nova`, dulu `car/taxi/suv/police/race` — preferensi skin lama di localStorage reset sekali ke default, minor & diterima). **Root cause bug lama**: emoji font GAK "nose-up" (orientasi natural beda-beda per emoji/platform), padahal `headingCss()` (`+90` offset) dikalibrasi khusus buat art yang DIGAMBAR nose-up (0deg=ngadep atas) — rotate emoji pakai formula itu jadinya keliatan miring/salah arah. SVG al-idrisi emang digambar nose-up dari sononya, jadi `headingCss()` gak diubah sama sekali, cuma ganti art-nya doang. `VehicleSkinSvg` (helper component) dipake di 3 tempat (baris dekoratif picker-phase, grid pilih skin, mobil in-game) + efek glow pulse per-skin (`.jk-vehicle-glow` + `--vehicle-glow` custom property, `tokens.css`) — juga di-port dari al-idrisi.
  - **Dino juga SVG top-down asli sekarang** (`DinoSvg` di `vehicleArt.jsx`, sama file/alasan kayak di atas) — GANTIIN emoji 🦖 polos yang sama-sama kena bug "miring". Termasuk animasi jalan kaki (`.jk-dino-leg-l`/`.jk-dino-leg-r` + class `jk-dino-walking` di-toggle tiap frame pas dino gerak, `tokens.css` — `transform-box:fill-box` WAJIB biar kaki muter dari pangkalnya sendiri, bukan dari pojok viewBox svg).
  - **Wajah [[Kiko]] di kaca depan (2026-08-06)** — `kikoFaceRef`, elemen `<Kiko size={11}/>` (dikecilin dikit dari `size={13}` biar muat di windshield SVG asli yang lebih sempit dari kotak emoji lama) nested DI DALAM `carRef` (jadi ikut posisi mobil kemanapun, gak perlu ref/state posisi terpisah), tapi di-COUNTER-ROTATE tiap frame (`rotate(${-headingCss(angle)}deg)`) di rAF loop yang sama tempat `carRef` di-rotate ke arah gerak — biar wajahnya selalu keliatan tegak/gak ikut muter pas mobil belok, kesan "nempel di kaca depan" bukan "muter bareng bodi mobil". Kosmetik doang, gak ganti skin apapun (semua 5 skin dapet wajah yang sama, posisi `top` disesuaikan ke ~30% biar pas di windshield rect SVG blaze/comet/sunburst/nova; turbo viewBox-nya beda dikit tapi gak di-tuning khusus, cukup deket).
  - Diverifikasi di browser: skin picker grid nampilin 5 art beda-beda (Nova ada decal petir), mobil+dino in-game top-down bener (gak miring) di posisi awal, disimulasiin rotate manual (90deg mobil, 45deg dino) via ref DOM tetep keliatan bener sebagai top-down (roda/kaki ke arah yang masuk akal), bukan cuma pas diem doang.
  - **Fix Turbo "kebalik" (2026-08-07)** — user lapor "mobil ijo kebalik ga?" pas cek picker "Ganti Mobil". Root cause: SVG Turbo (`vehicleArt.jsx`, di-port PERSIS dari al-idrisi termasuk elemen ini) nempatin bar aksen (niatnya rear spoiler) DI DEPAN body (`y=0`, sebelum windshield) alih-alih di belakang -- statis keliatan simetris/gak masalah, tapi begitu digabung sama konvensi nose-up (`headingCss()`, depan = atas viewBox), bar itu jadi "mimpin" di ujung nose kayak spoiler yang kebalik ke depan. 4 skin lain (Blaze/Comet/Sunburst/Nova) gak kena karena emang gak punya elemen aksen ekstra di luar body pokok. Fix: body/windshield/roda Turbo digeser biar proporsinya PERSIS sama Blaze (nose-up bener, `y=1` bukan `y=5`), spoiler bar dipindah ke `y=39-44` (belakang, nempel abis roda belakang) -- posisi rear spoiler yang bener. Perubahan ini CUMA di jagoan-kelas (`al-idrisi-games` read-only, gak disentuh). Diverifikasi: injeksi JS langsung ke `carEl.style.transform` (scale 6x buat lihat detail) di picker + in-game, windshield sekarang di atas (match 4 skin lain), bar aksen di bawah/belakang.
  - **Gambar ulang dino, masih "belum tampak atas" (2026-08-07)** — user cek ulang: "dino... belum tampak atas" walau udah diklaim top-down 2026-08-06. Root cause: `DinoSvg` versi lama (SAMA PERSIS kayak al-idrisi `#drive-dino`) punya 1 lingkaran kepala + 1 titik merah DI TENGAH (mata nyatu satu, kesannya wajah ngadep depan/kamera) + overlay biru transparan mirip kaca depan mobil -- bahasa visualnya beda dari mobil yang jelas top-down (4 roda pojok + kaca tengah), jadi walau secara TEKNIS udah nose-up (kepala di atas, ekor di bawah), gak KELIATAN top-down. Fix (desain ulang total, bukan lagi 1:1 al-idrisi): 2 titik mata di SISI kiri/kanan kepala (bukan 1 titik tengah -- mata beneran di samping kepala kalau dilihat dari atas), 3 duri punggung (dorsal spike) di garis tengah badan (petunjuk visual umum "dilihat dari atas"), 4 kaki (2 pasang depan+belakang) di samping badan (bukan 2 kaki doang). `jk-dino-leg-l`/`jk-dino-leg-r` dipake ulang di kedua pasang kaki (kiri selalu -l, kanan selalu -r) biar animasi jalan (`tokens.css`) tetep jalan tanpa CSS baru. Diverifikasi: game di-pause via manipulasi `gameRef.current.paused` langsung (React fiber scan, teknik yang sama kayak verifikasi nitro/water-gun), dino di-scale 6x via `style.transform` buat lihat detail dari dekat -- sekarang jelas top-down (mata samping, duri tengah, 4 kaki mengangkang), dicek juga ukuran normal in-game (26px) masih kebaca jelas.
  - **Verifikasi teknis penting**: `requestAnimationFrame` **gak jalan sama sekali** di browser-pane pas tab background/gak fokus (limitation tooling yang sama persis kayak yang dicatat BrainBox soal Plane Mode testing) — dicoba paksa `document.hidden=false`+`visibilitychange` event, tetep gak ngaruh. Semua fitur di atas diverifikasi lewat REF ASLI (`gameRef`/`boostingRef`/vecRef joystick) langsung dari React fiber component (`__reactFiber$` key di DOM node), BUKAN simulasi terpisah:
    - Nitro: konfirmasi tombol ⚡ beneran nyetel `boostingRef.current=true`, formula drain/regen dijalanin manual di atas OBJEK REF ASLI 200+400 kali (simulasi ~3.3s boost + ~6.7s idle) — hasil abis ke 0 lalu balik penuh ke 100 persis target.
    - Water gun: set `aimJoystick.vecRef` langsung ke arah dino (dino sengaja didekatin ke mobil dulu biar dalam range 128px), trigger beberapa real frame tick lewat interaksi asli (drag) — `waterStreamFrames` dan `dinoWetFrames` naik BARENG persis 5=5, membuktikan deteksi range+cone+akumulasi kerja bareng logic firing yang sama.
    - Vehicle skin: dipilih via UI asli (klik "Nova"), persist ke `localStorage`, dan ke-apply beneran ke sprite in-game (bukan cuma di picker) — diverifikasi via screenshot.
  - **Masih BELUM ada** (fitur BrainBox lain yang sengaja di-skip): city-markers-ke-chapter, dino obstacle-avoidance steering (dino kita jalan lurus ke arah mobil, gak belok-belok ngindarin rintangan kayak BrainBox `dinoSteerAngle`). Kalau mau lanjutin salah satu ini, pola portingnya sama kayak nitro/water-gun: baca constant/logic asli di `al-idrisi-games/mathville/script.js` (cari komentar section-nya), adaptasi ke React (ref buat data per-frame, state React cuma buat hal yang jarang berubah).
- **`plane/PlaneMode.jsx`** — shmup vertikal, auto-fire, musuh turun+nembak balik, quiz berkala = bomb semua musuh. Entry: tombol "✈️ Plane" di sebelah Drive, route `/kelas/:grade/matematika/plane`.
  - Ada: ship+joystick, auto-fire, 1 tipe musuh (+variasi sine drift), musuh nembak balik (aimed, ada spread error), lives+invuln flash, ledakan visual, quiz berkala = bomb, **power-up, boss fight, respawn gauntlet, endless mode + ramp, high score persisten (2026-08-05, lihat di bawah)**.
  - **Fase lanjutan (2026-08-05)** — di-port dari BrainBox `mathville/script.js` (Fase 5/6 + v2), nilai diadaptasi (bukan endless BrainBox yang murni, kita batasi skala biar cocok sama app 6-kelas):
    - **Power-up**: musuh biasa mati → 22% (`POWERUP_DROP_CHANCE`) drop salah 1 dari 5 tipe (`POWERUP_TYPES`, pick uniform), jatuh pelan, dipungut nabrak badan pesawat: ⚡ rapid-fire (8 detik, 2x fire rate), 🛡️ shield (6 detik, nyerap 1 hit), ❤️ heal (instan, `setLives` +1 dibatas `MAX_LIVES`), 👯 wingmen (10 detik, spawn 2 pesawat kecil 🛩️ offset ±11% dari ship, auto-fire interval NORMAL — gak kepengaruh rapid-fire, sama kayak al-idrisi), 🔱 spread (10 detik, `fireShip()` NAMBAH 2 peluru miring ±18deg di samping yang lurus, jadi 3 total per tembakan, BUKAN gantiin yang lurus). **Awalnya (2026-08-05) cuma 2 tipe** (rapid/shield doang, coin-flip 50/50) — **DILENGKAPIN ke 5 (2026-08-06)** nyusul feedback user "power up ada semua ya samain kaya al idrisi", di-port dari `PLANE_POWERUP_TYPES` al-idrisi persis (constant value + formula angle sama semua).
    - **Boss fight**: skor nyampe threshold (mulai 15, +15 tiap boss berikutnya) → musuh biasa berhenti spawn, boss 🐉 muncul (HP 8, gerak kiri-kanan mantul, nembak lebih sering). Jawaban benar = bom musuh **+ damage 3 ke boss** (`BOSS_QUESTION_DAMAGE`), bukan cuma bom musuh biasa. Boss kalah = +3 skor bonus, density musuh naik 1.2x (compounding), lanjut main (BUKAN menang-lalu-selesai).
    - **Respawn gauntlet**: nyawa habis TIDAK langsung game over — kalau `respawnsUsed < 3`, buka overlay "💔 Kesempatan Terakhir!" (beda dari overlay soal biasa), jawab 3 soal benar BERTURUT-TURUT (salah cuma reroll, progress gak turun) buat balik main full nyawa. Abis 3x kesempatan respawn, baru beneran game over.
    - **Endless + high score**: gak ada lagi "skor target buat menang" — main terus sampe nyawa+respawn abis. High score per-kelas kesimpen `localStorage` (`jk_plane_highscore_kelas{N}`, BUKAN Firestore — ini rekor lokal per device, beda dari XP yang kesimpen ke akun).
    - **2 bug KETEMU & DIFIX pas ngerjain ini** (baik buat diinget kalau ubah game loop lain yang mirip):
      1. Awalnya `useEffect` game loop punya `score` di dependency array (buat baca skor terbaru pas cek boss threshold) — ternyata itu bikin loop RESTART (+ `cleanupWorld()` ke-panggil, world ke-cleanup) tiap kali skor berubah (tiap musuh mati!). Fix: skor "beneran" dipindah ke `gRef.current.score` (dibaca sinkron di frame loop), React state `score` cuma buat display, `bumpScore()` helper update dua-duanya, deps balik ke `[phase]` doang.
      2. Respawn-gauntlet awalnya nulis logic keberhasilan (`setRespawning(false)`, `setLives(...)`, dst) DI DALAM parameter functional-updater `setRespawnProgress(p => {...})` — StrictMode dev me-double-invoke updater function kayak gitu, jadi side-effect-nya bisa kepanggil 2x. Fix: progress asli dipindah ke `respawnProgressRef` (ref biasa), functional-updater gak dipake lagi buat percabangan logic.
    - **Verifikasi**: sama kayak nitro, `requestAnimationFrame` gak reliable di tab background browser-pane. Kali ini malah lebih beruntung — abis beberapa tool-call (screenshot/interaksi/tunggu) `frame()` sempat jalan beneran cukup banyak kali, jadi bisa diverifikasi END-TO-END LEWAT GAMEPLAY ASLI (bukan simulasi manual kayak nitro): set `g.score=15` via ref asli → boss beneran spawn (`g.boss.hp=8`) → jawab soal benar via tombol asli → `hp` 8→5, skor 15→17 (persis `+2`) → set `hp=2` → jawab benar lagi → boss KALAH (`boss:null`), skor 17→22 (`+2+3` bonus persis), `bossesDefeated:1`, `bossThreshold` 15→30, `enemyDensity` 1→1.2 — SEMUA angka cocok pas sama formula. Respawn gauntlet juga diverifikasi lewat 3x klik jawaban benar asli: `respawnProgressRef` 0→1→2→3, abis itu `respawnsUsed:1`, `paused:false`, nyawa balik `❤️❤️❤️`. Power-up pickup CUMA direview kode (gak sempet ke-tick pas di-test, tapi pattern-nya identik sama bullet-vs-boss collision yang udah kebukti jalan).
  - Elemen bullet/musuh/power-up dimanipulasi langsung ke DOM (`document.createElement` + `style.left/top` tiap frame, BUKAN React state) — SAMA PERSIS pola performa BrainBox, krusial buat FPS yang smooth pas banyak bullet di layar.
  - **Pesawat SVG + picker 5 skin (2026-08-06)** — feedback user "pesawatnya kenapa miring? lurus aja kedepan" — SAMA ROOT CAUSE kayak bug mobil/dino Drive Mode (lihat bagian "Vehicle skin picker" di atas): emoji 🚀 kebanyakan platform DIGAMBAR diagonal/meluncur, padahal ship di sini gak PERNAH di-rotate sama sekali (selalu ngadep sama, beda dari mobil Drive Mode yang muter ngikutin arah gerak) — jadi gak ada rotasi buat di-benerin, cuma art-nya doang yang salah gambar. Fix: `src/games/plane/planeArt.jsx` (file baru, pola sama persis kayak `vehicleArt.jsx` Drive Mode) — SVG nose-up asli di-port dari al-idrisi `VEHICLE_SKINS.plane` (5 skin: falcon/inferno/viper/solstice/ghost, id+warna glow sama persis). Picker "Pilih Pesawat Kamu" ditambahin di picker-phase (tombol "Ganti Pesawat: X" → grid 5 skin, PERSIS pola UI Drive Mode punya), persisten `localStorage` (`jk_plane_vehicle_skin`).
  - **Diverifikasi (2026-08-06)**: `requestAnimationFrame` GAK ticking sama sekali kali ini (beda dari sesi Fase-5 sebelumnya yang sempet beruntung) — power-up baru (wingmen/spread) diverifikasi via simulasi manual PERSIS logic-nya di atas REF ASLI (`window.__gRef`, dicari lewat generic React-fiber hook-scan karena nama komponen ke-minify) + DOM asli: spread → 3 bullet ke-push ke `g.bullets` dengan `vx` [0, -0.68, +0.68] (persis `sin(18°)*2.2`), wingmen → 2 elemen 🛩️ ke-render di 39%/61% (offset ±11% dari ship 50%) y=88% (+6% dari ship) — kedua-duanya ke-screenshot dan keliatan bener secara visual. Picker 5 skin + ship lurus (nose-up) diverifikasi visual langsung (screenshot, semua 5 desain beda-beda + glow warna kebukti render bener).
- **DinoRace beneran (2026-08-05, SELESAI, pola reuse-file-statis)** — file statis `al-idrisi-games/dinorace/index.html` (+ `assets/`, icon, `manifest.json`) di-copy APA ADANYA ke `public/dinorace/`, di-serve sebagai static page (BUKAN route React/SPA) — persis pola BrainBox sendiri nge-embed DinoRace-nya (folder game konstan disalin, bukan di-reimplement). Cuma 2 baris diubah dari sumber aslinya: tombol "🏠" balik ke `/kelas` (bukan `../` hub BrainBox), sama title/aria-label.
  - **Firebase RTDB SENDIRI, bukan numpang al-idrisi-games** (dibikin user manual di Firebase Console 2026-08-05 sore, project `jagoan-kelas`, region asia-southeast1, `firebaseConfig` di `public/dinorace/index.html` udah diganti ke project sendiri). Rules di-set `.read: true, .write: true` (allow semua, sama pola kayak al-idrisi-games pakai — gak ada data sensitif di path `dinorace_games`, cuma posisi/skor race). **Diverifikasi write+read+delete beneran jalan** lewat `firebase.database().ref('dinorace_games/_healthcheck')` langsung di browser, bukan cuma asumsi dari config doang. Sempet kena `PERMISSION_DENIED` pas rules masih default locked-mode, fixed abis user publish rules allow-all.
  - Kalau nanti RTDB project lain (Drive/Plane atau fitur lain) butuh Realtime Database juga, database `jagoan-kelas` yang ini bisa dipakai bareng (satu RTDB per project bisa nampung banyak top-level path).
  - Game aslinya SUDAH pure survival (lives-based `hitMe()`/`killMe()`, gak ada mekanik quiz/revive-pakai-soal) — permintaan "apus soalnya, jadi pure game aja" ternyata udah kepenuhi dari sumbernya, gak ada yang perlu dihapus lagi.
  - Ada mode Solo Play juga (gak wajib nunggu 2 device buat nyoba).
  - **Histori entry point** (v1 2026-08-05 → v2 2026-08-06 → FINAL 2026-08-06): v1 = `WanderingKiko.jsx` patroli dekoratif + tombol pill terpisah "🦕 DinoRace". v2 (revamp Landing, lihat bagian "Kiko" poin "REVAMP TOTAL") = tombol pill DIHAPUS total dari Landing (kelewat nonjol/berlabel), `WanderingKiko.jsx` JUGA DIHAPUS (gak relevan lagi begitu Kiko jadi hero statis besar, bukan yang jalan-jalan). **GANTINYA**: `components/WalkingDino.jsx` (file baru) — dino 🦕 (emoji polos, BUKAN `DinoSvg` dari `vehicleArt.jsx`/Drive Mode yang didesain top-down buat di-rotate; ini jalan SAMPING/horizontal jadi gak butuh "nose-up", emoji biasa udah pas) jalan SATU ARAH kiri→kanan terus-terusan, ditaruh di bawah grid lingkaran kelas. Tap = `window.location.href="/dinorace/index.html"` LANGSUNG (sama kayak tombol pill lama, cuma bukan tombol berlabel lagi — "easter egg santai" keliatan tapi gak dilabelin, beda dari secret-tap-sequence yang emang sengaja sembunyi total). Ke-inspirasi pola roam-dino al-idrisi (dino/mobil jalan-jalan di hub), tapi disederhanain jadi cuma 1 arah + 1 karakter (gak ada mobil/chase-mechanic di sini, itu punya `RoamingCarDino.jsx` di `PickSubject.jsx`, beda konteks). (2) easter egg lama tetap ada: `useSecretTap.js` (tap 6 kuadran hero Landing sesuai urutan rahasia `TL,TR,TL,TR,BL,BR`) → `/rahasia/dinorace` (`DinoRaceUnlock.jsx`) — sekarang tombolnya beneran ngarah ke game (dulu cuma "segera hadir"). **Urutan tap sequence-nya SENGAJA gak didokumentasiin di UI/README publik manapun** (biar tetep rahasia) — cuma ada di kode ini.
  - **2 bug ketemu & difix pas verifikasi (2026-08-06, malam)**: (1) **arah muka kebalik** — emoji 🦕 orientasi natural-nya (gimanapun font/platform render dia) gak otomatis "ngadep arah gerak", jadi keliatan jalan mundur pas animasi geser ke kanan. Fix: `transform: "scaleX(-1)"` statis di tombolnya (aman ditumpuk sama `animation` yang cuma nyentuh `left`/`margin-top`, gak nyentuh `transform`, jadi gak konflik) — diverifikasi lewat debug sementara (`fontSize` gede + `animation:"none"`) buat screenshot close-up, kepala/leher kebukti ngadep KANAN (arah gerak) abis di-flip. (2) **loncat balik ke kiri masih keliatan** (bukan "ngilang di ujung layar lalu muncul lagi" kayak diminta) — sebelumnya keyframe cuma `left: 2% → 90%`, MASIH DI DALAM area keliatan pas snap balik. Fix: extend ke `left: -12% → 112%` (di luar 0-100%, ke-clip otomatis sama `overflow:hidden` Shell.jsx di titik itu) jadi transisi snap-back-nya kejadian pas dino udah bener-bener invisible di kedua ujung -- gak perlu logic show/hide tambahan, murni manfaatin overflow clipping yang emang udah ada.
  - Diverifikasi di browser: dino keliatan gerak (posisi beda antar screenshot beberapa detik, animasi CSS jalan meski `requestAnimationFrame` gak reliable di browser-pane background tab — ini CSS `animation` murni jadi gak kena limitation yang sama), tap dino beneran navigate keluar SPA ke `/dinorace/index.html` (halaman DinoRace asli muncul), tap kartu kelas yang ke-tutupan visual dino tetep jalan normal.

- **Roaming car+dino (2026-08-05, SELESAI; DIPINDAH ke PickSubject 2026-08-06)** — `components/RoamingCarDino.jsx`. Di-port dari hub landing al-idrisi-games (`index.html`, cari komentar "ROAMING CAR + DINO") — mobil jalan random ke titik acak di dalam kotaknya sendiri (`pickTarget()`), dino ngejar dengan kecepatan sinus (`ROAM_DINO_BASE_SPEED` + `sin(t*1.3)*ROAM_DINO_AMPLITUDE`) tapi jaraknya dibatasin `ROAM_MIN_DIST` biar GAK PERNAH nangkep. Posisi di-update via ref/DOM langsung (`style.transform`), bukan React state, biar gak re-render tiap frame — pola sama kayak Drive/Plane.
  - **Tap mobil = popup kecil pilih Drive/Plane (2026-08-06, iterasi ke-3)** — histori: (1) awalnya langsung `navigate` ke `/matematika/drive`, (2) diubah ke `navigate` ke `SubjectDetail` Matematika (`/kelas/:grade/matematika`, biar anak liat pilihan Drive/Plane/Math Race dulu), (3) SEKARANG (final) `setShowPicker(true)` buka `OverlayCard` kecil ("Main apa nih?" + 2 tombol "🚗 Drive"/"✈️ Plane") LANGSUNG DI LAYAR YANG SAMA, gak pindah layar sama sekali sampe anak beneran milih salah satu — feedback user "langsung muncul card kecil pesawat/mobil, kaya di al-idrisi". Milih salah satu langsung `navigate` ke `/kelas/:grade/matematika/drive` atau `/plane`.
  - **Soal Drive/Plane udah "campuran" dari awal** (konfirmasi user 2026-08-06, gak ada perubahan kode) — `shared/quickQuestion.js` `generateQuickQuestion()` pilih operasi (`+`/`-`/`x`/`:`) RANDOM tiap soal (`choice(range.ops)`), bukan dari 1 topik/bab tertentu, jadi soal kilat di Drive/Plane emang udah campur berbagai operasi sesuai kelas dari awal desainnya.
  - **Dipindah dari `Landing.jsx` ke `PickSubject.jsx` (2026-08-06)** — penempatan awal di `Landing.jsx` ternyata salah analogi: di al-idrisi, dekorasi ini nempel di hub tempat milih GAME (`index.html`), yang padanan strukturalnya di Jagoan Kelas itu `PickSubject.jsx` (layar milih pelajaran, isinya kartu-kartu kayak hub BrainBox), BUKAN `Landing.jsx` (layar sambutan doang, gak ada di BrainBox). Dipindahin ke dalam list kartu pelajaran (`PickSubject.jsx`, di atas `SUBJECTS.map(...)`) biar match.
  - **`data/lastGrade.js` DIHAPUS** — workaround "kelas terakhir dibuka" itu cuma perlu SELAMA komponennya nangkring di `Landing.jsx` (layar tanpa konteks kelas). Begitu pindah ke `PickSubject.jsx` yang udah punya `grade` dari URL params, `grade` tinggal di-pass sebagai PROP — gak ada lagi tracking localStorage terpisah, lebih simpel dan lebih akurat (dulu bisa nyasar ke kelas yang gak relevan kalau anak baru pertama kali buka app dari layar lain). `SubjectDetail.jsx` gak manggil `setLastGrade` lagi.
  - Diverifikasi: transform car/dino beneran ke-set setelah mount (bukan cuma di titik awal), tap tombol mobil beneran navigate ke Drive Mode SESUAI kelas yang lagi dibuka (bukan lagi hardcoded/localStorage) di browser asli.
  - **Bukan lagi kotak/card kecil (2026-08-06, feedback user "drive mode jangan didalam card, biarin dia bisa jalan2 selayar")** — wrapper-nya dulu `height:92` + background+borderRadius (keliatan kayak card list biasa, roaming area sempit banget). Sekarang `position:absolute` TRANSPARAN (gak ada background/border lagi) nutupin `height:"100vh"` (satu layar penuh, pakai unit viewport langsung karena Shell.jsx gak ngebatesin tinggi parent-nya — lihat komentar di `RoamingCarDino.jsx`), `pointerEvents:"none"` di layer-nya biar gak nge-block tap kartu pelajaran DI BELAKANGnya, KECUALI tombol mobil sendiri yang eksplisit `pointerEvents:"auto"`. `PickSubject.jsx` wrapper div ditambahin `position:"relative"` biar area roaming ke-bound ke lebar konten app (~398px), bukan lebar viewport browser penuh (penting di desktop preview yang jauh lebih lebar dari card app). Diverifikasi: `getBoundingClientRect()` layer beneran 398×720 (bukan lagi 92px), klik kartu Matematika yang ke-overlay layer TETEP jalan (pointer-events pass-through kebukti), tap mobil TETEP navigate ke Drive Mode.

- **Bo Bridge (2026-08-05, SELESAI)** — beda dari status TERBARU al-idrisi (yang sempet nyabut mekanik quiz-nya jadi dekorasi doang), di sini user eksplisit minta versi **DENGAN game** (Glass Bridge Challenge), jadi di-bangun ULANG dari nol berdasarkan logic `azkacraft/script.js` (cari komentar "Bo Bridge"/"GLASS BRIDGE CHALLENGE") — source code-nya sendiri gak di-reuse langsung (beda dari DinoRace) karena versi production al-idrisi SEKARANG udah gak punya game-nya lagi, cuma dekorasinya doang.
  - **`components/BoBridgeBanner.jsx`** — banner ambient (dino 🦕 jalan ngelewatin 9 kaca, looping, reveal-window berbasis progress, `requestAnimationFrame`) DI DALAM `SubjectDetail.jsx` (`(subject === "bindo" || subject === "binggris") && <BoBridgeBanner .../>` — cuma muncul di 2 subject bahasa itu). Tap banner = navigate ke `/kelas/:grade/:subject/bobridge`.
  - **`games/bobridge/GlassBridge.jsx`** — game beneran: tahan tombol buat jalan naik 10 kaca (rAF loop, posisi via ref), tiap nyampe kaca muncul soal MC 2 pilihan (1 jawaban bener dari `loadRawTopics(subject, grade)` + 1 salah acak dari opsi lain), salah = kaca retak + soal baru DI KACA YANG SAMA sampe `MAX_ATTEMPTS=3`, percobaan ke-3 masih salah = kaca pecah (`navigator.vibrate`), balik ke overlay hasil + tombol "Coba Lagi". Nyampe kaca ke-10 = menang.
  - **Karakter walker pakai dino 🦕** (bukan "Bo" maskot BrainBox — gak ada asetnya di project ini, dan dino udah jadi karakter berulang di jagoan-kelas lewat DinoRace/WanderingDino, jadi lebih konsisten). Player sprite in-game pakai lingkaran oranye top-down simpel, sama kayak sumber aslinya.
  - **Bug ketemu & difix**: `rollQuestion()` (dipanggil dari dalam rAF loop pas nyampe kaca) awalnya baca `pool` (React state) langsung — closure di dalam `useEffect(..., [stepIndex])` cuma dibuat ulang pas `stepIndex` berubah, jadi kalau `pool` masih `null` (soal belum ke-load) pas effect PERTAMA kali jalan, `rollQuestion` bakal SELAMANYA lihat `pool=null` biarpun soal udah ke-load belakangan — persis pola bug stale-closure yang udah pernah kejadian di Plane Mode (`score` di deps array). Fix: `poolRef` (ref, selalu ter-update), `rollQuestion` baca `poolRef.current` bukan state `pool` langsung.
  - **Bug ketemu & difix #2**: tile 10-kaca awalnya pakai `height: ${100/10}%` di flex container `column-reverse` — percentage height gak resolve dengan benar (semua 10 tile numpuk jadi strip tipis di 1 sudut, bukan ngisi penuh track). Fix: `flex: "1 1 0%"` di tiap tile (bagi rata main-axis space flex container), lebih robust daripada percentage height buat kasus "N item sama besar dalam flex container".
  - Diverifikasi end-to-end di browser: soal muncul pas nyampe kaca, jawaban salah → attempt naik + soal reroll, jawaban bener → kaca jadi hijau "done" + lanjut ke kaca berikutnya + attempt reset ke 1.

- **Math Race (2026-08-05, SELESAI)** — `games/mathrace/MathRace.jsx`, entry "🏁 Math Race" di `SubjectDetail.jsx` khusus Matematika (di bawah Drive/Plane), route `/kelas/:grade/matematika/mathrace`. BEDA dari DinoRace: sumber aslinya (`multipleazka` di al-idrisi-games) **gak ke-copy ke folder manapun di jagoan-kelas**, cuma dideskripsiin di CLAUDE.md BrainBox ("racing kuis matematika, multiplayer 2-3 pemain, RTDB realtime") — jadi ini dibangun dari NOL di React, bukan reuse file statis. Pola room-pairing (kode 6 karakter, `mathrace_games/{code}` di RTDB) niru struktur `dinorace_games/{code}` yang udah kebukti jalan di project ini.
  - **Race-nya BUKAN sinkron-posisi-per-frame** kayak DinoRace (gak perlu, gak ada "obstacle" bareng) — tiap pemain jawab **soal SENDIRI-SENDIRI** (beda soal per pemain, di-generate lokal dari `shared/quickQuestion.js` yang udah dipakai Drive/Plane Mode, bukan bikin bank soal baru), jawaban bener = progress bar maju 10% (10 soal = 100%). Progress ditulis ke RTDB tiap jawaban bener, listener `onValue` di semua klien gambar ulang track bareng. Placement (finis ke-berapa) dihitung via `runTransaction` di counter `finishCount` biar atomic walau 2-3 klien selesai hampir bareng.
  - **Solo Play** juga ada (gak wajib nunggu temen) — skip RTDB sepenuhnya, langsung main lokal, hasil cuma nampilin waktu tempuh (gak ada placement karena sendirian).
  - **Lobby**: Solo Play / Bikin Room (pilih 2 atau 3 pemain, host) / Join Room (input kode 6 digit). Host liat daftar pemain yang udah join real-time (RTDB listener), tombol "Mulai Race!" cuma aktif kalau minimal 1 pemain lain udah join, cuma host yang bisa mulai.
  - **Diverifikasi end-to-end pakai 2 tab browser beneran** (bukan cuma baca kode): tab A bikin room dapet kode, tab B join pake kode itu → tab A langsung update nampilin nama tab B (RTDB listener kebukti jalan REAL, bukan simulasi), host mulai race → KEDUA tab otomatis pindah ke layar racing (listener `status==='racing'` kebukti jalan), masing-masing dapet soal BEDA (bukti tiap klien generate soal sendiri-sendiri, bukan sinkron 1 soal buat semua), jawab bener di tab B → tab A ke-update nampilin progress bar tab B maju real-time TANPA reload. Solo mode juga diverifikasi penuh sampe soal ke-10 → layar "Kamu finis ke-1!" + waktu tempuh.
  - Data room test (`mathrace_games/GPJFLX`) sengaja gak dibersihin abis testing — beda dari akun test player yang WAJIB dihapus (ada nama anak), data race cuma posisi/skor doang, gak sensitif, sama kayak filosofi DinoRace.

## Parent Portal (`/parents`)

Pola disamain **persis** sama BrainBox `parents/index.html`/`script.js` (baca dulu itu kalau mau ubah sesuatu di sini). Poin kunci yang WAJIB dipertahankan kalau diubah:

- **Auth pakai PIN ANAK**, bukan PIN guru/dashboard terpisah — `signInAsChild()` di `authService.js` cuma alias ke `signIn()` yang sama dipakai anak login sendiri. Ini keputusan privacy yang sengaja: orang tua cuma bisa lihat data anaknya sendiri (butuh tau PIN anaknya), bukan semua murid. **Jangan pernah bikin 1 PIN admin buat semua data** — itu privacy bug persis yang dihindarin BrainBox.
- **Route `/parents` TIDAK di-wrap `RequireAuth`** — beda sesi dari login player utama, biar orang tua gak harus login sebagai anak dulu di app utama.
- **Semua 3 section udah jalan pakai data asli** (per 2026-08-04, lihat bagian "Progress Tracking + Focus Round"):
  - **"Kirim Pesan"** — 1-arah (`players/{id}.parentMessage: {text, sentAt, read}`, nimpa pesan lama, bukan thread), muncul sebagai popup `OverlayCard` di Landing anak pas dibuka lagi, ditandain `read:true` begitu di-dismiss.
  - **"Fokus Minggu Ini"** (Assign) — `TopicPicker` (shared component, sama yang dipake `FocusRoundPicker`) nulis ke `players/{id}.assignedTopics` (array topicId `subject:grade:babKey`, max 8, LINTAS KELAS — orang tua gak dibatasin ke 1 kelas kayak picker anak). Anak liat ini pre-checked pas buka Fokus Latihan sendiri dari `PickSubject.jsx` (`/kelas/:grade/fokus`) — TAPI cuma yang match kelas itu (lihat bagian "Progress Tracking" buat detail filter-nya).
  - **"Perlu Latihan Lagi"** (Needs Practice) — `computeWeakTopics()` di `progressService.js`, formula SAMA PERSIS BrainBox (`≥3 percobaan && akurasi<70%`, constants `MIN_ATTEMPTS`/`WEAK_ACCURACY`), baca dari `progress` map yang sama dipake locked/current/done. Title di-resolve dari `loadAllFocusTopics()` (gak didenormalisasi ke Firestore).
  - **XP per Pelajaran** (2026-08-04) — section baru di atas "Kirim Pesan", breakdown `computeXpBySubject()` (`progressService.js`) yang jumlahin field `xp` per bab di seluruh `progress` map per subject, dibandingin ke `SUBJECTS` (`content.js`) buat nama/emoji, bar proporsional ke subject XP tertinggi. Fix penting: `recordFocusRoundAttempt()` sebelumnya cuma nulis XP ke top-level `player.xp`, gak ke `progress.{subject}.{grade}.{babKey}.xp` — sekarang ditambahin juga biar breakdown ini akurat termasuk kontribusi Focus Round, bukan cuma practice biasa. Diverifikasi di browser: akun test 20 XP Matematika + 10 XP PAI → breakdown nampilin proporsi bar yang bener, total cocok 30 XP.
- Entry point: link kecil "Untuk orang tua →" di footer Landing (`Landing.jsx`), sengaja gak menonjol — sama alasan BrainBox: biar anak gak notice/ke-klik gak sengaja.

## Progress Tracking + Quiz Engine + Focus Round (selesai 2026-08-04)

Fondasi ini bikin app-nya "berfungsi" beneran (gak cuma nampilin daftar topik doang) — anak beneran ngerjain soal, progress kesimpen, XP naik, bab kebuka satu-satu.

**Schema** — `players/{id}.progress.{subject}.{grade}.{babKey} = {status, stars, xp, correct, wrong, lastAt}` (`progressService.js`). `status` CUMA pernah ditulis `"done"` — locked/current dihitung ULANG tiap baca (`computeStatuses()`: bab pertama yang belum `done` = `"current"`, sisanya `"locked"`), jadi gak ada 2 sumber kebenaran yang bisa out-of-sync kayak kalau "unlock next" ditulis eksplisit pas save.

- **`games/quiz/QuizRunner.jsx`** — komponen quiz reusable, dipake DUA jalur (practice per-topik normal DAN Focus Round). 1 soal/layar, handle `multiple_choice` (tombol) & `short_answer` (input teks, `normalizeAnswer.js` buat trim+lowercase compare). `onFinish({correct,wrong,total,results})` — `results` = `[{id,correct}]` per soal (pake `useRef`, bukan state, biar gak kena race batching pas dibaca tepat di titik finish), dipake FocusRoundQuiz buat atribusi correct/wrong balik ke topik asal soal.
- **`screens/TopicQuiz.jsx`** (`/kelas/:grade/:subject/topik/:babKey`) — practice normal, sample 8 soal acak dari `bab.soal`, `+10 XP`/jawaban benar, `recordTopicResult()` nulis `status:"done"` (INI yang bikin bab berikutnya kebuka).
- **`screens/FocusRoundPicker.jsx`** (`/kelas/:grade/fokus`) + **`FocusRoundQuiz.jsx`** (`/kelas/:grade/fokus/main`) — anak pilih max 8 topik lintas subject **DALAM 1 KELAS** (`loadFocusTopicsForGrade(grade)` di `focusTopics.js`), pre-checked dari `assignedTopics` (yang di-set orang tua, DI-FILTER ke kelas ini doang biar gak ada id ke-select yang gak nongol di list — assigned bisa lintas kelas dari sisi ParentPortal, tapi picker per-kelas cuma nampilin+ngitung yang match kelas yang lagi dibuka). Quiz-nya campur 3 soal/topik terpilih, `recordFocusRoundAttempt()` nulis correct/wrong+xp per topik TAPI **sengaja gak nyentuh `status`** (Focus Round nyumbang ke akurasi/weak-topic-calc doang, gak ganggu urutan locked/unlocked linear SubjectDetail) — sama filosofi kayak BrainBox misahin chapter-progress dari topicStats.
  - **Dipindah dari global `/fokus` ke per-kelas `/kelas/:grade/fokus` (2026-08-06)** — sebelumnya list topik nyampur SEMUA 6 kelas x 6 subject sekaligus (`loadAllFocusTopics()`, bisa puluhan grup, kepanjangan buat di-scroll anak). Sekarang scoped ke 1 kelas doang (`loadFocusTopicsForGrade()`, fungsi baru di `focusTopics.js`, `loadAllFocusTopics()` yang lama TETEP ADA buat ParentPortal yang emang butuh assign lintas kelas). Route lama `/fokus` DIHAPUS (bukan dibiarin nyisa), semua internal nav (`FocusRoundPicker`/`FocusRoundQuiz` back button, tombol "Kembali" abis selesai) diarahin balik ke `/kelas/${grade}` bukan `/` lagi.
- **`components/TopicPicker.jsx`** — checklist topik shared, dipake `FocusRoundPicker` DAN `ParentPortal` (Assign section) biar UI-nya konsisten.
- **Entry point Focus Round (diubah 2026-08-06)** — dulu tombol "🎯 Fokus Latihan" di `Landing.jsx` (di bawah "Ayo Main!"), SEKARANG kartu `GameCard accent="focus"` di `PickSubject.jsx` (di bawah 6 kartu pelajaran) — accent `focus` (ungu, `--product-focus`) sebelumnya CUMA kepake PPKn (`content.js`), sekarang dipakai 2 kartu (PPKn + Fokus Latihan) — bukan bug, GameCard cuma punya 5 accent buat 6 subject + 1 kartu tambahan ini jadi reuse warna emang gak terhindarkan (accent `math` sendiri udah dipakai 2x buat Matematika+B.Inggris dari awal).

`SubjectDetail.jsx` sekarang baca progress ASLI (`getSubjectProgress` + `computeStatuses`), bukan hardcoded lagi — topik current/done bisa diklik → `TopicQuiz`, "Lanjut Belajar" langsung lompat ke topik current.

### Bug: layar bisa nyangkut selamanya di "Memuat..." kalau fetch gagal (fix 2026-08-06)
User report: "matematika kelas 1 kenapa kosong" — screenshot nunjukin `SubjectDetail.jsx` nyangkut permanen di "Memuat topik...". **Root cause BUKAN data rusak** (`kelas_1.json` Matematika normal, diverifikasi ulang di dev — 7 bab kebaca sempurna) — kemungkinan besar Firestore gagal di-fetch di device user (jaringan/ad-blocker/dll), dan `useEffect`-nya cuma punya `.then()` TANPA `.catch()`, jadi promise reject = `setLoading(false)` gak pernah kepanggil = UI nyangkut selama-lamanya tanpa pesan error atau cara recover, anak/user cuma liat "kosong".
- **Fix di `SubjectDetail.jsx`**: tambah `.catch()` → state `loadError` + tombol "Coba Lagi" (`retryTick` counter di dependency array `useEffect`, increment buat re-trigger fetch). Diverifikasi PERSIS via forced-error sementara (`Promise.all([..., Promise.reject(...)])`, direvert abis kebukti UI-nya bener) — muncul pesan "Gagal muat topik. Coba cek koneksi internet kamu, ya!" + tombol Coba Lagi yang beneran re-fetch, bukan macet.
- **Pola yang SAMA diterapin ke layar lain** yang punya `.then()` tanpa `.catch()` di initial data-load: `TopicQuiz.jsx` (loadError + tombol Coba Lagi), `FocusRoundPicker.jsx` (loadError + tombol Coba Lagi, sama-sama manggil Firestore lewat `getAssignedTopics`). `FocusRoundQuiz.jsx` juga dikasih (tapi tombolnya "Pilih Topik Lagi" balik ke picker, BUKAN "Coba Lagi" — karena `topicIds` numpang di router state yang ilang kalau reload, jadi retry-di-tempat gak masuk akal di situ).
  - **SENGAJA GAK disentuh**: `GlassBridge.jsx` (Bo Bridge) dan `ParentPortal.jsx`'s `loadAllFocusTopics()` — dua-duanya cuma load CONTENT STATIS (`loadRawTopics`/`loadAllFocusTopics`, bukan Firestore langsung), jadi risiko reject jauh lebih rendah dari `getSubjectProgress`/`getAssignedTopics` yang beneran network call ke Firestore. `GlassBridge.jsx` juga punya rAF game-loop yang lebih kompleks/riskan buat disentuh demi failure mode yang probabilitasnya rendah.

`contentLoader.js` — `loadRawTopics()` (ganti nama dari `loadTopics` lama) sekarang include SOAL PENUH per bab (dibutuhin quiz), bukan cuma metadata. Masih lazy-load per kelas pakai `import.meta.glob`. `hasContent(subjectId)` (`SUBJECTS_WITH_CONTENT`) sekarang `true` buat SEMUA 6 subject (`matematika`/`ipas`/`ppkn`/`binggris`/`bindo`/`pai`) — gak ada lagi yang fallback "coming soon".

**Verified end-to-end** (browser, bukan cuma baca kode): selesain quiz bab-1 Matematika kelas 4 → bab-1 jadi "Selesai", bab-2 kebuka, XP naik — dicek ulang abis reload (baca Firestore fresh, bukan state lokal doang). Focus Round 3-topik → round 9 soal campur → tulis ke 3 topik terpisah → muncul bener di "Perlu Latihan Lagi" Parent Portal. Assign topik dari Parent Portal → muncul pre-checked di picker anak.

**Minor gap yang tersisa**: `stars` masih dihitung dari akurasi single-round doang (≥90%→3, ≥70%→2, ≥40%→1) — data-nya aman (`Math.max(stars, prev.stars)`), UI-nya udah ada tombol "Ulangi buat Naikin Bintang" (2026-08-04, lihat TODO #7) buat naikin lewat retry manual, tapi gak ada logic otomatis apa pun. XP breakdown per-subject dan tombol retry-bintang, dua-duanya UDAH kelar — lihat bagian Parent Portal & TODO list, bukan gap lagi.

## IPAS — "SolarQuest" (2026-08-07, LIVE sejak 2026-08-08 — halaman resmi, bukan mockup lagi)

User minta tampilan subject IPAS di-desain ulang niru **PERSIS** (bukan cuma "terinspirasi") tampilan `azkauniverse` ("SolarQuest") di `al-idrisi-games` -- jalur planet melengkung, pesawat yang terbang planet-ke-planet pas di-tap, maskot AI di dalem pesawat, easter egg dino. File: `src/screens/IpasQuestMap.jsx` (baru).

**Awalnya preview-only** (`/kelas/:grade/ipas/peta-mockup`) -- **di-wire jadi halaman resmi** setelah user gak sadar itu masih mockup ("ipas bukan udah pake solarquest ya? kok masi static bgt", ternyata buka jalur normal yang masih `SubjectDetail.jsx` lama). Pola fix SAMA PERSIS kayak Bindo (lihat bagian di bawah): route statis `/kelas/:grade/ipas` ditaro sebelum `/kelas/:grade/:subject` generik di `App.jsx`, `onBack` diubah dari `/kelas/${grade}/ipas` (dulu nunjuk balik ke `SubjectDetail.jsx` sebagai "yang asli") ke `/kelas/${grade}` (PickSubject), title header dari "Peta IPAS" jadi "IPAS", route `/peta-mockup` lama dihapus. Matematika (`MathTownMap.jsx`) MASIH mockup preview-only -- belum diminta di-live-in di ronde ini.

- **8 planet, bukan 5** — SolarQuest di-hardcode buat PERSIS 5 topik (`PATH_NODE_POS`/`PATH_SEGMENTS` array literal). IPAS jagoan-kelas selalu 8 bab/topik per kelas (semua kelas 1-6 sama, dicek lewat `loadRawTopics()`), jadi layout jalur DIGENERALISASI (`buildPathLayout(n)`) -- generate node position + bezier segment apa aja jumlah node-nya, bukan di-hardcode.
- **Planet art LITERAL di-port dari `azkauniverse/script.js` `planetSVG()`** (5 tema asli: sun/mercury/earth/mars/jupiter, SVG gradient+shape SAMA PERSIS termasuk hex color), di-cycle per index (`index % 5`) karena topik lebih banyak dari tema yang ada -- BUKAN dipetain ke isi bab (topik IPAS kayak "Tubuhku"/"Cuaca & Musim" gak nyambung ke planet beneran, planet cuma estetika). v1 mockup awal sempet pake planet generik pastel bikinan sendiri, DIGANTI TOTAL ke planet asli abis feedback user "desainnya plek2 ikutin solarquest ga bisa?".
- **Pesawat terbang ngikutin lengkung SVG asli** (`getPointAtLength`, bukan interpolasi garis lurus) -- `flyShipTo()` di-port dari `script.js` versi al-idrisi (hop antar node berurutan, avoid-radius biar gak numpuk pas ngelewatin planet yang gak jadi tujuan, sudut tangent buat rotasi pesawat). Tap pesawat = buka `KikoChatPanel` (mode `"general"`, REUSE komponen yang sama kayak Landing.jsx, BUKAN Bo -- ini game jagoan-kelas, bukan SolarQuest asli) + bubble hint oranye "Hai, ini Kiko! 👋" (styling disalin dari `.ship-bo-hint` SolarQuest).
- **Easter egg dino** — `DinoSvg` (dari `vehicleArt.jsx`, udah ada) + overlay visor kecil (lingkaran biru transparan) di kepalanya niru "helm astronot"-nya `dino-bonus-fab` SolarQuest, ngambang di pojok deket ujung jalur, animasi bounce+rotate SAMA PERSIS keyframe-nya (`dino-fab-bounce`), klik → `/dinorace/index.html` (route yang emang udah ada).
- **CSS/warna disalin persis dari `style.css` tema "colorful" (default)**: `--bg-grad` (`linear-gradient(160deg, #0b0f2e 0%, #1b1464 45%, #3a1b6e 100%)`), `--accent-yellow` `#ffd93d`, `--secondary` `#1266d8` (dipake gradient jalur yang "lit" + glow filter SVG), `--halo-glow` (glow pulse di planet yang unlocked), lock-chip bulat putih di pojok planet terkunci.
- **Starfield 3-layer parallax** di-port dari `buildPathStarfield()` (tile SVG data-uri, beda density/opacity/speed per layer, transform scroll-driven) -- tapi container-nya beda: SolarQuest scroll di `window`, app ini scroll di dalem div (`Shell` gak scroll body), jadi listener parallax dipasang di container div itu sendiri (`scrollRef`), bukan `window`.
- **Bintang kelap-kelip (2026-08-07, TAMBAHAN, BUKAN port)** — al-idrisi cuma punya dots statis, gak ada animasi nyala-redup sama sekali. User minta eksplisit "jangan lupa ornamen dan hiasan bintang2 kelap kelipnya yang banyak jadi kaya di antariksa bneran" -- ditambahin `TwinkleStars` (komponen baru): 50-140 titik kecil (density `height/22`, dibatasi 50-140) + beberapa aksen ✨ lebih gede (density `height/260`), posisi+delay+durasi animasi di-generate lewat `seededRandom(42)` (bukan `Math.random()` biasa) biar POSISI STABIL antar render (gak "loncat-loncat" tiap kali state lain berubah, misal abis progress fetch selesai) tapi tetep keliatan organik/gak seragam.
- **Bug ketemu+dibenerin**: bintang "kosong" (`☆` unicode, dipake buat progress 0/3 bintang) keliatan SOLID/penuh di font app ini (glyph fallback beda), padahal harusnya keliatan kosong buat topik yang belum dikerjain. Fix: ikutin pola SolarQuest sendiri -- pake glyph `★` YANG SAMA buat filled DAN empty, cuma beda WARNA (`STAR_FILLED` vs `LOCKED_COLOR`), bukan ganti karakter unicode.
- **Verifikasi**: dites di dev (`/kelas/4/ipas/peta-mockup`, waktu masih mockup), semua 8 planet render judul bab asli (bukan dummy), tap planet unlocked → beneran navigate ke `TopicQuiz` (`/kelas/4/ipas/topik/bab-1`), tap pesawat → chat Kiko beneran kebuka (bukan mockup), tap planet locked → no-op. Screenshot dikirim ke user 2x (v1 planet generik, v2 planet asli+starfield rame) via Playwright (`scratchpad/shot_ipas.mjs`, browser-pane screenshot gak kekirim otomatis ke user).
- **Diverifikasi ulang abis di-wire live (2026-08-08)**: `/kelas/4/ipas` nunjukin SolarQuest (bukan list lama), tombol back balik ke `/kelas/4` (bukan nyangkut), `/kelas/4/matematika` TETEP `SubjectDetail.jsx` (subject lain gak ke-affect), gak ada console error.
- **Belum dikerjain**: testing di 6 kelas (baru dites kelas 4).

## Matematika — "MathVille/Blockville" (2026-08-07, LIVE sejak 2026-08-08 — halaman resmi, bukan mockup lagi)

Pola yang sama kayak "IPAS" di atas, tapi sumbernya `mathville` (bukan `azkauniverse`) -- user: "untuk mathville inget ga di play alidirisi jg? ... yang ada truck jalan? didalamnya ada ai mascot kita bisa di click." File: `src/screens/MathTownMap.jsx` (baru).

- **Tema "Blockville Workshop"** (kayu/cherry/emas, `--wood #C1793E`/`--cherry #E4572E`/`--gold #F7C548`/`--taupe #D8C7AE`/`--cream #F7EEE0`) -- BEDA dari IPAS yang tema luar angkasa gelap. Background CREAM terang, bukan navy.
- **Truck 🚚 (bukan pesawat/kapal)** jalan di jalan kota ngikutin kurva **Catmull-Rom** (`catmullRomSegments`, BEDA dari bezier-per-segmen sederhana yang IPAS/SolarQuest pake -- kurva Catmull-Rom mulus ngelewatin SEMUA titik sekaligus, dihitung dari 4 titik tetangga per segmen), di-port persis dari `mvWalkTo()`/`mvPointOnSeg()` MathVille. Kiko numpang di atas truck (ganti Bo), tap buka `KikoChatPanel` mode `"general"` (reuse yang sama), bubble hint "Hai, ini Kiko!" gaya sama kayak yang IPAS pake.
- **Jumlah bab VARIASI per kelas** (5-9, dicek langsung: kelas 3=5, kelas 4=6, kelas 5=9, kelas 6=4) -- BEDA dari IPAS yang selalu 8 DAN dari MathVille asli yang di-hardcode PERSIS 9 stop (`CHAPTER_META` object literal). Layout jalan (`buildTownLayout(n)`) DIGENERALISASI kayak `buildPathLayout` IPAS, ornamen kota (`TownOrnaments`) juga di-generate proporsional ke tinggi peta (seeded random, bukan posisi tangan kayak `MAP_ORNAMENTS` asli yang pas buat 9 stop doang).
- **Ikon lokasi kota** (🏛️🥐🌳🌉🎡🚰🚦🏪🕰️, 9 ikon asli MathVille) di-cycle per index (`index % 9`) sama alasannya kayak planet IPAS -- topik matematika gak nyambung ke "Town Hall"/"Bakery" secara harfiah, ikon cuma estetika kota.
- **8 bentuk ornamen** (cloud/tree/bush/house/mountain/wave/deer/bird) di-port SVG-nya PERSIS dari `ornamentSvg()` MathVille, animasi drift/sway/bob juga keyframe yang sama, di-scatter di margin kiri-kanan jalan (bukan posisi tangan).
- **BEDA SENGAJA dari MathVille asli**: (1) MathVille "nothing ever locked" (semua topik kebuka dari awal) -- di sini TETEP pake progression lock/current/done yang udah ada di seluruh app (`computeStatuses`), demi konsisten sama IPAS map + `SubjectDetail.jsx` lain, state locked (gak ada di source asli) dapet treatment dim+lock-chip sama kayak IPAS. (2) Drive/Plane/Math Race (fitur matematika yang udah ada) TETEP ada tapi LAYOUT-nya beda dari request user (lihat poin di bawah).
- **Layout header+CTA (2026-08-07, revisi ke-2, request eksplisit user)** — awalnya footer 3 tombol lebar (persis pola `SubjectDetail.jsx`), user minta diringkes: Drive 🚗 & Plane ✈️ jadi ICON BULAT KECIL di header (sejajar tombol back, `justify-content:space-between`, BUKAN pake `ScreenHeader` shared component lagi karena butuh slot kanan yang gak didukung -- header di-custom inline, `ScreenHeader` sendiri GAK diubah, masih dipake apa adanya di semua screen lain). Math Race jadi CARD promo lebar di atas peta (bukan tombol), background solid `CHERRY` (awalnya gradient + pola garis diagonal, user: "card mulus aja, ga usah pake gradasi" -- kedua-duanya dicabut, tinggal warna solid), ada 🏎️ yang jalan animasi loop (`jkF1Drive`, `left:-15%→115%` pola sama kayak `WalkingDino.jsx`) di dalem card.
- **Fix truck numpuk sama node (2026-08-07)** — offset vertikal truck relatif ke node (`TRAVELER_Y_OFFSET`) awalnya `-46` (sama persis kayak MathVille asli), tapi node di sini 64px + pulse box-shadow ring bikin truck numpuk/nempel keliatan berantakan pas parkir persis di 1 stop -- user oke buat dirapihin, digedein ke `-72` (~10px gap bersih).
- **Verifikasi**: dites di dev (`/kelas/4/matematika/peta-mockup`, waktu masih mockup), 6 bab kelas 4 render judul asli, icon Drive di header beneran navigate ke `/kelas/4/matematika/drive` (dicek `location.pathname`), tap truck buka Kiko chat. Screenshot dikirim ke user 2x (v1 truck numpuk+footer lama, v2 header icon+Math Race card) via Playwright (`scratchpad/shot_math.mjs`).

**Di-wire live (2026-08-08)** — user: "matematika sekalian rubah lah" (samain kayak Bindo & IPAS yang barusan). Pola fix SAMA PERSIS: route statis `/kelas/:grade/matematika` ditaro sebelum `/kelas/:grade/:subject` generik, `onBack` dari `/kelas/${grade}/matematika` (dulu nunjuk balik ke `SubjectDetail.jsx`) ke `/kelas/${grade}` (PickSubject), title header dari "Peta Matematika" jadi "Matematika", route `/peta-mockup` lama dihapus. **Sekalian dibersihin**: kartu promo "🏁 Math Race" yang nempel di atas peta ini DICABUT (udah jadi kartu sendiri di `PickSubject.jsx` dari kerjaan sebelumnya -- kalau dibiarin bakal dobel), keyframe `jkF1Drive`-nya ikut kehapus karena gak dipake lagi di tempat lain. `SubjectDetail.jsx` juga dibersihin: blok `subject === "matematika"` (tombol Drive/Plane) jadi dead code (matematika gak pernah lagi nyampe ke situ) dan cabang `bindo` di kondisi `BoBridgeBanner` juga (dari cutover Bindo sebelumnya) -- dua-duanya dihapus.
- **Diverifikasi ulang abis di-wire live**: `/kelas/4/matematika` nunjukin town map (bukan list lama, kartu Math Race udah ilang), tombol Drive di header masih beneran navigate ke `/kelas/4/matematika/drive`, tombol back balik ke `/kelas/4`, `/kelas/4/binggris` TETEP `SubjectDetail.jsx` + Dino Bridge banner-nya masih ada (subject lain gak ke-affect), `/kelas/4/mathrace` (kartu Math Race yang sekarang berdiri sendiri) masih jalan normal, gak ada console error.
- **Belum dikerjain**: testing di 6 kelas (baru dites kelas 4).

## Fix: joystick gak responsif di device asli (2026-08-07)

User report (device fisik, BUKAN browser-pane): "pesawat dan mobil ga bisa dipencet kanan kiri togglenya. mobil bahkan ga jalan" — Drive Mode & Plane Mode dua-duanya kena (pertama kalinya bug ini kebukti sejak `useJoystick.js`/`Joystick.jsx` ditulis, karena project ini **belum pernah dites di device fisik** sampe sekarang, cuma browser-pane).

**Root cause**: `useJoystick.js` lama nempelin `onPointerMove`/`onPointerUp`/`onPointerLeave` LANGSUNG ke elemen base joystick (lingkaran kecil, radius ~40px). Padahal buat dapet deflection PENUH, jari WAJIB geser sampe/lewat batas fisik lingkaran itu — itu emang cara kerja joystick, bukan edge case. Di beberapa browser mobile, geser sejauh itu bikin `pointerleave` ke-fire di tengah drag WALAU `setPointerCapture` udah dipanggil (capture-nya gak selalu reliable nyekat boundary event di semua browser/versi) — tiap kali itu kejadian, vector joystick ke-reset ke nol di tengah gesture, jadi KERASA kayak "gak bisa digeser ke kanan/kiri sama sekali".

**Fix** (`useJoystick.js`): pointermove/pointerup/pointercancel dipantau di **`window`**, bukan di elemen base — difilter pakai `pointerId` (`pointerIdRef`) biar 2 joystick (steer+aim water-gun Drive Mode) gak interferensi satu sama lain. `onPointerDown` tetep di elemen base (biar cuma nge-drag mulai kalau nekan DI DALAM lingkaran), tapi begitu drag aktif, gerakan/lepas dipantau global — imun dari masalah boundary-element di atas. `Joystick.jsx` disederhanain (buang prop `onPointerMove`/`onPointerUp`/`onPointerLeave` yang gak dipake lagi).

**Verifikasi**: browser-pane, `PointerEvent` didispatch manual — pointerdown di tengah base, lalu pointermove di `window` ke `clientX` +300px (jauh ngelewatin lebar base yang cuma 96px) — nub joystick tetep ke-clamp di radius maksimal (BUKAN reset ke nol kayak sebelumnya), lalu pointerup bener-bener ngereset ke nol. **Belum bisa diverifikasi ulang di device fisik beneran** (masih browser-pane) — kalau user masih lapor sama setelah fix ini, cek lagi lebih dalam (kemungkinan lain: `touch-action` kena override parent, atau splash/overlay lain numpuk di atas joystick pas gameplay).

### Fix susulan: water gun gak keliatan nyemprot (2026-08-07, sesi yang sama)

User lapor lagi abis fix joystick di atas: "untuk mobil, semprotan airnya ga bisa nyemprot bneran. ikutin repo di al idrisi aja udah". Ternyata BUKAN soal joystick lagi (itu udah kena fix di atas) — root cause KEDUA yang beda: visual jet-nya sendiri dari awal cuma render 1 emoji 💦 STATIS nempel di posisi mobil, gak ada bentuk terarah sama sekali, jadi walau hit-detection logic-nya (cone+range) udah bener dari awal, SECARA VISUAL emang gak pernah keliatan "nyemprot" ke mana pun.

**Fix**: port ULANG persis dari `al-idrisi-games/mathville/style.css` (`.drive-water-stream`) — segitiga CSS panjang (zero-size box, border-left/right transparan 10px, border-bottom solid 128px, animasi pulse opacity), BUKAN emoji. Class baru `.jk-water-stream` di `tokens.css`. **Bonus bug ketemu sekalian**: rotasi jet sebelumnya salah pakai `headingCss()` (formula +90 buat sprite "nose-up" kayak mobil/dino) — segitiga CSS ini defaultnya ngarah KE BAWAH (bukan ke atas), jadi butuh formula BEDA (`aimAngle*180/π - 90`, persis punya BrainBox punya `driveWaterTick()`). Kalau cuma benerin bentuknya doang tanpa benerin rotasi ini, jet-nya bakal keliatan tapi ngarah ke arah yang salah.

**Verifikasi**: browser-pane, drag aim-joystick via `window` pointermove (pola sama kayak verifikasi fix joystick di atas) — screenshot nunjukin balok segitiga biru transparan kepancar dari mobil ke arah drag, animasi pulse jalan. Git hygiene: `tokens.css` juga lagi ada perubahan gak-terkait dari sesi lain (accent warna "wood" Matematika) — di-pisah pakai `git add -p` (bukan `git add` biasa) biar cuma hunk punya sendiri yang ke-commit, hunk punya sesi lain dibiarin uncommitted kayak semula.

## Ninja Runner (2026-08-08) — SELESAI, deployed & verified end-to-end

User minta engine cross-subject "ninja runner" dari `al-idrisi-games` (`mathville/script.js`, cari komentar "Ninja Runner") diporting ke Jagoan Kelas, ditaro di card sendiri di bawah Fokus Latihan (`PickSubject.jsx`). Sempet ditanya dulu soal makna nama badge "sprout"/"sensei" dan cara scoring sebelum eksekusi (user: "card bikin gini" + screenshot 3-card picker asli) — itu yang jadi acuan visual persis.

- **`src/games/ninja/`** — `NinjaRunner.jsx` (komponen utama), `NinjaCardFace.jsx` (SVG wajah kartu, port persis `NINJA_CARD_FACE_SVG`), `ninjaPools.js` (mapping 3 kategori subject BrainBox ke konten sendiri: `math` → `generateQuickQuestion` grade-aware yang udah dipake Drive/Plane, `lang` → gabungan Bindo+Binggris, `sci` → IPAS, cuma soal `multiple_choice` yang diambil).
- **Mekanik SAMA PERSIS BrainBox** (constant/timing/formula gak diubah): run-lane 2 tahap (batu → JUMP buat dodge, lanjut musuh mendekat → trigger gate), 3 kartu subject fixed tiap round (tiap kartu dapet difficulty RANDOM sendiri-sendiri), badge tier "Twig Sprout"(easy)/"Star Ninja"(medium)/"Golden Sensei"(hard), 20 soal/round, skor `PTS[difficulty]` (10/25/50) + streak bonus non-cumulative (+5 di 3-streak, +15 di 5-streak, +30 di 10-streak), animasi "slice" kartu kebelah pas jawaban benar.
- **Deviasi sengaja**: karakter pelari [[Kiko]] (maskot resmi) gantiin stick-figure ninja BrainBox. XP profil pakai `XP_PER_CORRECT=8` (rate sama kayak Focus Round) BUKAN `NINJA_WIN_XP=20` flat per-round BrainBox — biar konsisten sama ekonomi XP mode lain, meskipun skor "poin" yang ditampilin di HUD tetep formula asli BrainBox (dua angka ini sengaja beda makna: poin = skor performa in-round, XP = mata uang profil). Review kartu salah disederhanain jadi 1 list scroll-able (bukan paginated kayak BrainBox punya).
- **Soal campur lintas subject SENGAJA gak nyentuh status locked/current/done topik manapun** — sama filosofi Focus Round, cuma nyumbang correct/wrong ke topicStats (buat "Perlu Latihan Lagi" Parent Portal) + XP.

**2 bug ketemu & difix pas verifikasi gameplay end-to-end** (browser-pane, akun test asli lewat signup form -- BUKAN localStorage fake session, biar Firestore write beneran ke-test, akun `NinjaTestZZ` dihapus lagi abis kelar via Firestore REST API `DELETE`):
1. **Crash null pas jawab SALAH** (`Cannot read properties of null (reading 'subjectKey')`, nge-unmount seluruh komponen) — `advance()` awalnya manggil `setPhase("lane")` sebagai side-effect DI DALAM parameter functional-updater `setQnum(n => {...})`. Pola bug PERSIS yang udah pernah kejadian & didokumentasiin di respawn-gauntlet Plane Mode (lihat bagian Games): StrictMode/urutan proses update React bisa ngerender 1 frame antara di mana `question` udah `null` tapi `phase` masih `"question"` (hint text baca `question.subjectKey` → crash). Fix: `qnum` dibaca dari closure biasa, `setPhase`/`setQnum` dipanggil sebagai 2 setState terpisah di body function, bukan nested di dalem updater.
2. **XP Ninja Runner gak nongol di "XP per Pelajaran" Parent Portal** — `finishRun()` awalnya nulis `xpEarned:0` ke tiap topik (`recordFocusRoundAttempt`) terus nambahin XP totalnya SEKALI lewat `addXp()` (biar gak dobel), tapi ternyata `recordFocusRoundAttempt` UDAH nambahin ke top-level `xp` juga (via `increment()` di update yang sama) — jadi nulis `xpEarned:0` per-topik bikin breakdown per-subject (`computeXpBySubject`) gak pernah keitung kontribusi Ninja Runner sama sekali, walau total `player.xp` global-nya bener. Fix: XP dibagi ASLI per topik (`stats.correct * XP_PER_CORRECT`) dikirim ke `recordFocusRoundAttempt`, sisanya (correct dari kartu MATH yang procedural, gak punya topicId) baru ditambahin lewat `addXp()` buat porsi yang gak ke-atribusi. Diverifikasi: jawab 2 soal SCIENCE bener → Parent Portal breakdown nampilin "🔬 IPAS 16 XP" persis (2×8).

**Verifikasi end-to-end** (browser-pane, otomatisasi klik via script di-inject `javascript_tool` biar gak manual 20 soal per test — native DOM `.click()` di tombol asli, bukan simulasi terpisah): render awal (3 kartu match desain persis screenshot user), jawab MATH/LANG&ARTS/SCIENCE manual satu-satu (soal+opsi dari konten ASLI, bukan dummy), jawab salah (verifikasi wrong-flash + no-crash abis fix #1), JUMP button, full 20-soal sampe overlay "Your journey has completed" + skor + tombol "Lihat N Soal yang Salah" (review list nampilin prompt/jawaban-kamu/jawaban-benar per soal, scroll jalan), dan write Firestore (topicStats + XP) dikonfirmasi lewat re-login FRESH ke Parent Portal (bukan baca state lokal) abis fix #2.

## Favicon/app icon (2026-08-08) — diganti dari placeholder Vite ke Kiko

Sebelumnya masih favicon default Vite (blob petir ungu) — kepake juga jadi ikon "Add to Home Screen" iOS karena app ini belum pernah punya `apple-touch-icon`/manifest sama sekali. User minta diganti pake Kiko versi "cream" (salah satu dari 2 avatar PNG/JPEG yang sebelumnya dibikinin buat foto profil email), bentuknya bulat bukan kotak, niru gaya foto profil Zoho yang di-crop bulat.

- **2 varian rasterisasi, alasan teknis beda tujuan** (source SVG di-generate manual, path & warna Kiko PERSIS `Kiko.jsx`, bukan re-desain):
  - `public/favicon.svg` + `favicon-32.png` — **bulat, pojokan TRANSPARAN**. Browser gak nge-mask favicon sama sekali, jadi bentuk asli file yang nampil di tab — ini yang bikin bisa keliatan "bulat" beneran di address bar/tab, sesuai request user.
  - `public/apple-touch-icon.png` (180x180) + `icon-192.png`/`icon-512.png` (buat `manifest.json`) — **kotak penuh, TANPA transparansi**. iOS/Android nge-mask app-icon pake bentuk sistem sendiri (squircle/adaptive icon) — kalo sumbernya dikasih transparan di pojok, iOS render pojokan itu JADI HITAM (gotcha yang udah dikenal luas), bukan tembus. Jadi versi ini sengaja full-bleed persegi, biar OS yang bulet-in otomatis, bukan kita yang manual crop.
- **`public/manifest.json`** (baru, belum pernah ada) — nama/theme-color/icons buat PWA "Add to Home Screen" Android, referensi `icon-192.png`/`icon-512.png`.
- **`index.html`** ditambahin `<link rel="apple-touch-icon">`, `<link rel="manifest">`, `<meta name="theme-color">`, plus fallback PNG icon (`favicon-32.png`) buat browser yang gak support favicon SVG.
- Diverifikasi: build sukses, semua asset baru serve 200 di dev DAN production (`curl` langsung ke tiap path), `<link>` tags kebaca bener di DOM, gak ada console error.

## Auth screen didekorasi + PIN masking (2026-08-08)

User kirim 2 screenshot perbandingan (Auth Jagoan Kelas yang masih polos vs. Welcome screen al-idrisi-games yang penuh hiasan pastel) + 4 poin request:

1. **Header badge** (`Auth.jsx`) — 🎒 emoji diganti `<Kiko size={24}/>` di sebelah teks "Jagoan Kelas", ukuran disetarain sama tinggi teks (bukan icon kegedean/kekecilan kayak emoji sebelumnya).
2. **Ornamen pastel** — `src/components/AuthDecor.jsx` (baru), di-port dari al-idrisi-games (`index.html` komentar "Page-level floating decoration", `style.css` `.sc-blob`/`.sc-star`/`.sc-cloud`/`.sc-dot`/`.sc-bob`) — teknik SAMA PERSIS (clip-path polygon buat bintang, `position:absolute` blob/dot/cloud, keyframe bob pelan), warna pake token `--pastel-*` yang emang udah nilainya identik sama punya BrainBox dari awal (bukan re-derive). Dipasang di `Auth.jsx` sebagai layer `position:absolute;inset:0;z-index:0` di dalem card Shell (yang emang udah `position:relative;overflow:hidden`), konten form di-bungkus `z-index:1` biar tetep di atas. **Cuma dipasang di `Auth.jsx`**, bukan `Landing.jsx` (screenshot user emang ngebandingin layar Auth-nya doang).
3. **Bubble Kiko di Landing** — teks diganti dari "Hai, ini Kiko! 👋" jadi **"Kiko disini"** (permintaan user persis), background bubble diganti dari solid putih jadi `color-mix(in srgb, var(--pastel-purple) 35%, white)` -- pola "soft pastel wash" yang sama kayak dipake `Chip.jsx` buat state unselected, biar "super lembut" beneran (bukan cuma ganti 1 warna solid pekat). Fungsinya (tap buka `KikoChatPanel` mode general) TIDAK diubah, ini emang udah ada dari batch kerjaan Kiko 2026-08-06 -- request user cuma soal teks+warna bubble-nya doang.
4. **PIN kebaca pas Sign In** — `Input.jsx` sebelumnya SELALU render `type="text"` biarpun prop `pin` true, jadi 4 digit PIN kebaca polos di kedua mode (Daftar & Masuk). Fix: `type={pin ? "password" : type}` -- sekarang browser native-mask pake titik, sama kayak PIN field al-idrisi-games. Satu perubahan di komponen shared otomatis kena ke Daftar & Masuk dua-duanya (cuma ada 1 instance Input buat PIN di `Auth.jsx`, dibedain lewat `mode` state, bukan 2 komponen terpisah).

Diverifikasi di browser: screenshot Daftar & Masuk dua-duanya (ornamen render penuh, badge Kiko+teks sejajar), isi PIN "1234" beneran ke-mask jadi titik (placeholder DAN value asli, bukan cuma placeholder-nya doang yang kebetulan mirip titik), tap Kiko di Landing tetep buka chat panel normal (fungsi gak somehow ke-regress), gak ada console error.

**Fix susulan: favicon gak ke-update di browser user (2026-08-08, sesi yang sama)** — user lapor tab masih nampilin petir ungu lama padahal `curl` langsung ke `jagoan-kelas.vercel.app/favicon.svg` udah kebukti nyimpen Kiko yang baru (server-side udah bener). Root cause: favicon di-cache browser JAUH lebih agresif/persisten daripada asset halaman biasa, hard-refresh biasa gak mempan. Fix: `index.html` link tags favicon/apple-touch-icon ditambahin query `?v=2` — pola cache-busting yang SAMA kayak dipake project azkauniverse (`?v=N` di `script.js`/`style.css`), maksa browser nganggep itu resource baru sepenuhnya, bukan nunggu cache lama expire.

## Landing (pilih kelas) didekorasi + dino diperlambat (2026-08-08)

User kirim screenshot BrainBox hub (`playalidrisi.fun`, penuh bintang/titik/blob pastel di sekitar card game) vs. Landing Jagoan Kelas yang masih polos, minta 2 hal:

1. **Ornamen** — `src/components/LandingDecor.jsx` (baru, BUKAN reuse `AuthDecor.jsx` — posisi elemennya beda karena layout Landing beda: header+Kiko+grid 6 kelas+dino vs. form Auth doang, jadi bikin file baru lebih simpel daripada 1 komponen shared dengan logic percabangan posisi). Teknik identik (`STAR_CLIP` clip-path, `--pastel-*` token, `position:absolute;pointer-events:none;z-index:0` di belakang konten yang di-bungkus `z-index:1`) — blob di 4 pojok, bintang+titik+sparkle nyebar di margin kiri-kanan (deket header, samping Kiko, pinggir grid kelas, atas dino) biar gak numpuk di atas tombol/card yang emang udah rame warna.
2. **Dino jalan 15% lebih pelan** (`WalkingDino.jsx`) — `jkDinoWalkAcross` durasinya `7s` → `8.05s` (`7 × 1.15`), cuma keyframe horizontal-nya doang yang disentuh, `jkDinoWalkBounce` (bob naik-turun) TETEP `0.4s` (gak ada di request user).

Diverifikasi: screenshot Landing abis login (ornamen kebaca jelas, gak nutupin avatar/XP/tombol mute/Kiko/grid kelas/dino), klik salah satu kartu kelas TETEP navigate normal (`pointer-events:none` di layer dekorasi kebukti gak nge-block), `getComputedStyle` tombol dino nunjukin `animationDuration: "8.05s, 0.4s"` persis target, gak ada console error.

**Fix susulan: sapaan harian Kiko (2026-08-08, sesi yang sama)** — user minta di bagian bawah Landing dikasih "maskot Kiko + tulisan 'Hai {nama} ayok main lagi', 50 template gonta-ganti tiap hari, kayak di playalidrisi".

- **`src/data/kikoGreetings.js`** (baru) — 50 template Bahasa Indonesia (bukan translate 5 template BrainBox punya, ditulis dari nol biar sesuai suara app ini), token `{name}` di tiap template. `greetingTemplateForToday()` pilih 1 lewat `Math.floor(Date.now()/86400000) % 50` — ganti otomatis tiap pergantian hari kalender, BUKAN random tiap reload (port formula PERSIS punya BrainBox punya `SC_GREETINGS`).
- **`src/components/KikoGreeting.jsx`** (baru) — avatar `<Kiko size={34}/>` yang wiggle pelan + teks sapaan yang "gelombang" kata-per-kata (tiap kata `<span>` sendiri, staggered `animationDelay`), nama di-highlight warna yang gonta-ganti per hari (`--ink-on-blue`/`--ink-on-pink`/`--ink-on-green` — token yang app ini emang udah punya, nilainya persis sama kayak 3 hex hardcoded BrainBox, bukan kebetulan karena token itu awalnya emang di-port dari situ). Badge "Tap Kiko!" nongol di atas avatar sampe di-tap sekali (localStorage `jk_kiko_greeting_hint_seen`, port `aig_bo_chat_hint_seen` punya BrainBox). Tap = manggil `onTap` yang dikasih dari `Landing.jsx` (`setChatOpen(true)`) — buka `KikoChatPanel` yang SAMA persis kayak tap Kiko gede di atas, bukan chat/trigger terpisah.
- Ditaro di `Landing.jsx` antara `WalkingDino` dan link "Untuk orang tua" (BrainBox naronya setelah game-card list, sebelum footer link — posisi analog di layout Jagoan Kelas).
- Diverifikasi: screenshot nunjukin "Kamu keren, Adit!" + hint "Tap Kiko!" muncul (animasinya pop-fade berulang, ke-capture di frame yang lagi visible), tap beneran buka `KikoChatPanel`, abis tap hint ilang dan `localStorage.getItem('jk_kiko_greeting_hint_seen')` jadi `"1"`, gak ada console error.

## Math Race dapet card sendiri + semua layar didekorasi (2026-08-08)

User kasih 4 poin request lewat screenshot Kelas 1 (subject list) + Matematika (footer Drive/Plane/Math Race/Lanjut Belajar):

1. **Math Race pindah jadi card sendiri** — sebelumnya tombol lebar di dalem `SubjectDetail.jsx` khusus subject matematika (sejajar Drive/Plane). Sekarang `GameCard` sendiri di `PickSubject.jsx`, ditaro **di atas** card Matematika (sebelum `SUBJECTS.map(...)`), accent baru `race` (`--product-race: #F6C1C1`, pastel pink, token baru di `tokens.css` — 7 accent lain semua udah kepake, gak ada slot kosong buat reuse). Route pindah dari `/kelas/:grade/matematika/mathrace` (nested) ke `/kelas/:grade/mathrace` (top-level, sejajar `/fokus`/`/ninja`) — `MathRace.jsx`'s tombol back/keluar ikut diupdate ke `/kelas/${grade}` (bukan lagi `/kelas/${grade}/matematika`).
2. **Soal Math Race udah grade-aware dari awal** — dicek kodenya (`generateQuickQuestion(grade, "medium")`, `grade` dari `useParams()`), gak perlu perubahan, ternyata udah otomatis nyesuain kelas berapapun anak masuk lewat.
3. **Tombol "Lanjut Belajar" dihapus dari SEMUA card** (`SubjectDetail.jsx`) — user nanya "gunanya apa ya?", jawabannya: tombol itu cuma shortcut ke topik `status:"current"`, TAPI topik itu udah keliatan & clickable langsung di list topik di atasnya (row `current` gak locked) — genuinely redundant, bukan cuma "kelihatannya". `currentTopic` variable ikut dihapus (gak ada consumer lain).
4. **Semua layar yang masih polos didekorasi** — `src/components/PageDecor.jsx` (baru): BEDA dari `AuthDecor.jsx`/`LandingDecor.jsx` (posisi manual per-elemen), di sini posisi blob/bintang/titik/sparkle di-**generate dari `seed` string** lewat PRNG seeded (mulberry32 + hash string, pola sama kayak `seededRandom(42)`-nya `TwinkleStars` di `IpasQuestMap.jsx`) — biar SCALABLE ke banyak layar sekaligus tanpa nulis array posisi manual satu-satu tiap file. Dipasang (masing-masing `<PageDecor seed="...unik.../>` + wrapper `position:relative;zIndex:1` buat konten di atasnya) ke: `PickSubject.jsx`, `SubjectDetail.jsx` (seed per-subject), `TopicQuiz.jsx` (seed per-topik), `FocusRoundPicker.jsx`, `FocusRoundQuiz.jsx`, `ParentPortal.jsx` (2 screen: signin+portal), `MathRace.jsx`, `DinoRaceUnlock.jsx`. SENGAJA gak dipasang ke layar game aktif yang emang udah penuh elemen sendiri (Drive/Plane/NinjaRunner/GlassBridge) atau mockup peta yang udah punya tema sendiri (IPAS/MathVille/Bindo).

Diverifikasi: build sukses, screenshot Math Race lobby+lengkap-main via Solo Play (soal 3-digit subtraction sesuai kelas 4), `SubjectDetail.jsx` Matematika cuma nyisa Drive+Plane (Math Race+Lanjut Belajar kebukti ilang), ornamen `PageDecor` kebaca di semua layar yang disebut tanpa nutupin tombol/card (klik tetep normal).

## Bahasa Indonesia — "Storybook Trail" (2026-08-08, LIVE — halaman resmi, bukan mockup lagi)

Pola sama kayak "Peta IPAS"/"Peta Matematika" di atas, tapi sumbernya `azkacraft` ("Language & Arts") — user minta riset detail dulu ("tau games language and arts di al idrisi ga? ceritain detail apa aja disana"), baru minta **desainnya doang** yang di-ambil ("cuma butuh design, nanti soal mengikuti dari salah 1 bab... plek2 ikutin aja"). File: `src/screens/BindoStorybookTrail.jsx` (baru).

**Awalnya preview-only** (`/kelas/:grade/bindo/peta-mockup`), **di-wire jadi halaman resmi SAMA HARI** setelah user cek screenshot langsung dari app ("ini bahasa indonesia, begitu bner? ga beda dul" — ternyata dia buka jalur normal yang masih nunjukin `SubjectDetail.jsx` lama, karena mockup-nya emang sengaja ditaro di URL terpisah) dan eksplisit minta "langsung pasang jadi halaman resmi". Route sekarang: `/kelas/:grade/bindo` (statis) ditaro SEBELUM `/kelas/:grade/:subject` (dinamis) di `App.jsx` — React Router ranking otomatis menangin segmen statis lepas dari urutan deklarasi, jadi 5 subject lain TETEP kena `SubjectDetail.jsx` generic, gak perlu percabangan kondisional di dalem satu file. Route `/peta-mockup` lama DIHAPUS (redundant). Konsekuensi kecil dari transisi ini: `onBack` diubah dari `/kelas/${grade}/bindo` (dulu nunjuk ke SubjectDetail lama sebagai "detail sebenarnya") ke `/kelas/${grade}` (PickSubject, samain pola tombol back subject lain), judul header `ScreenHeader` dari "Peta Bahasa Indonesia" jadi "Bahasa Indonesia" (gak lagi kedengeran kayak preview).

- **Hero scene** (langit gradient biru-hijau, matahari glow, bintang kelap-kelip, 2 bukit overlap) + **title ribbon** biru + **stat strip** (border dashed) + **2 info-card** ("Kata Hari Ini"/"Fakta Menarik") — semua warna/ukuran/border di-port LANGSUNG dari `azkacraft/style.css` (`.hero-scene`/`.hero-sun`/`.hero-hill`/`.title-ribbon`/`.stat-strip`/`.info-card`), palet default "Sky Explorer" (`--brand:#2F6FED` dst) yang di-hardcode (BUKAN sistem ganti-tema gender/palet azkacraft yang jauh lebih kompleks, di luar scope — app ini gak punya konsep itu).
- **Mascot hero DIGANTI Kiko** (request eksplisit user: "diganti maskot kiko aja dengan ukuran sama dan bisa di click jadi ai chat box") — azkacraft aslinya pakai karakter CSS-built (rambut/wajah/badan boy-girl themed) yang cuma dekoratif (bounce doang, gak bisa di-klik). Kiko di sini ukuran sama (~104px), TETEP bounce (keyframe `bounceMascot` di-port `jkBindoMascotBounce`), TAPI sekarang jadi tombol beneran → buka `KikoChatPanel` mode `general` (pola sama kayak semua entry point Kiko lain di app ini). Bubble "Kiko disini" (bukan translate "Let's play!" azkacraft — reuse copy yang app ini emang udah pake konsisten di tempat lain).
- **Trivia "Kata Hari Ini"/"Fakta Menarik"** — konsepnya port dari azkacraft's info-card "Word of the Day"/"Fun Fact" (mereka punya 300 entri Inggris, rotasi harian), TAPI isinya ditulis dari nol Bahasa Indonesia di `src/data/bindoTrivia.js` (30 kata + 30 fakta), formula rotasi PERSIS sama kayak `kikoGreetings.js` (`Math.floor(Date.now()/86400000) % panjang array`).
- **Stat strip pakai DATA ASLI** (XP/bab-selesai/bab-current dari `computeStatuses`), BUKAN placeholder statis kayak contoh HTML azkacraft ("Keep going!"/"0 Badges").
- **Mode-grid Solo/Multiplayer azkacraft DICABUT** — app ini gak punya multiplayer per-topik, cuma 1 jalur practice.
- **"Storybook Trail" chapter timeline** — garis putus-putus vertikal di tengah + kartu bab selang-seling kiri-kanan + emoji dekorasi muter di samping tiap kartu + ikon bulat warna topik (5 warna `TOPIC_STYLE` — ✏️oranye/🔤pink/📐biru/🌊hijau/🎨ungu — di-cycle `index%5` PERSIS kayak azkacraft `renderBookshelf()`, karena bab Bindo kita gak punya field "topic" azkacraft buat mapping 1:1) — di-port dari CSS `.timeline`/`.timeline-node`/`.timeline-card` + logic `renderBookshelf()`/`TOPIC_STYLE`/`CHAPTER_DECO` di `script.js`. **BEDA dari IpasQuestMap/MathTownMap**: garis LURUS vertikal (bukan kurva SVG bezier/Catmull-Rom), tap kartu = navigate LANGSUNG (gak ada animasi "karakter terbang ke tujuan" kayak ship IPAS/truck MathVille — azkacraft aslinya emang gak punya animasi itu, cuma tap langsung).
- **Banner "Dino Bridge"** — REUSE komponen `BoBridgeBanner.jsx` yang UDAH ADA (dipasang di `SubjectDetail.jsx` versi sekarang buat bindo/binggris), bukan dibangun ulang dari nol.
- Diverifikasi: screenshot full-height (resize viewport ke 450×1550 biar gak perlu scroll, browser-pane kadang gagal capture abis scroll gesture) nunjukin hero+ribbon+stat+trivia+timeline semua render sesuai target, tap Kiko beneran buka chat (`document.querySelector` cek input chat muncul abis native `.click()`), tap "Bab 1" beneran navigate ke `/kelas/4/bindo/topik/bab-1` (`location.pathname` dicek), 6 bab kelas 4 semua judul asli bukan dummy. Abis di-wire live: dicek ulang `/kelas/4/bindo` (halaman baru muncul) DAN `/kelas/4/ipas` (subject lain TETEP `SubjectDetail.jsx`, gak ke-affect), tombol back dari halaman baru bener balik ke `/kelas/4` (bukan nyangkut ke path lama), tap card "Bahasa Indonesia" dari `PickSubject.jsx` juga nyampe ke halaman baru.
- **Belum dikerjain**: testing di 6 kelas (baru dites kelas 4), IPAS & Matematika masih tetep status mockup preview-only (belum diminta di-live-in juga).

**Revisi susulan (2026-08-08, sesi yang sama)** — user review langsung di halaman live, 3 feedback:
1. **Kiko cuma bounce di tempat, kurang menarik** — sekarang 2 animasi CSS numpuk di elemen yang sama (properti beda, gak nabrak): `jkBindoKikoWalk` geser `left` bolak-balik pelan (14s) + `jkBindoKikoDance` bounce+rotate cepat terus-terusan (0.9s) — hasilnya "jalan sambil joget", bukan diem doang.
2. **Bubble "Kiko disini" ekornya salah arah** — sebelumnya di-port PERSIS radius azkacraft (`14px 14px 14px 4px`, sudut lancip di kiri-bawah), padahal Kiko di app ini posisinya di KANAN-bawah bubble, jadi keliatan kayak bukan Kiko yang ngomong. Dibalik jadi `14px 14px 4px 14px` (lancip kanan-bawah). Sekalian bubble dipindah jadi CHILD di dalem button Kiko (bukan elemen absolute terpisah) biar otomatis ikut kemanapun Kiko jalan. Timing juga diubah dari "selalu nongol" jadi siklus nongol 2 detik / ilang 5 detik (`jkBindoBubbleCycle`, loop 7 detik).
3. **Mode-grid Solo/Multiplayer yang tadinya dicabut, diminta balik** ("wireframenya ikutin jg plek plek") — ditanya dulu soal scope tombol Multiplayer (visual doang vs fitur head-to-head beneran kayak Math Race), user pilih **visual doang**. "Solo Adventure" (hijau) `scrollIntoView` ke jejak bab di bawah (app ini 1 layar, gak ada transisi state kayak azkacraft yang punya screen terpisah), "Multiplayer" (oranye) nampilin toast "Mode ini segera hadir! 🚧" 1.8 detik pas di-tap, GAK bikin fitur baru.

Diverifikasi: `getComputedStyle` konfirmasi 2 animation-name numpuk jalan bareng di tombol Kiko, bubble punya `animationName: jkBindoBubbleCycle`, tap Kiko pas lagi mid-animasi tetep buka chat normal (gak ke-block), tap Multiplayer toast muncul lalu ilang sendiri abis ~1.8detik (dicek via `await sleep` di browser, bukan asumsi), tap Solo beneran scroll ke section timeline (dicek via screenshot, bukan cuma `scrollTop` container yang salah dicek awalnya).

**Revisi susulan #2 (2026-08-08, sesi yang sama) — dipecah jadi 2 layar beneran**: user gak suka pola "scroll ke bawah" di atas ("jangan turun kebawah kaya sekarang jelek, ganti"), minta disamain PERSIS sama azkacraft yang punya 2 LAYAR terpisah (Landing vs Quest Map, pindah lewat tap, bukan scroll). Dikonfirmasi dulu ("betul, sama kaya di al idrisi, periksa aja") baru dikerjain:
- **`BindoStorybookTrail.jsx`** sekarang CUMA "layar depan" — hero+Kiko+title-ribbon+mode-grid+trivia. Daftar bab (timeline) DICABUT TOTAL dari file ini.
- **`BindoQuestMap.jsx`** (baru) — isinya CUMA daftar bab (Dino Bridge banner + timeline zig-zag Bab 1-N), `ScreenHeader` sendiri ("Jejak Ceritamu"), route `/kelas/:grade/bindo/bab`. Fetch topics/progress SENDIRI (duplikat logic dari `BindoStorybookTrail.jsx`, bukan di-share lewat hook — matching pola yang emang udah ada di codebase ini, belum ada shared-fetch-hook buat 2 titik kayak gini, jadi duplikasi kecil ini konsisten sama existing pattern, bukan bikin abstraksi baru buat kasus 1-off).
- Tombol "Solo Adventure" di layar depan sekarang `navigate()` ke `/kelas/:grade/bindo/bab` (dulu `scrollIntoView`). Tombol back di `BindoQuestMap` balik ke `/kelas/:grade/bindo` (layar depan), BUKAN ke `/kelas/:grade` (PickSubject).
- Diverifikasi: `location.pathname` dicek tiap langkah (landing → tap Solo → `/kelas/4/bindo/bab` → tap back → `/kelas/4/bindo` → tap Bab 1 → `/kelas/4/bindo/topik/bab-1`), gak ada console error, `/kelas/4/bindo` dan `/kelas/4/bindo/bab` dua-duanya 200 di production.

**Revisi susulan #3 (2026-08-08, sesi yang sama) — toggle warna Boy/Girl x 3 palet**: user minta 2 hal — (1) toggle ganti kombinasi warna "samain kaya di al idrisi, ambil reponya", (2) musik disamain juga sama azkacraft. Buat #2, dicek dulu pakai `md5` — `bindo.mp3`/`binggris.mp3` di app ini ternyata SUDAH byte-identical sama `menu.mp3`/`game.mp3` asli azkacraft (dari kerjaan BGM lama), jadi gak ada yang perlu diubah, cukup dikonfirmasi ke user. Buat #1: `src/data/bindoTheme.js` (baru) nyimpen 6 kombinasi (`PALETTES`) + swatch preview (`SWATCH_COLORS`), di-port PERSIS dari `azkacraft/style.css` (`:root`/`[data-gender][data-palette]`, 6 blok) dan `script.js` (`PALETTE_COLORS`/`THEME_KEY`/`loadTheme`/`saveTheme`), persisten `localStorage` key `jk_bindo_theme`. Dipasang CUMA di `BindoStorybookTrail.jsx` (layar depan) — semua warna hardcoded (`BRAND`/`ACCENT_1`/dst) diganti jadi derive dari `pal = PALETTES[theme.gender][theme.palette]`. `BindoQuestMap.jsx` SENGAJA gak disentuh — warna bab-nya dari `TOPIC_STYLE` (kategori soal: Spelling/Vocabulary/dst), independen dari brand/accent theme di sumber aslinya juga. Toggle UI (pill Boys/Girls + 3 swatch dot) ditaro di bawah kartu trivia, port persis `.gender-pill`/`.theme-swatch` CSS. Diverifikasi: switch ke tiap kombinasi (screenshot Coral Bloom & Lilac Dream, semua elemen — ribbon/kartu Solo-Multiplayer/ikon info-card — ikut berubah bareng), persisten abis reload (`localStorage` kebaca ulang), gak ada console error.

## Bahasa Inggris — "Kiko's World Tour" (2026-08-08, LIVE — halaman resmi, konsep ORISINIL bukan porting)

BEDA dari Bindo/IPAS/Matematika di atas: ketiganya port desain al-idrisi-games, tapi user bilang "di al idrisi udah kepake smua" (semua desain BrainBox yang relevan udah abis dipake), jadi diminta ide sendiri ("coba ide dong gimana di yang static ini mau dibikin gameplay gimana dan design gimana") buat 3 subject yang masih polos (PPKn/Bahasa Inggris/PAI). Dipitch 3 konsep sekaligus (Kampung Pancasila/World Tour/Taman Akhlak), user pilih Bahasa Inggris duluan lewat AskUserQuestion. File: `src/screens/BinggrisWorldMap.jsx` (baru).

- **Konsep**: peta dunia sederhana, tiap bab = 1 "kota" landmark ikonik ABSTRAK (emoji `🗼🏰🗽🎡⛩️🏔️🌉🏟️` di-cycle per index, SENGAJA bukan nama kota/negara asli biar netral), Kiko naik pesawat kertas ✈️ terbang nyusurin **garis rute penerbangan lurus putus-putus** (BUKAN Catmull-Rom kayak Matematika, BUKAN bezier kayak IPAS — garis lurus antar titik ala peta rute maskapai beneran, dipilih karena paling ikonik buat tema travel dan paling gampang di-generate dari layout zigzag). Posisi node zigzag kiri-kanan dengan jitter random seeded (`buildRouteLayout`, mirip pola `buildTownLayout` Matematika tapi tanpa kurva).
- **Stempel paspor** — bab selesai dapet ornamen lingkaran tinta merah putus-putus rotate -16deg bertuliskan "OK!" (custom, BUKAN checkmark hijau biasa kayak subject lain) buat kesan "cap paspor", ditambah counter `🛂 doneCount/total` di header (pola sama kayak icon Drive/Plane Matematika, slot kanan header custom inline).
- **Traveler** pesawat ✈️ (bukan truck/kapal) jalan LINEAR antar titik (interpolasi `x/y` `requestAnimationFrame` sederhana, BUKAN `getPointAtLength` SVG kayak Matematika yang butuh path curve) — lebih simpel karena rute emang garis lurus. Kiko numpang di atas pesawat, badge sama persis pola truck Matematika/kapal IPAS (tap buka `KikoChatPanel` mode `general`, bubble hint "Hai, ini Kiko!").
- **Palet warna**: navy `#2C4B6B` + merah cap pos `#D6524A` + emas `#E8B84B` + kraf cream `#F6ECD9` — SENGAJA beda dari Blockville (kayu/cherry hangat) dan SolarQuest (navy gelap luar angkasa), lebih ke nuansa "peta kuno/pos surat" biar gak keliatan reskin subject lain.
- **Route**: `/kelas/:grade/binggris` (statis, sebelum `:subject` generik) — pola SAMA PERSIS kayak bindo/ipas/matematika. `SubjectDetail.jsx` dibersihin: import `BoBridgeBanner` + kondisi `subject === "binggris"` DICABUT (dead code, binggris gak pernah lagi nyampe situ).
- Diverifikasi: `/kelas/4/binggris` nunjukin 6 kota (Daily Activities/Hobbies/School Subjects/Food & Drinks/Places in Town/Simple Directions — data ASLI dari `content/binggris/kelas_4.json`, bukan dummy), node current pulsing merah, node locked dim+gembok, tap node current beneran jalanin animasi terbang lalu navigate ke `/kelas/4/binggris/topik/bab-1` (`location.pathname` dicek), quiz-nya render soal asli, tombol back balik ke `/kelas/4`, `/kelas/4/ppkn` (subject lain) TETEP `SubjectDetail.jsx` gak ke-affect, gak ada console error.

**Revisi susulan (2026-08-08, sesi yang sama)** — user: "dino bridge delete dari bahasa inggris". Banner "Dino Bridge" (`BoBridgeBanner.jsx`) yang tadinya dipindah ke bawah `BinggrisWorldMap.jsx` DICABUT LAGI (import + JSX-nya), bukan sekadar disembunyiin. Komponennya sendiri TETEP ADA & TETEP dipake di `BindoQuestMap.jsx` (Bahasa Indonesia) — cuma dilepas dari Bahasa Inggris, subject lain gak kesentuh. Diverifikasi: `/kelas/4/binggris` udah gak nunjukin banner (map berenti bersih di node terakhir "Simple Directions"), `/kelas/4/bindo/bab` banner-nya masih ada normal, gak ada console error abis dicabut.
- **Belum dikerjain**: testing di 6 kelas (baru dites kelas 4). (PPKn & PAI udah digarap juga sekarang, lihat 2 section di bawah.)

## PPKn — "Kampung Pancasila" (2026-08-08, LIVE — halaman resmi, konsep ORISINIL bukan porting)

Konsep ke-2 dari brainstorm yang sama kayak Bahasa Inggris di atas. User sempet nanya lebih jauh "selain peta2 gitu doang, kudu gimana biar lbh seru... anak2 lbh atraktif" — dijawab dengan 5 ide lapisan tambahan (feedback real-time pas quiz/mini-game per-subject/koleksi-kustomisasi/momen perayaan/node kejutan), user setuju urutan kerja "design [peta] dulu, baru poin 1-5". File: `src/screens/PpknVillageMap.jsx` + `src/data/ppknDilemmas.js` (baru).

- **Beda dari Bahasa Inggris**: bukan cuma re-skin peta, ada MEKANIK TAMBAHAN — tap node (abis animasi jalan) BUKAN langsung navigate ke quiz, tapi buka **modal skenario dilema sipil kecil** dulu (`DilemmaModal`, generic pool 8 skenario di `ppknDilemmas.js`, di-cycle by index — bukan 1:1 per bab karena jumlah bab beda tiap kelas). User pilih 1 dari 3 opsi tindakan, dapet feedback singkat POSITIF/reflektif (SEMUA opsi dapet feedback yang ngajarin, BUKAN cuma yang "bener" — konsisten sama nada non-punitive `encouragement.js` app ini), baru tombol "Lanjut ke Kuis" nge-navigate ke quiz asli. Ada tombol "Lewati" buat anak yang gak mau ikut dilema, langsung ke quiz — safety net biar gak ngunci progress.
- **Traveler Kiko JALAN KAKI sendiri** (BUKAN numpang kendaraan kayak 3 map lain — truck Matematika/kapal IPAS/pesawat Binggris) — dipilih karena lebih pas buat tema "warga kampung", dan `Kiko.jsx` sendiri GAK DIUBAH (desainnya FINAL per keputusan sebelumnya) — cuma dipakein sebagai traveler polos, tap langsung buka `KikoChatPanel` (gak ada badge kecil terpisah kayak map lain, karena Kiko-nya sendiri udah gede jadi travelernya).
- **Jalan kampung** — SVG dua-layer (stroke coklat tebal + dash putih tipis di atasnya buat kesan marka jalan), BEDA dari style path 3 map lain (Catmull-Rom Matematika, bezier IPAS, dash tipis penerbangan Binggris) — di sini sengaja niru jalan aspal beneran.
- **Ikon bangunan kampung** `🏘️🏪🏫⛲🚩🌳🏠🎪` di-cycle per index (balai warga/pasar/sekolah/alun-alun/tugu/taman/rumah/lapangan). Bab selesai dapet badge **lencana** (🎖️, border emas) gantiin checkmark biasa, counter header `🎖️ doneCount/total` "Lencana Pancasila".
- **Palet warna**: merah `#C8313A` + emas `#D4A017` + cream `#FDF6EE` (nuansa bendera+Garuda), beda dari 4 map lain.
- **Route**: `/kelas/:grade/ppkn` (statis, sebelum `:subject` generik). Gak ada cleanup di `SubjectDetail.jsx` (ppkn emang gak pernah punya percabangan kondisional di situ sebelumnya).
- Diverifikasi: `/kelas/4/ppkn` nunjukin 4 bab kelas 4 (data ASLI dari `content/ppkn/kelas_4.json`), tap node current jalanin animasi jalan lalu buka modal dilema, pilih opsi nunjukin feedback lalu tombol "Lanjut ke Kuis" beneran navigate ke `/kelas/4/ppkn/topik/bab-1` (`location.pathname` dicek), quiz-nya render soal asli, `/kelas/4/pai` (subject lain) TETEP `SubjectDetail.jsx` gak ke-affect, gak ada console error.
- **Belum dikerjain**: testing di 6 kelas (baru dites kelas 4), poin 1-5 lapisan tambahan (feedback real-time/mini-game/koleksi/perayaan/node kejutan) belum digarap, PAI ("Taman Akhlak") masih ide belum digarap.

## PAI — "Taman Akhlak" (2026-08-08, LIVE — halaman resmi, konsep ORISINIL bukan porting)

Konsep ke-3 (terakhir) dari brainstorm yang sama kayak Bahasa Inggris/PPKn di atas — user: "ok kerjain gas" lanjutin urutan yang udah disepakati (design PPKn dulu, baru PAI, baru poin 1-5). **Abis subject ini, SEMUA 6 subject udah punya halaman tema sendiri** -- gak ada lagi yang plain `SubjectDetail.jsx`. File: `src/screens/PaiGardenPath.jsx` (baru).

- **Nuansa SENGAJA lebih TENANG** dari 4 map lain (bukan "rame kayak game") -- kontennya nilai akhlak & kisah nabi, bukan tema petualangan. Animasi lebih lambat (idle bob 2.2s vs 1.6-1.8s di map lain), gak ada mekanik tambahan kayak dilema PPKn -- tap node LANGSUNG ke quiz (pola sama kayak Bahasa Inggris), SENGAJA gak ditambahin mekanik ekstra biar tetep kalem.
- **"Jalan setapak batu"** — lingkaran kecil (bukan garis) berjejer sepanjang tiap segmen (`SteppingStones`), BEDA dari 4 style path lain (Catmull-Rom/bezier/dash penerbangan/jalan aspal 2-layer PPKn) — kesan taman, bukan jalan raya.
- **Node = tanaman, bukan bangunan/landmark** — ikon bunga `🌷🌻🌼🌺🌹🌵🍀🌾` di-cycle per index. Ukuran font ikon BEDA per status (locked kecil 22px, current 26px, done 30px, plus CSS `transition` di properti `fontSize`) — biar keliatan kayak "tumbuh" pas status berubah, ini yang paling literal ngewujudin ide "kebun yang tumbuh" dari brainstorm awal. Badge selesai pake ✨ (bukan checkmark/stempel/lencana kayak map lain), counter header `🌸 doneCount/total` "Kuncup Mekar".
- **Gapura kecil** (`GardenGate`, custom inline SVG -- kubah + 2 pilar disederhanain, hijau-emas) ditaro statis di ATAS peta (bukan per-node) sebagai elemen dekoratif pembuka, ngewujudin ide awal "bab dibuka lewat gerbang masjid kecil" tanpa bikin tiap node jadi gerbang literal.
- **Traveler Kiko jalan kaki sendiri** — pola SAMA kayak PPKn (`Kiko.jsx` gak diubah, dipakein polos, tap langsung buka chat, gak ada badge terpisah), TAPI animasi lebih lambat/kalem sesuai nuansa subject ini.
- **Palet warna**: hijau `#4C8C6B` + emas `#D9A544` + cream lembut `#F5F1E3` — beda dari PPKn (merah-emas) meski sama-sama pakai emas, kesan lebih adem/tenang.
- **Route**: `/kelas/:grade/pai` (statis, sebelum `:subject` generik). Gak ada cleanup di `SubjectDetail.jsx` (pai emang gak pernah punya percabangan kondisional di situ).
- Diverifikasi: `/kelas/4/pai` nunjukin 6 bab kelas 4 (data ASLI dari `content/pai/kelas_4.json` -- Kisah Nabi Musa & Nabi Isa/Kitab-Kitab Allah/Zakat & Sedekah/dst), gerbang render di atas, node current pulsing hijau lembut, tap node current jalanin animasi jalan lalu LANGSUNG navigate ke `/kelas/4/pai/topik/bab-1` (`location.pathname` dicek), quiz-nya render soal asli, gak ada console error.
- **Belum dikerjain**: testing di 6 kelas (baru dites kelas 4). Poin 1-5 lapisan tambahan (feedback real-time quiz/mini-game per-subject/koleksi-kustomisasi/momen perayaan/node kejutan) dari brainstorm masih belum digarap sama sekali di subject manapun — ini next step abis semua 6 subject beres desainnya.

## Toggle bahasa ID/EN (2026-08-08) — UI doang, bukan konten pelajaran

User: "Bikin togle untuk bahasa indonesia/english di landing page. Jadi anak2 bisa milih basaha inggris, dan lo translate itu smua isi konten jadi bahasa inggris. Togle kecil sederhana aja kaya di website2 seperti google dll." Scope dikonfirmasi lewat AskUserQuestion sebelum ngerjain (2 opsi: "UI doang" vs "UI + soal quiz semua subject") — user pilih **UI doang**. Alasan: nerjemahin ribuan soal 6 subject × 6 kelas itu proyek terpisah gede sendiri, dan subject "Bahasa Indonesia"/PPKn/PAI kontennya emang gak masuk akal diterjemahin ke Inggris (soal Bindo ngajarin Bahasa Indonesia, PPKn/PAI konten budaya-spesifik).

- **`src/data/LanguageContext.jsx`** (baru) — pola PERSIS sama kayak `PlayerContext.jsx`: React Context + `useState`, persisten `localStorage` key `jk_lang` (`"id"`/`"en"`, default `"id"`). `useLanguage()` expose `{lang, setLang, toggleLang}`. Di-wire di `App.jsx` ngebungkus `PlayerProvider` (independen, gak ada dependency ke player).
- **`src/data/translations.js`** (baru) — kamus datar per grup (`common`/`subjects`/`landing`/`pickSubject`/`auth`/`map`/`dilemma`/`bindo`/`quiz`), tiap grup punya `{id:{...}, en:{...}}`. `useT()` hook expose `t(group, key, vars?)` (interpolasi `{var}` sederhana via regex), plus 2 helper: `subjectName(id)`/`subjectSub(id)` khusus buat translate nama+deskripsi 6 subject (dipake `PickSubject.jsx` & header tiap map screen, GANTIIN string hardcoded per-subject biar konsisten 1 sumber).
- **Toggle UI** di `Landing.jsx`, sebelah tombol mute — 2 tombol huruf kecil "ID"/"EN" bersebelahan dalem pill abu-abu, yang aktif dapet background putih+shadow (BUKAN dropdown/switch besar, sesuai request "togle kecil sederhana aja kaya di website2 seperti google dll"). Klik langsung ganti (gak ada konfirmasi, preference toggle biasa).
- **Cakupan yang di-translate** (chrome/UI doang, isi soal & konten tetep Bahasa Indonesia apa adanya): `Landing.jsx`, `PickSubject.jsx`, `Auth.jsx`, `ScreenHeader` (`Shell.jsx`, tombol back), SEMUA 6 map screen (header/counter/loading/error/hint-bubble Kiko — judul bab/topik individual TETEP Bahasa Indonesia karena itu konten asli), modal dilema PPKn (chrome tombol "Skip"/"Continue to Quiz" — skenario & feedback-nya TETEP Bahasa Indonesia karena itu konten nilai, bukan UI), `TopicQuiz.jsx`+`QuizRunner.jsx` (label "Question X/Y"/tombol Jawab-Lanjut-Selesai/pesan benar-salah — teks SOAL & jawaban TETEP Bahasa Indonesia), tombol trigger "Ask Kiko" di `KikoTutorChat.jsx` (panel chat AI Kiko sendiri di-skip, karena responsnya generated server-side dalem Bahasa Indonesia — nerjemahin cuma tombol pembukanya doang gak konsisten kalau isi percakapannya tetep Indonesia).
- **SENGAJA gak disentuh**: `KikoGreeting.jsx` (pool sapaan random di landing, mis. "Kamu keren, Adit!" — sama kelasnya kayak `encouragement.js`, pool konten bukan UI chrome literal), isi chat AI Tutor (`KikoChatPanel`), `ParentPortal.jsx`, games (`DriveMode`/`PlaneMode`/`MathRace`/dst) — semua di luar scope pass ini, next step kalau diminta lanjut.
- **Bug ketemu+fix pas ngerjain**: `BindoQuestMap.jsx` punya `topics.map((t, i) => ...)` yang variabel loop-nya JUGA namanya `t` — collide sama `const { t } = useT()` (function translate) dalem scope callback yang sama, bakal nge-throw "t is not a function" kalau dipanggil di situ. Fix: rename loop var jadi `topic` (bukan alias translate function-nya, biar konsisten sama pola pemanggilan `t(...)` di semua file lain). 5 map screen lain PUNYA pola `.map((t, i) => ...)` yang sama tapi DIPERIKSA aman -- semua panggilan `t(...)` di file-file itu ada di LUAR scope callback map (di blok `{travelerPos && (...)}` yang sibling, bukan nested).
- Diverifikasi: toggle klik EN → semua string di atas ganti seketika (screenshot Landing/PickSubject/PPKn map/dilema modal/quiz), toggle balik ke ID → teks PERSIS sama kayak sebelum fitur ini ada (gak ada drift, dicek word-by-word di screenshot "Hai, Adit!"/"Keluar"/"Kiko disini"/dst), localStorage persisten lintas navigasi, gak ada console error di sepanjang alur (Landing → PickSubject → PPKn map → dilema → quiz → jawab).
- **Belum dikerjain**: PPKn/PAI/Bindo/IPAS/Matematika/Binggris testing cuma di kelas 4. `ParentPortal.jsx` dan games (Drive/Plane/MathRace/DinoRace/NinjaRunner/BoBridge) belum ke-translate sama sekali.

## Ikon subject di PickSubject diganti dari emoji ke custom SVG (2026-08-08)

User kirim screenshot kartu subject di `PickSubject.jsx` (semua masih pake emoji mentah 🏁🔢🔬🇮🇩🕌📖🔤🎯🥷): "Design ulang iconnya yang bagus dan sedikit 3d. jangan terlalu emoticon gini jelek." File baru: `src/components/ds/SubjectIcons.jsx` (9 komponen ikon).

- **Style konsisten**: gradient 2 warna (terang→gelap, linear atau radial tergantung bentuk) + `feDropShadow` filter halus + highlight glossy (ellipse putih semi-transparan pojok kiri-atas, pola SAMA kayak `Kiko.jsx`) -- kesan "sedikit 3D"/sticker gaya Duolingo-Kahoot, BUKAN flat kayak font emoji OS.
- **9 ikon**: `MathRaceIcon` (bendera catur checkered), `MathNumbersIcon` (tile biru "12/34"), `ScienceIcon` (labu erlenmeyer -- awalnya coba bentuk mikroskop tapi gak kebaca di ukuran kecil, diganti labu kimia yang lebih ikonik), `CivicsFlagIcon` (bendera merah-putih di tiang emas), `MosqueIcon` (kubah+menara masjid, disederhanain -- versi awal ada 2 menara samping bikin keliatan kayak gembok/tas, dipangkas jadi kubah+1 menara doang), `BookIcon` (buku kebuka gradient coklat), `EnglishBubbleIcon` (chat bubble biru isi "Aa"), `FocusTargetIcon` (papan target radial merah-putih + panah kecil), `NinjaStarIcon` (shuriken metalik abu-abu, di-rotate 15° biar dinamis).
- **2 iterasi revisi di sesi yang sama** (dicek visual via browser SEBELUM dianggap kelar, bukan cuma asumsi kode bener): `ScienceIcon` v1 (mikroskop garis-garis abstrak) dan `MosqueIcon` v1 (kubah+2 menara) TERNYATA gak kebaca jelas di ukuran 52×52 pas discreenshot -- diganti versi lebih simpel (flask 1 bentuk solid, kubah+1 menara doang) yang legible.
- **`PickSubject.jsx`**: `SUBJECT_ICONS` map (subject id → komponen ikon) gantiin `s.emoji` buat 6 subject; Math Race/Fokus Latihan/Ninja Runner juga diganti icon barunya masing-masing. `GameCard.jsx` SENGAJA gak diubah -- prop `icon` udah generic (nerima apa aja termasuk elemen React), jadi cukup ganti apa yang di-pass dari pemanggilnya.
- **SENGAJA gak disentuh**: emoji subject di tempat lain (`ParentPortal.jsx` list kecil 22px, `PageDecor.jsx`/`LandingDecor.jsx`/`AuthDecor.jsx` -- itu ornamen ambient background acak, beda konteks, bukan kartu pilihan yang di-screenshot user) dan `SubjectDetail.jsx` (udah jadi dead code, gak ada subject yang nyampe situ lagi).
- Diverifikasi: build clean, screenshot full-height (`resize_window` 450×1600 biar semua 9 kartu keliatan sekaligus tanpa scroll gesture yang browser-pane kadang gagal capture abis itu) nunjukin semua 9 ikon legible & konsisten gayanya, gak ada console error.

## 3 bug report abis full QA (2026-08-09)

User: "Bug: 1. Saat mathrace, posisi mobil terbalik... 2. Drive mode, dinonya culun banget... 3. Saat flight mode, pesawat anak buah masih icon kebalik." Tiga-tiganya soal orientasi sprite, ketemu setelah full QA pass (session sebelumnya) gak nyoba edge case ini secara spesifik.

- **Math Race — mobil kebalik**: `RaceLane` (di `MathRace.jsx`) render emoji mobil (`🚗`/`🚕`/`🚙`) via `left: progress%` yang NAIK dari kiri ke kanan, tapi emoji mobil defaultnya ngadep KIRI (kayak font emoji laen) -- jadi keliatan mundur. Fix: `transform: scaleX(-1)` di elemen avatar (pola PERSIS kayak fix walker Dino Bridge sebelumnya, `BoBridgeBanner.jsx`). Diverifikasi: `getComputedStyle` konfirmasi `matrix(-1,0,0,1,...)` kepasang, screenshot pas progress ~50% nunjukin mobil ngadep kanan (arah jalan) dengan jelas.
- **Math Race — musik + kata semangat**: dua-duanya belom ada sama sekali sebelumnya. Musik: dicek dulu md5 sebelum nyalin file baru -- `multipleazka/audio/bgm/bgm.mp3` (BGM Math Race al-idrisi) ternyata BYTE-IDENTIK sama `plane.mp3` yang UDAH ADA di repo ini, jadi cukup nambah `TRACK_MATHRACE = "/audio/bgm/plane.mp3"` (`bgm.js`) + `useBgmTrack(TRACK_MATHRACE)` di `MathRace.jsx`, gak perlu file audio baru. Kata semangat: REUSE `pickEncouragement()` yang udah ada (`encouragement.js`, dipake TopicQuiz/FocusRoundQuiz) -- BUKAN nyalin teks al-idrisi (`CHEERS_CORRECT`/`CHEERS_WRONG` di `multipleazka/script.js` itu HARDCODE nama anak `${CHILD_NAME}` di tiap baris, gak cocok sama request "tanpa nama"; app ini juga udah punya sistem encouragement Indonesia non-personalized sendiri, tinggal dipake ulang biar konsisten 1 sumber di seluruh app). Tier accuracy di-derive dari posisi finis (juara1/solo=HIGH, juara2=MID, sisanya=LOW) karena Math Race emang gak nge-track akurasi soal. Ditampilin di layar "finished", state `encouragement` di-set sekali di `finishRace()` (bukan inline render, biar gak re-random tiap re-render).
- **Drive Mode — dino "culun"**: `DinoSvg` (`vehicleArt.jsx`) pernah di-redesign 2026-08-07 (badan/kaki/duri jadi lebih jelas top-down), TAPI kepalanya diganti jadi lingkaran polos + 2 titik mata -- ternyata BUKAN desain terbaik yang ada di al-idrisi. `RoamingCarDino.jsx` (dino ngejar mobil di Landing, di-port PERSIS dari `index.html` hub al-idrisi) udah lebih dulu pake bentuk kepala TENGKORAK MELANCIP (moncong dino beneran) yang gak ikut dipake pas redesign Drive Mode kemarin. Fix: kepala `DinoSvg` diganti PERSIS sama shape `RoamingCarDino.jsx` (`M15 2 Q9 2.5 8.5 9 Q8 15 15 17.5 Q22 15 21.5 9 Q21 2.5 15 2 Z`), badan/ekor/4-kaki/duri punggung dari redesign #1 DIPERTAHANIN (itu perbaikan top-down yang valid), duri digeser turun (y 17→26) biar gak numpuk sama kepala yang sekarang lebih panjang ke bawah. Diverifikasi via screenshot in-game (Drive Mode kelas 4) -- moncong dino keliatan jelas, bukan lingkaran polos lagi.
- **Plane Mode — pesawat "anak buah" (wingmen) kebalik**: pesawat utama UDAH di-fix nose-up SVG dari sesi lama (`planeArt.jsx`, gantiin emoji 🚀 yang suka "miring"), tapi 2 pesawat wingmen (power-up `wingmen`, `ensureWingmen()` di `PlaneMode.jsx`) masih pake emoji mentah `🛩️` yang orientasinya gak konsisten antar platform -- persis masalah yang sama kayak pesawat utama dulu, cuma belum ke-fix di sini. Fix: `el.textContent` diganti `el.innerHTML` isi SVG mini nose-up (`M10 1 L14 14 L10 11 L6 14 Z`, viewBox 20x24) -- BUKAN React component (wingmen dirender lewat DOM manipulation langsung buat performa, sama kayak bullet/musuh lain, jadi gak bisa pake `PlaneSkinSvg` React biasa). Diverifikasi: markup SVG-nya dites isolated (di-append manual ke DOM, discreenshot) nunjukin bentuk nose-up yang bener, konsisten sama ikon pesawat utama di header; gak sempet mancing power-up wingmen keluar beneran di gameplay (drop-nya acak dari 5 tipe power-up) tapi kodenya udah exact pattern yang sama kayak fix pesawat utama yang udah terbukti jalan.
- Diverifikasi ketiganya: `npm run build` clean, gak ada console error di Drive Mode/Math Race (main penuh 1 race sampe selesai)/Plane Mode.

## Gameplay baru Bindo & Binggris — Susun Kata + Tebak Kata + Koleksi Frasa (2026-08-09)

User: "gameplay apa yang harus gw kembangin buat bahasa indonesia dan bahasa inggris agar lebih menarik? ga static aja" — dipitch beberapa opsi, user pilih SEMUA ("boleh garap keduanya gas gas gas"): 1 mini-game shared (Susun Kata) + 2 perluasan dari fitur yang udah setengah jadi (trivia Bindo, stempel paspor Binggris).

- **`src/games/scramble/WordScramble.jsx`** (baru) — mini-game "Susun Kata", SHARED lintas Bindo & Binggris (1 komponen, `subject` dibaca dari route param, sama pola kayak `GlassBridge`/Dino Bridge yang juga generic `:subject`). Route generic `/kelas/:grade/:subject/susun-kata` (App.jsx). Data: `src/data/wordScramble.js` (`SENTENCES.bindo`/`SENTENCES.binggris`, 10 kalimat pendek 5-kata tiap bahasa, SVO simpel biar cuma ada 1 urutan yang masuk akal). 5 ronde per sesi (`ROUNDS_PER_SESSION`), tap kata di tray buat nyusun ke jawaban (tap lagi buat undo), "Acak Ulang" ngocok ulang tray tanpa ganti kalimat, "Periksa" ngebandingin jawaban PERSIS sama kalimat asli. Hasil akhir reuse `pickEncouragement()` (bukan bikin pool baru). **BUKAN bagian sistem progress/XP resmi** (gak nulis Firestore) -- pola sama kayak Dino Bridge, murni bonus mini-game. Entry point: banner CTA di `BindoStorybookTrail.jsx` (di bawah kartu trivia, sebelum toggle tema) dan `BinggrisWorldMap.jsx` (gantiin slot Dino Bridge yang udah dicabut sebelumnya).
- **Bindo — "Tebak Kata"**: kartu "Kata Hari Ini" yang tadinya CUMA info pasif sekarang jadi tombol (`onClick={() => setGuessOpen(true)}`) buka modal `GuessWordModal` (di file yang sama, `BindoStorybookTrail.jsx`) -- 4 ronde tebak kata dari artinya (multiple choice, 1 jawaban bener + 3 distractor acak), REUSE pool `WORD_OF_DAY` yang udah ada di `bindoTrivia.js` (30 entri) -- gak nambah data baru, cukup di-acak jadi soal. Pola modal SAMA PERSIS kayak `DilemmaModal` PPKn (fixed overlay, card putih, warna ikutin `pal` tema aktif).
- **Binggris — "Koleksi Frasa"**: `src/data/binggrisPhrases.js` (baru, 8 pasang frasa Inggris+arti tema travel) -- strip horizontal-scroll di `BinggrisWorldMap.jsx` (di atas banner Susun Kata), 1 kartu per topik/bab (`phraseForIndex(i)`, cycle by index kayak `ppknDilemmas.js`), kartu KEBUKA (nampilin frasa+arti) kalau bab-nya udah `status === "done"`, kalau belum nampilin placeholder "🔒 Selesaikan bab buat buka". Ngisi slot yang ditinggal kosong pas Dino Bridge dicabut dari Binggris sebelumnya.
- **Bug ketemu+fix pas verifikasi**: key translation `scramble.entrySubBinggris` versi `id` KETUKER isinya teks Inggris (copy-paste error nulis dictionary-nya) -- ketauan pas toggle ID nunjukin subtitle banner "Arrange the words into English!" padahal harusnya Indonesia. Fix: diganti "Susun kata jadi kalimat Inggris yang benar".
- Diverifikasi: main penuh Tebak Kata (4 ronde, termasuk jawaban salah -- highlight merah/hijau kebaca bener) sampe hasil akhir + encouragement muncul; main penuh Susun Kata Bindo (susun "Burung berkicau merdu di pohon" dari acak, "Betul!" muncul abis Periksa) DAN versi Binggris (kalimat Inggris beda, komponen sama kepake bener buat 2 subject); toggle EN nunjukin "Word Scramble"/"Round 1/5"/"Shuffle Again"/"Check" semua translated, konten kalimat tetep sesuai bahasa aslinya; koleksi frasa nunjukin placeholder terkunci yang bener (0 bab selesai kelas 4 test); gak ada console error di semua alur.
- **Belum dikerjain**: testing di 6 kelas, poin #3-5 dari brainstorm awal (koleksi kustomisasi/momen perayaan/node kejutan) belum digarap.

## Gaya kerja user (sama kayak BrainBox punya, disalin langsung)
- Komunikasi campur Indonesia-Inggris.
- **Diskusi dulu sebelum eksekusi** kalau ada keputusan arsitektur/scope — baru "gas"/"lanjut" abis sepakat. Tapi task yang udah dikasih instruksi detail (misal restrukturisasi soal dengan spec lengkap) boleh langsung dikerjain tanpa nanya ulang tiap step.
- **Ikutin spec/wireframe persis**, jangan improvisasi kalau ada yang ambigu — tanya, jangan nebak (kejadian: subject IPA/IPS vs IPAS, ditanya dulu baru dieksekusi).
- Testing di real device krusial buat bug yang gak muncul di desktop — belum pernah dites di device asli sama sekali buat project ini (masih full browser-pane testing).
- **Aksi yang butuh login interaktif (GitHub OAuth, Firebase Console) gak bisa diwakilin** — kasih instruksi jelas step-by-step, user yang jalanin sendiri, baru lanjut.

## 3 bug report lagi (2026-08-09, sesi yang sama abis gameplay Bindo/Binggris)

- **Kiko chat panel kepotong di iPhone asli**: user kirim screenshot device asli nunjukin seluruh halaman (bukan cuma panel-nya) keliatan kegeser ke kanan pas panel chat kebuka + keyboard aktif. `html`/`body` SEBELUMNYA gak punya `overflow-x: hidden` sama sekali -- kombinasi itu + kuirk iOS Safari yang suka nge-shift elemen `position: fixed` pas soft-keyboard muncul (visual viewport vs layout viewport beda) bisa bikin halaman jadi bisa di-scroll horizontal dikit, cukup buat "geser" tampilan. Fix defensif (`tokens.css`): `html, body { overflow-x: hidden; width: 100%; }`. Sekalian dikerasin `KikoChatPanel` (`KikoTutorChat.jsx`): overlay fixed dikasih `width/maxWidth: 100vw` + `boxSizing: border-box` eksplisit, input chat dikasih `minWidth: 0` (defense kalau ada teks super panjang di elemen `flex:1` bikin overflow), 3 bubble chat dikasih `wordBreak: "break-word"` (defense kalau user ngetik 1 kata super panjang tanpa spasi). **Gak bisa dites 100% presisi di browser-pane** (gak bisa simulasiin soft-keyboard iOS beneran), tapi diverifikasi: resize ke preset mobile (375px), `document.documentElement.scrollWidth` PERSIS sama kayak `window.innerWidth` (375=375, gak ada overflow sama sekali), ngetik 1 kata 80 karakter tanpa spasi ke chat -- bubble-nya wrap rapi, gak dorong lebar panel.
- **Sapaan Kiko di bawah "Pilih kelas kamu!" gak translate ke EN**: `KikoGreeting.jsx` SENGAJA di-skip pas kerjaan toggle bahasa sebelumnya (dianggap "pool konten" kayak `encouragement.js`), tapi user nganggep ini BUG, bukan out-of-scope. Fix: `kikoGreetings.js` sekarang punya 2 pool (`KIKO_GREETINGS_ID`/`KIKO_GREETINGS_EN`, 50-50, index PARALEL biar `dayIndex % length` nunjuk ke pasangan makna terkait di 2 bahasa), `greetingTemplateForToday(lang)` sekarang minta parameter lang, `KikoGreeting.jsx` manggil `useT()` buat dapetin `lang` aktif. Sekalian dibenerin 2 string laen yang ketinggalan di file yang sama (`aria-label`, hint "Tap Kiko!") pake key translation yang udah ada/baru ditambah.
- **Drive Mode soft-lock abis obstacle abis**: `SCORE_TARGET = 12` tapi jumlah obstacle awal cuma 6 (easy) / 8 (medium) / 9 (hard) -- MATEMATIS GAK MUNGKIN nyampe 12 di difficulty manapun, karena obstacle yang abis ditabrak DIHAPUS PERMANEN (`filter`) tanpa pernah diganti. Begitu obstacle abis, gak ada lagi yang bisa ditabrak buat munculin soal baru -- mobil nyangkut selama-lamanya (cuma idle-bounce animation yang keliatan "goyang2"), skor gak akan pernah nyampe target atau digigit dino (game gak pernah brenti). User: "harusnya soal muncul lagi sampai poin tercapai atau mati digigit dino." Fix: `spawnReplacementObstacle()` (baru, `DriveMode.jsx`) -- tiap obstacle abis ditabrak, LANGSUNG di-spawn 1 obstacle baru di posisi acak (avoid posisi mobil SEKARANG via `g.x/g.y`, bukan cuma `CAR_START` kayak `makeObstacles` awal), jumlah obstacle di layar tetep KONSTAN sepanjang permainan, jadi soal terus muncul sampe beneran nyampe 12 atau kena gigit dino 3x. Diverifikasi: teleport posisi mobil (lewat React Fiber, `gameRef.current.x/y`) ke atas 1 obstacle 2x berturut-turut, tiap kali skor naik + soal baru muncul + jumlah obstacle di React state TETEP 6 (bukan berkurang) -- dicek dari `obstacles` state langsung, bukan cuma DOM (percobaan pertama pake regex-emoji buat itung DOM sempet false-alarm karena ikon "⚠️" di luar range Unicode yang di-tes).
- Diverifikasi ketiganya: `npm run build` clean, gak ada console error baru (404 AI Tutor yang muncul itu emang limitation dev-environment lokal yang udah ada dari sebelumnya, bukan regresi).

## Ninja Runner + Plane Mode disamain sama update al-idrisi (2026-08-10)

User minta baca `CLAUDE.md` al-idrisi-games buat cari update baru di Ninja Runner & Plane Mode, terus "kerjakan semua". Dibandingin dulu sama kode kita sebelum ngerjain -- ternyata SEBAGIAN udah ada di sini (jangan diulang), sisanya baru.

**Ninja Runner:**
- **Dodge jadi PRESISI** (`NinjaRunner.jsx`) -- versi lama: tap JUMP kapan aja selama batu mendekat (1.8s) otomatis dianggap lolos, cuma flag `obstacleDodgedRef`. Sekarang yang dicek APAKAH masih di udara TEPAT pas batu nyampe (`jumpingRef.current` dibaca sinkron di dalem setTimeout resolusi) -- lompat kepagian (udah mendarat) atau kelamaan (belum lompat) dua-duanya nabrak. Window lolos jadi 450ms (`OBSTACLE_MS - JUMP_MS` = 1350-1800ms). `jumpingRef` WAJIB ref, bukan state `jumping` -- closure setTimeout bakal baca nilai lama.
- **Grace period obstacle pertama** `FIRST_OBSTACLE_DELAY_MS=700` -- cuma buat batu PERTAMA tiap run (`qnum === 1`), batu setelahnya gak perlu karena udah ada jeda alami dari soal sebelumnya. Alasannya sama kayak al-idrisi: begitu dodge butuh timing presisi, batu yang langsung meluncur pas layar kebuka jadi gak adil.
- **Musuh kebelah 2 diagonal pas jawaban bener** -- dulu musuh LANGSUNG DILEPAS begitu "nyampe" (`setEnemyApproaching(false)`), jadi gak ada apa-apa buat ditebas. Sekarang `enemyState` punya 4 fase (`approaching`→`standing`→`split`→null): musuh BERDIRI di tempat selama kartu subject + soal, baru kebelah pas jawaban bener (clip-path + flash brightness, teknik sama kayak kartu soal `.jk-nj-qhalf` yang emang udah ada). Elemen `.jk-nj-run-lane` sekarang SELALU ke-render (bukan cuma `phase === "lane"`) supaya musuhnya bisa persist lintas fase.
- ⚠️ **Bug ketemu pas verifikasi (dari perubahan ini sendiri)**: posisi berhenti musuh (`right: 84%`) NUMPUK persis sama Kiko -- dulu gak keliatan karena musuhnya langsung dilepas, sekarang dia berdiri di situ jadi ketimpa. Diukur di browser: enemy x 68-98 vs runner x 58-102, `overlap: true`. Fix: berhentiin di `right: 70%` (animasi lari + state standing/split disamain), jadi ada jarak ~21px -- kebaca sebagai dua karakter berhadapan.
- **SENGAJA GAK di-port**: sistem nyawa al-idrisi (`NINJA_START_LIVES=3`, game over di tengah jalan). Di sini Ninja Runner posisinya latihan santai lintas-subject; nambah nyawa ngubah keseimbangan kesulitan buat anak SD kelas bawah, dan itu di luar 4 poin yang disepakati. Kena batu sekarang cuma feedback visual (`.jk-nj-runner.hit`).
- Poin ke-4 al-idrisi (bulatan jawaban numpuk di soal cerita panjang) TERNYATA UDAH ke-fix di sini dari dulu -- `.jk-nj-encounter` emang udah flex-column, bukan fixed `top` offset. Gak ada yang perlu diubah.

**Plane Mode:**
- **4 tipe musuh biasa, pola gerak beda** (`ENEMY_TYPES`) -- sebelumnya 3 emoji tapi SEMUA gerak identik (sinus doang). Sekarang: 👾 straight (gak geser horizontal), 👽 sine (pola lama), 🛸 homing (pelan tapi ngedeketin kapal), 🦇 zigzag (cepet, belok patah-patah). `speed` jadi pengali `ENEMY_SPEED` per tipe.
- **4 tipe boss** (`BOSS_TYPES`, di-cycle `bossesDefeated % 4`) -- 🐉 hp8 / 🦂 hp10 / 👹 hp12 / 🦑 hp11+gerak angka-8. Tiap tipe punya speed & fire-rate sendiri. `bossHp` HUD sekarang baca `hpMax` per-instance, bukan konstanta global.
- **Spread tembakan boss dibedain** -- musuh biasa `ENEMY_AIM_SPREAD_DEG=24`, boss `BOSS_AIM_SPREAD_DEG=12` (lebih akurat = kerasa naik level). `spawnEnemyBullet()` sekarang nerima parameter spread.
- **UDAH ADA dari dulu, gak diapa-apain**: peluru musuh yang di-aim ke posisi kapal (bukan lurus ke bawah), mode endless (`handleBossDefeat()` cuma naikin density/threshold, `endRun()` CUMA kepanggil pas nyawa habis + respawn gauntlet abis), power-up 5 macem, wingmen, respawn gauntlet. Dicek satu-satu sebelum ngerjain biar gak dobel.

**Verifikasi** (+ keterbatasannya, penting buat sesi berikutnya):
- Musuh kebelah: dikonfirmasi di browser -- 2 elemen `.jk-nj-enemy-half` dengan clip-path berlawanan (`polygon(0 0,100% 0,0 100%)` vs `polygon(100% 0,100% 100%,0 100%)`) + dua animasi `jk-nj-esplit-a/b` jalan, BARENGAN sama 2 half kartu soal.
- Jarak musuh-vs-Kiko diukur ulang abis fix: gap 21px, `overlap: false`.
- 4 pola gerak musuh Plane Mode: di-seed manual 1 tiap tipe ke `gRef` lewat React fiber, terus diukur delta posisinya antar frame -- straight dX=0, sine dX=+0.56, homing dX=−0.24 (bener-bener nge-arah ke kapal di x=50 dari x=56), zigzag dX=+0.44; dY-nya juga beda-beda ngikutin `speed` (0.8 > 0.7 > 0.63 > 0.56). Tabel `ENEMY_TYPES`/`BOSS_TYPES` + urutan cycling boss diverifikasi lewat node yang baca LANGSUNG dari source file (bukan diketik ulang).
- ⚠️ **Timing dodge presisi GAK BISA dites real-time di browser-pane** -- setInterval/rAF sub-detik ke-throttle parah pas pane lagi hidden (keterbatasan yang emang udah kecatat di CLAUDE.md ini buat game lain). Yang diverifikasi: aturan keputusannya di-replay di node (window lolos 1350-1800ms, tap di 0/200/800/1200ms = BUMPED, tap di 1500/1799ms = DODGED). **Timing beneran di jari anak belum dites di device asli** -- kalau kerasa terlalu susah/gampang, tinggal geser `JUMP_MS` (lebih gede = window lebih longgar).
- Build clean, gak ada console error.

**Yang SENGAJA gak dikerjain: 2-player multiplayer Plane Mode (WebRTC).** Ini fitur paling gede di update al-idrisi, TAPI ditahan bukan karena males: (1) browser-pane di sini TOTAL gak bisa nyambungin WebRTC ICE -- al-idrisi's CLAUDE.md sendiri nyatet ini eksplisit (mereka buktiin lewat loopback test 2 RTCPeerConnection di 1 tab, tetep gagal), jadi ~800 baris kode networking bakal masuk production TANPA bisa diverifikasi sama sekali; (2) al-idrisi nemu 4 bug nyata di fitur ini yang CUMA ketauan setelah dites di 2 HP fisik (gagal connect beda jaringan, kartu soal geter, wingmen gak kesiar, respawn gauntlet ilang) -- artinya jalur ini emang gak bisa dipercaya tanpa real-device test. Kalau mau digarap, harus direncanain bareng testing di 2 HP beneran.

## Yang masih perlu ditindaklanjuti
1. ~~Konten IPS/PAI/B.Indo/B.Inggris belum ada sama sekali~~ — **B.Inggris (526 soal) & B.Indonesia (282 soal) selesai dibikin & AKTIF di app 2026-08-04**, lihat bagian "Bahasa Inggris & Bahasa Indonesia — konten baru". **PAI: 168 soal draft selesai ditulis 2026-08-04 TAPI SENGAJA BELUM diaktifin di app** — lihat bagian "PAI — draft konten (belum aktif)" di bawah, WAJIB direview ustadz dulu. IPS udah gabung ke IPAS (bukan gap).
2. ~~80 soal IPAS + 341 soal Matematika belum_terpetakan~~ — **selesai direklasifikasi 2026-08-04**, lihat bagian "Mismatch kurikulum — SUDAH diselesaikan".
3. ~~1 akun test nyangkut di Firestore production~~ — **dihapus 2026-08-04**.
4. ~~Hero illustration di Landing masih placeholder emoji~~ — **diganti SVG ilustrasi anak baca buku 2026-08-04** (`components/ds/HeroIllustration.jsx`), lihat bagian "Hero Illustration" di bawah.
5. ~~Drive Mode & Plane Mode masih v1/core-engine~~ — **SELESAI 2026-08-05**: Plane Mode (power-up/boss/respawn-gauntlet/endless/high-score) dan Drive Mode (nitro/water-gun/vehicle-skin) sekarang punya hampir semua fitur lanjutan BrainBox, full verified via gameplay asli (lihat bagian Games). ~~DinoRace beneran (2-player racing) belum di-porting~~ — **SELESAI 2026-08-05 juga, tapi via reuse file statis, bukan porting ke React** (lihat bagian Games, `public/dinorace/`) — 2 entry point: dino jalan-jalan satu arah di Landing (`WalkingDino.jsx`, lihat bagian "Games" → DinoRace buat histori lengkap gonta-ganti entry point-nya) + easter egg lama.
6. ~~Parent Portal XP breakdown masih total doang~~ — **selesai 2026-08-04**, section "XP per Pelajaran" baru, lihat bagian Parent Portal.
7. ~~Stars/XP di TopicQuiz cuma dihitung dari 1 round, gak ada tombol ulang~~ — **tombol "Ulangi buat Naikin Bintang" ditambahin 2026-08-04** ke `TopicQuiz.jsx`, `progressService.starsFor()` di-export biar dipakai UI juga. Diverifikasi di browser: run 1/8 benar → 0⭐ dim, retry 7/8 benar → 2⭐ terang.
8. **Firebase Authentication belum diaktifin di console** (cuma Firestore) — belum masalah karena auth sistem sendiri (name+PIN) gak pakai Firebase Auth SDK, tapi kalau nanti butuh Google Sign-In/dst buat orang tua, perlu diaktifin.
9. **Belum ada dual-deploy pattern** kayak BrainBox (standalone domain per produk) — wajar karena masih 1 domain (`jagoan-kelas.vercel.app`), belum relevan sampai ada kebutuhan domain kustom.

## Hero Illustration (2026-08-04, DIGANTI Kiko 2026-08-06)
`Landing.jsx` hero slot (220x160, div yang sama dipake buat tap-quadrant DinoRace easter egg) awalnya render `<HeroIllustration />` (SVG flat-style anak duduk baca buku, hand-coded, bukan foto/AI-gen) — **komponennya udah DIHAPUS** (`components/ds/HeroIllustration.jsx`), diganti `<Kiko size={140}/>` pas batch kerjaan mascot (lihat "Kiko — maskot resmi") karena sekarang app punya maskot resmi beneran, jadi hero slot logisnya jadi "rumah" utama Kiko bukan ilustrasi generik anak baca buku. `useSecretTap` tetap jalan gak berubah (quadrant dihitung dari `getBoundingClientRect()` div pembungkus, bukan dari isi di dalamnya — ganti konten anak div aman gak ganggu easter egg, sama kayak pas ganti dari emoji placeholder ke HeroIllustration dulu).

## PAI — konten AKTIF, user udah konfirmasi review (2026-08-04)
168 soal (6 kelas x 6 bab) ditulis (`content-pipeline/draft_pai.py` + `build_pai.py`, output `content-pipeline/output/pai/kelas_{1-6}.json`), lalu **di-copy ke `app/src/data/content/pai/` dan ditambahin ke `SUBJECTS_WITH_CONTENT`** di `contentLoader.js` setelah user eksplisit bilang "oke konfirmasi gas" — dianggap sebagai konfirmasi review, subject PAI SEKARANG AKTIF & tampil ke anak (udah diverifikasi jalan di browser, kelas 4 bab "Kisah Nabi Musa & Nabi Isa" render bener).

- Struktur bab (`PAI_BAB` di `bab_struktur.py`) DIRANCANG SENDIRI (bukan port 10-bab Buku Siswa resmi kelas 1 yang ditemukan riset), diringkas 6 bab/kelas biar konsisten sama subject lain.
- Konten SENGAJA dibatasi ke materi universal disepakati lintas ormas/mazhab umum di Indonesia (rukun Iman/Islam, nama 25 nabi & rasul, nama kitab, asmaul husna & kalimat thayyibah paling umum, akhlak/adab sehari-hari, kisah nabi versi umum) — **SENGAJA MENGHINDARI** fiqih yang berpotensi beda pendapat (detail rakaat sholat, nisab zakat, dst). Karena itu beberapa bab lebih tipis (4-7 soal) dibanding target subject lain — kualitas/keamanan konten diutamakan di atas kuantitas buat topik sesensitif ini.
- Semua soal masih tertandai `"sumber": "draft_ai_perlu_review"` di data-nya (gak diubah pas aktivasi) — kalau user minta tambahan/revisi materi PAI nanti, tetep perlakukan sehati-hati draft awal (jangan asal generate soal baru tanpa research yang sama ketatnya).

## 14 bug report dari jagoankelas.fun production (2026-08-10)

User laporin 14 masalah sekaligus abis coba app-nya beneran di production (bukan browser-pane) -- campuran bug kecil, 1 fitur yang KETINGGALAN kepasang, dan 1 fitur besar yang bener-bener belum ada. Dikerjain semua dalem 1 sesi panjang, di-triase dulu (investigasi tiap satu) sebelum eksekusi karena skalanya beda-beda jauh.

**#1 DinoRace tombol home nyasar** — `public/dinorace/index.html` `hubBackBtn` masih `window.location.href = "/kelas"`, padahal route itu DIHAPUS dari `App.jsx` sejak `PickGrade.jsx` digabung ke `Landing.jsx` (lihat bagian "Kiko"). Fix: diarahin ke `/`.

**#3 Chat Kiko kegedean di iPhone 13 Pro** — `KikoTutorChat.jsx` panelnya `maxHeight:"80vh"`, di layar 844px itu ~675px, nutup hampir semua layar apalagi pas keyboard aktif. Fix: `maxHeight:"min(58vh, 460px)"`.

**#9 Semua layar "ada card besar sendiri"** — `Shell.jsx` sebelumnya `<main>` dikasih padding 28px/16px + card di dalemnya `border-radius:32px` + shadow gede, keliatan kayak card ngambang di atas background gradient body (bukan full-bleed kayak playalidrisi). Root cause: struktur `#app` al-idrisi SEBENERNYA mirip (`max-width:480px; margin:0 auto`), tapi TANPA padding luar/border-radius/shadow gede -- di layar HP asli (viewport <480px) itu artinya ngisi PERSIS tepi-ke-tepi, shadow lembutnya cuma keliatan kalau browser lebih lebar dari 480px (preview desktop doang). Fix: `Shell.jsx` disederhanain jadi 1 wrapper (bukan 2 nested div), `maxWidth:480`, gak ada padding/border-radius, shadow diringankan ke `0 0 40px rgba(0,0,0,.08)` (port persis nilai al-idrisi). Nyentuh SEMUA layar tapi diverifikasi di 5+ tipe layar beda (Landing/PickSubject/peta subject/picker game/gameplay aktif) gak ada yang somehow kepotong.

**#4/#6/#8/#10 (Matematika/IPAS/PAI/Bindo abis 8/8 soal gak ada rekap+Kiko)** — ternyata SATU titik perbaikan, bukan 4 -- semua subject lewat `TopicQuiz.jsx` yang sama (`/kelas/:grade/:subject/topik/:babKey`). Sebelumnya cuma nampilin skor+bintang+2 tombol. Port pola MathVille (`showReward()`+`loadAiHint()`): Kiko SELALU muncul abis quiz, kalau ada yang salah tampilin daftar REKAP tiap soal salah (prompt + jawaban-kamu + jawaban-benar, mirip pola review NinjaRunner yang udah ada) + tombol "Tanya Kiko soal ini" per soal yang buka `KikoChatPanel` mode `"quiz"` PRE-LOADED sama fakta soal itu (`answered:true, isCorrect:false`) biar quick-reply "Kenapa jawabanku salah?" langsung available, kalau sempurna (0 salah) tampilin 1 card ucapan selamat + tombol chat umum. Perlu nambah `wrongLog` (detail LENGKAP tiap soal salah, beda dari `results` yang cuma `{id,correct}` dan udah dipake FocusRoundQuiz) di `QuizRunner.jsx`'s `onFinish` payload. **Cakupan**: cuma yang lewat `TopicQuiz.jsx` (Matematika/IPAS/PAI/Bindo/Binggris/PPKn semua kena, karena semua map screen navigate ke situ abis tap topik) -- Focus Round (`FocusRoundQuiz.jsx`) & Ninja Runner PUNYA hasil-screen sendiri, gak lewat file ini, DILUAR SCOPE fix ini.

**#7 Load lama banget pas pindah subject, khususnya PAI** — user nanya eksplisit "Apa ada kurang di arsitektur saya?" JAWABANNYA IYA: `App.jsx` sebelumnya `import` EAGER semua screen+game (Drive/Plane/NinjaRunner/6 peta subject/dst) di top-level, jadi build production nge-bundle SEMUANYA jadi 1 file JS (>1.1MB) -- anak HARUS download+parse+eksekusi kode SELURUH APP sebelum layar pertama kebuka, gak peduli screen mana yang lagi dibuka. Fix: `lazy()` + `<Suspense>` per screen/game route -- Vite otomatis code-split, main bundle turun dari 1,168KB ke 240KB, tiap screen jadi chunk sendiri (PaiGardenPath/PlaneMode/NinjaRunner/dst, masing-masing cuma beberapa-puluh KB) yang di-fetch on-demand pas beneran dibuka. `firebase-*.js` (665KB, vendor SDK) masih 1 chunk gede tapi gak dipecah lebih lanjut -- itu library, bukan kode kita, dan auth check butuh dia dari awal jadi gak masuk akal di-defer.

**#12 Focus Round blank putih abis "Mulai Latihan"** — root cause: React Router SPA nav TIDAK reset scroll position (beda dari full page-load browser biasa) -- pindah dari `FocusRoundPicker` yang di-scroll jauh (nyentang topik) ke `FocusRoundQuiz` yang lebih pendek bikin browser nyangkut di `scrollY` lama, konten baru ke-render DI ATAS titik itu jadi putih kosong sampe discroll manual. Fix generic di App level (`ScrollToTop` component baru di `App.jsx`, `useLocation()`+`useEffect`, `window.scrollTo(0,0)` tiap `pathname` berubah) -- bukan cuma Focus Round, biar kelas bug yang sama gak muncul lagi di LAYAR MANAPUN.

**#13 Ninja Runner "box soal nutupin jawab soal"** — dibandingin markup al-idrisi persis: `.ninja-hint` itu SIBLING DI LUAR `#ninja-world` (index.html: `</div>` nutup ninja-world DULUAN, baru `<div class="ninja-hint">`). Di `NinjaRunner.jsx` versi kita, `.jk-nj-hint` KEPASANG JADI CHILD `.jk-nj-world`, normal-flow SETELAH `.jk-nj-run-lane` yang absolute -- karena semua sibling sebelumnya absolute (keluar flow), hint jadi "anak flow pertama" yang collapse ke PALING ATAS wrapper, numpuk sama card/soal yang emang sengaja `top:20px` absolute. Fix: pindahin div-nya (JSX doang, CSS `.jk-nj-hint` gak diubah) ke LUAR `.jk-nj-world`, row sendiri di bawah kotak game -- match posisi asli persis.

**#2 Math Race kurang banyak (AI lawan/isian/difficulty/pilihan mobil)** — beda dari yang lain, ini genuinely BELUM ada (`MathRace.jsx` dibangun dari nol React sebelumnya, `multipleazka` sumbernya gak ke-copy jadi beberapa fitur asli gak ke-port). Ke-4-nya di-port dari `multipleazka/script.js`:
  - **Difficulty** (`DIFFICULTY` easy/medium/hard) -- `generateQuickQuestion(grade, difficulty)` UDAH nerima param ini dari Drive/Plane Mode, MathRace CUMA hardcode `"medium"` -- tinggal tambahin UI segmented-control + pass state, gak perlu ubah generator.
  - **6 kendaraan** (`VEHICLES`: mobil🏎️/pesawat🛩️/kapal🚢/bus🚌/truk🚚/kereta🚂, `VEHICLE_EMOJI` map) -- picker grid di lobby (mirip pola picker Drive/Plane Mode), disimpen di RTDB `players/{role}/vehicle` buat multiplayer, dipake ganti avatar `RaceLane` yang tadinya emoji fix per-role (`p1`🚗/`p2`🚕/`p3`🚙).
  - **Mode jawab isian** (`answerMode`: `"choice"`|`"type"`) -- UI doang beda (input angka + tombol Jawab vs grid tombol MC), jawaban BENAR-nya tetep dari `question.correctLabel` yang sama (generator gak berubah), dibandingin abis di-normalize (`.replace(/[,.\s]/g,"")`) biar "7898" ketikan anak cocok sama "7,898" format `fmt()`.
  - **AI lawan (Solo Play doang)** — `DIFFICULTY[x].opponentSeconds` (easy 90/medium 55/hard 40, angka PERSIS al-idrisi) drive `setInterval` 100ms yang naikin progress AI KONSTAN (gak reaktif ke performa anak, filosofi sama kayak sumber asli) lewat ref (`aiProgressRef`, state cuma buat re-render bar) -- placement solo ditentuin di `finishRace()`: AI udah 100% duluan = anak finis ke-2, kalau enggak ke-1.
  - Diverifikasi end-to-end: solo + mode isian + kendaraan kereta -- AI lane progress jalan independen dari kecepatan jawab (dites lewat delay tool-call asli, BUKAN simulasi), 10 soal ke-jawab lewat input ketik (dibandingin ke `correctLabel` yang di-normalize), hasil akhir "Kamu finis ke-2!" (AI menang karena race berlangsung lebih lama dari `opponentSeconds` medium 55s) -- placement logic kebukti bener buat kasus MENANG dan KALAH.

**#5/#14 Plane Mode 2-player — TERNYATA UDAH ADA, tinggal kelar-in bug-nya** — pas mulai investigasi #2, ketemu `git status` udah punya perubahan BESAR belum di-commit di `PlaneMode.jsx` (629 baris diff) + 2 file baru (`planeP2P.js`, `planeBossArt.js`) dari sesi laen yang KEHABISAN limit session terus pindah ke sesi ini (dikonfirmasi user). Implementasi WebRTC P2P co-op yang CUKUP LENGKAP (lobby host/join UI, signaling numpang `mathrace_games/{code}/planeSignal`, guest render-doang host simulasi dunia, tiap pemain ngadilin kena-nya sendiri) -- bukan scaffolding setengah jadi.
  - **Bug ketemu**: `ReferenceError: now is not defined` NGE-CRASH game loop TIAP FRAME begitu status "playing" (termasuk SOLO mode, bukan cuma 2P) -- `const now = performance.now()` ke-declare DI DALEM blok `if (!g.paused)`, tapi `p2pTick(now)` di baris berikutnya (DI LUAR blok itu, jalan tiap frame apapun mode-nya) butuh `now` yang udah out-of-scope. Fix: `now` dipindah ke sebelum `if (!g.paused)`, dihitung SEKALI di awal `frame()`.
  - **WebRTC gak bisa dites di browser-pane ini** (SAMA PERSIS limitation yang udah dicatet dari BrainBox punya) -- dikonfirmasi ULANG lewat 2 cara: (1) 2 tab beneran, host bikin room + guest join pake kode, dua-duanya timeout "Gagal nyambung" abis 12 detik; (2) loopback test 2 `RTCPeerConnection` DALEM 1 TAB SAMA (gak lewat network/Firebase sama sekali) tetep `connectionState:"new"` selama-lamanya. Poin (2) yang ngebuktiin ini BUKAN bug kode planeP2P.js -- WebRTC beneran gak jalan di sandbox ini titik, independen dari kode apapun. Yang BISA diverifikasi: build production clean, signaling Firebase (room create+join, room code, error state, cleanup) semua kebukti jalan bener, solo mode gak keregresi abis fix bug `now`.
  - **Kesimpulan**: kerjaan dilanjutin (bukan diulang dari nol) -- kodenya solid, cuma 1 bug scoping yang ketemu+difix, sisanya UDAH match pola `multipleazka` yang TERBUKTI jalan di 2 device fisik beda jaringan (klaim sumbernya). **Testing 2 device fisik BENERAN masih WAJIB sebelum dianggap kelar 100%** -- browser-pane manapun gak bisa jadi bukti WebRTC-nya jalan, cuma bisa mastiin kode-nya gak crash & signaling-nya bener.

**Verifikasi keseluruhan**: `npm run build` clean di setiap tahap (dijalanin berkali-kali sepanjang sesi ini, bukan cuma di akhir), semua fix di-cek visual/interaktif di browser-pane (screenshot + klik asli, bukan cuma baca kode), 2 kasus stale-console-log ketemu lagi (`ReferenceError: now is not defined` abis di-fix TETEP muncul di `read_console_messages` berkali-kali walau gameplay real via screenshot ngebuktiin kode udah jalan bener) -- pola yang sama kayak yang udah dicatet sebelumnya di sesi laen, sekarang makin kebukti robust: JANGAN percaya 1 console-error tunggal tanpa cross-check ke bukti visual/behavioral asli.

**Susulan sama hari (user tanya "apa yang belum")** — cross-check ulang ke 14 poin ketemu 2 yang KELEWAT di commit pertama:
- **"AI box" Math Race** — poin ini bukan "encouragement" (yang udah ada), tapi entry point CHAT Kiko persisten selama race, yang genuinely gak ada sama sekali. Al-idrisi punya `.game-bo` (fixed bottom-right, 40px, selalu ada selama+abis race) TERPISAH dari `ai-hint-card` (auto-explain soal terakhir yang salah di layar reward). Cuma bagian PERSISTEN-nya yang di-port (`KikoChatPanel` mode `"general"`, tombol bulat fixed bottom-right) -- versi auto-explain-miss SENGAJA di-skip karena Math Race gak punya state "1 soal terakhir yang salah buat direnungin" kayak TopicQuiz (jawaban salah langsung ganti soal berikutnya, gak ada jeda buat nampilin penjelasan).
- **Kiko di pesawat Bahasa Inggris "gak bisa dipencet"** — dites klik PRESISI via JS ternyata kodenya jalan (`onClick` bener nembak, chat kebuka) -- BUKAN bug logic. Root cause sebenarnya: badge Kiko-nya cuma tombol TERPISAH 18x18px nempel di ujung pesawat (jauh di bawah standar minimum touch-target ~44px), jadi susah kepencet jari beneran. `IpasQuestMap.jsx` udah punya pola yang BENER dari awal (`ShipSvg` PENUH jadi 1 button, Kiko cuma dekorasi non-interactive DI DALEMNYA) -- `MathTownMap.jsx` (truck) sama `BinggrisWorldMap.jsx` (pesawat) DUA-DUANYA kena masalah sama (badge kecil terpisah), disamain ke pola IPAS: SELURUH kendaraan (40x40) jadi 1 button, badge Kiko di dalemnya murni visual. `PpknVillageMap.jsx`/`PaiGardenPath.jsx` udah bener dari awal (Kiko SENDIRI jadi traveler, gak ada badge terpisah) -- dicek, gak kena masalah ini.
- Diverifikasi: klik langsung ke elemen visual pesawat/truck (bukan badge-nya doang) via `.closest('button')` + `.click()` di kedua map, chat kebuka bener; tombol Kiko Math Race muncul pas `mode==="racing"`/`"finished"`, klik beneran buka `KikoChatPanel`.

**Testing 2 device fisik (2026-08-10) — HASIL: SUKSES.** User konfirmasi langsung "sudah bisa dan connect" abis nyoba Plane Mode 2P di 2 HP fisik beda device (bukan cuma 1 WiFi/1 tab kayak testing browser-pane) -- WebRTC signaling+P2P connect beneran jalan di real network, nutup ketidakpastian yang udah dicatet berkali-kali sebelumnya (browser-pane gak bisa mbuktiin ini sama sekali).

**Plane Mode picker di-restrukturisasi jadi step-wizard (2026-08-10)** — user abis konfirmasi 2P jalan, langsung kasih feedback UX: "UX sulit dipahami, samain sama UX alidirisi... dari pilih pesawat dulu baru masuk ke pilihan solo/multiplier baru ke page create game atau join game. tiru aja UXnya plek plek". Sebelumnya SEMUA (ringkasan pesawat + tombol "Ganti Pesawat" + 3 tombol difficulty + section "Main Berdua" lengkap dgn host/join) numpuk di 1 layar `picker` doang -- `pickingSkin` cuma boolean toggle buat sub-layar ganti skin, bukan alur berjenjang.

Dicek referensi `mathville/script.js` (`playVehiclePicker` → `playPlaneModePicker` → `launchPlaneMode`/`launchPlane2P`) + `mathville/index.html` (`#drive-vehicle-overlay` 2 tombol, `#plane-mode-overlay` cuma "Solo"/"2 Players") -- al-idrisi Plane Mode-nya BENERAN cuma 2 step overlay berurutan, TANPA tahap difficulty sama sekali (difficulty picker cuma ada di Drive Mode/mobil di sana).

Fix di `PlaneMode.jsx`: `pickingSkin` (boolean) diganti `pickerStep` (`"vehicle"|"mode"|"difficulty"|"room"`, default `"vehicle"`), phase `picker` sekarang render 1 dari 4 layar penuh gantian:
1. **`vehicle`** — grid 5 skin pesawat (persis layar lama), tap skin = pilih + auto-lanjut ke step berikutnya (`pickPlaneSkin()` sekarang `setPickerStep("mode")`, bukan cuma nutup sub-layar).
2. **`mode`** — deskripsi game + tombol "🧑 Solo" / "👥 2 Pemain" (baru, gak ada sebelumnya sebagai layar terpisah) + tombol kecil "‹ Ganti Pesawat" buat balik ke step 1.
3. **`difficulty`** — 3 tombol Gampang/Sedang/Susah (persis logic lama, `startRun(d)` gak diubah), CUMA kejangkau lewat "Solo" di step 2 -- fitur ini gak ada equivalent di al-idrisi, sengaja DIPERTAHANKAN (bukan dihapus, user gak minta dihapus) tapi ditaro di jalur Solo doang karena 2P dari dulu emang udah hardcode `startRun("medium")` abis connect (gak diubah).
4. **`room`** — section "Main Berdua" (Bikin Room/Gabung/kode room/status connecting, SEMUA logic `hostRoom`/`joinRoom`/`cancelCoop`/`lobby` state PERSIS gak disentuh) sekarang jadi layar sendiri penuh, bukan section nempel di bawah difficulty picker lagi.

Tiap step (kecuali `vehicle`, entry point) punya tombol "‹ Kembali"/"‹ Ganti Pesawat" buat mundur -- gak ada di al-idrisi (yang cuma alur maju doang) tapi ditambahin biar gak ada jalan buntu kalau anak salah pencet, konsisten sama UX app lain di sini.

Diverifikasi: `npx vite build` clean, lalu urutan lengkap dites di browser via `.click()` langsung ke tiap tombol (bukan cuma baca kode) -- vehicle→pilih Inferno→step mode muncul (skin kesimpen, "Ganti Pesawat: Inferno")→Solo→step difficulty muncul→Kembali→balik ke mode→2 Pemain→step room muncul (Bikin Room/Gabung/Kembali) sebagai LAYAR SENDIRI (bukan section nempel lagi, screenshot dicek). Console errors yang muncul semua entry STALE yang udah dicatet sebelumnya (`now is not defined` yang udah difix, FirebaseError `p2ptest2` dari akun test lama) -- nol error baru dari perubahan ini.

**Room step (`room`) direvisi lagi sesudahnya, per screenshot langsung dari `playalidrisi.fun`** -- user kirim 2 screenshot layar "Fly Together" al-idrisi ASLI (bukan cuma deskripsi teks) + instruksi: (1) "page create game atau gabung, bikin 1 page aja" (2) "klik create game langsung muncul code atau back. Bila klik back balikkan ke landing page pemilihan pelajaran". Dicek ulang source-nya persis (`mathville/index.html` `#screen-plane2p`, `script.js` baris ~3284-3320): `#p2p-choose` (Create Game + divider "or" + input kode + Join Game + area error) itu MEMANG 1 blok nempel bareng dari awal -- BUKAN 2 tombol yang masing-masing buka sub-layar terpisah kayak versi kita sebelumnya (yang salah nembak dari asumsi sendiri, bukan dari source asli). Klik Create ATAU Join dua-duanya toggle ke `#p2p-waiting` (kode + status teks beda: "Share this code..." vs "Connecting to the other pilot…"). Satu-satunya tombol "Back" (`data-back="screen-landing"`) SELALU ada di paling bawah baik di state choose MAUPUN waiting, dan fungsinya EXIT TOTAL ke landing page hub -- bukan mundur satu step ke layar solo/2p sebelumnya.

Fix di `PlaneMode.jsx`'s `pickerStep === "room"` block:
- Gabung jadi 1 blok `!lobby`: tombol "Bikin Game" (primary) → divider "atau" → input kode 6 digit → tombol "Gabung Game" (disabled kalau belum 6 digit, gak ada lagi tombol "Gabung" terpisah yang buka sub-layar sebelum ini).
- `joinRoom()`'s catch path diubah dari `setLobby("joining")` (state lama yang sekarang gak eksis lagi di UI) jadi `setLobby(null)` -- balik ke blok choose gabungan di atas, PERSIS pola `p2pConnectFailed()` al-idrisi yang nyetel balik ke `#p2p-choose`.
- Tombol "‹ Kembali" SEKARANG SELALU nampil (bukan cuma pas `!lobby`) dan `onClick`-nya `navigate(\`/kelas/${grade}\`)` -- exit total ke halaman pilih pelajaran (`PickSubject.jsx`), BUKAN `setPickerStep("mode")` lagi. Cleanup koneksi P2P kalau di-klik pas lagi hosting/connecting numpang `useEffect` unmount yang UDAH ADA dari awal (`p2pRef.current?.close()` pas komponen unmount) -- navigasi keluar otomatis unmount `PlaneMode`, gak perlu manggil cleanup manual sebelum navigate.
- Fungsi `cancelCoop()` (tombol "Batal" yang sebelumnya ada di tiap sub-state) DIHAPUS TOTAL -- al-idrisi gak punya tombol cancel-tapi-tetap-di-layar-ini sama sekali, cuma "Back" yang exit penuh, jadi fungsi ini jadi dead code kalau dipertahankan.

Diverifikasi ulang: build clean, klik "Falcon"→"2 Pemain"→screenshot dibandingin visual ke screenshot referensi user (layout Bikin Game/atau/input/Gabung Game/Kembali cocok), klik "Bikin Game"→kode muncul+"Kembali" tetep ada (cocok sama screenshot ke-2 user), klik "Kembali" dicek `location.pathname` jadi persis `/kelas/{grade}` (bukan cuma balik ke step sebelumnya).

**Parent Portal (`ParentPortal.jsx`) di-redesign total, per keluhan eksplisit user: "vibes jagoan kelas kayak murahan banget"** — user kirim 2 screenshot berdampingan: punya kita (`TopicPicker` versi lama, list checkbox polos 1 kolom, background putih datar) vs punya `playalidrisi.fun/parents` (grid 2 kolom, badge emoji bulat, label group berwarna, kotak count dashed border + pill). Instruksi: "samain aja dengan /parents pada playalidrisi. samain aja beda bahasa. untuk design dan ornamen juga."

Dibaca source asli persis (`al-idrisi-games/parents/index.html`+`style.css`+`script.js`) buat mastiin bukan re-interpretasi dari screenshot doang -- struktur `.p-section` (gradient putih→lavender tipis, judul ungu `#6b3f9b`), `.p-topic-list` (grid 2 kolom, `.p-group-label{grid-column:1/-1}` nempel di flow yang sama), `.p-topic-item` (pill ber-tint per kategori, badge emoji bulat putih shadow tipis, ring `inset 0 0 0 2px` pas kepilih), `.p-picked` (border dashed lavender + pill list topik kepilih), `.p-weak-game` (badge amber kecil), `.p-xp-icon`/`.p-xp-fill` (kotak ikon + bar berwarna per game) semua di-port 1:1 ke inline style React.

**Adaptasi yang gak bisa 1:1** (al-idrisi 3 kategori tetap Math/Language/Science, jagoan-kelas 6 subject x per-kelas):
- Warna per grup **numpang token `--product-{accent}` yang UDAH ADA** dari `GameCard.jsx`/`content.js`'s `SUBJECTS` (bukan bikin palet baru) — ditambah `export const ACCENT_BY_SUBJECT` baru di `content.js` (lookup `subjectId → accent`) biar `TopicPicker.jsx` & `ParentPortal.jsx` gak duplikat mapping.
- Topik gak ada icon per-item custom kayak al-idrisi (yang hand-curated 19 topik doang) -- jagoan-kelas badge emoji-nya numpang `subjectEmoji` yang sama buat semua topik di grup itu (tetap kebaca sebagai "badge bulat" visual, cuma gak unik per-topik).
- Section background/judul TETEP 1 warna lavender konsisten di semua section (bukan ikut accent subject) -- persis prinsip al-idrisi ("Parent Portal reaches across every game, leans lavender", bukan warna game manapun).
- Tombol aksi (`Kirim Pesan`/`Simpan Fokus`) & tombol `Keluar` di-override style-nya (`LAVENDER_BTN` const, numpang `Button`'s existing `style` prop override) ke lavender, ganti dari hijau/ghost generik sebelumnya -- match `.p-save-btn`/`.p-signout-btn` al-idrisi.

Diverifikasi end-to-end di browser (bukan cuma baca kode): daftar akun test baru (`QaParentTest`/PIN 1234) lewat `/masuk`, sign-in ke `/parents` pake kredensial sama, screenshot dibandingin ke referensi user -- grid 2 kolom + dashed picked-box + section gradient semua match. Toggle 2 checkbox via `.click()`, cek pill "2 / 8 dipilih" + 2 pill warna muncul di kotak dashed, ring highlight nongol di item yang kepilih. Nol console error. Akun test dihapus lagi abis verifikasi (Firestore REST `DELETE` ke `players/qaparenttest`, respons `{}` = sukses) -- gak ninggalin data test di Firestore production.

**Blob/animasi/ilustrasi -- DICEK, SENGAJA BEDA dari BrainBox, bukan gap.** User nanya eksplisit "udah sama termasuk warna blob, animasi dan ilustrasi?" abis redesign di atas. Dicek langsung `grep` ke `al-idrisi-games/parents/index.html`+`script.js` buat `sc-blob`/`sc-star`/`sc-dot`/`sc-balloon` -- NOL hasil. Halaman `/parents` BrainBox asli ternyata **PLAIN, gak ada blob/animasi/ilustrasi sama sekali** (blob cuma ada di landing page hub mereka, gak di-reuse ke Parent Portal). Punya kita (`ParentPortal.jsx`) justru UDAH punya blob+bintang+animasi bob dari `PageDecor` (`<PageDecor seed="parent-signin"/>` & `seed="parent-portal"`) -- warisan aturan standing lebih lama ("semua page harus ada blob, ilustrasi, ornamen, jangan polos", lihat komentar di `PageDecor.jsx`). Ditawarin ke user: biarin blob kita (lebih rame dari referensi) VS copot biar plek-plek plain kayak BrainBox -- **user pilih BIARIN** (opsi 1). Jadi status akhir: section/warna/layout match BrainBox, TAPI ornamen sengaja beda (lebih banyak di kita) -- JANGAN dianggap bug/gap kalau ada yang nanya lagi nanti, ini keputusan sadar. Ilustrasi/maskot sendiri nol di kedua app buat halaman ini (gak ada mismatch).

**Commit `4125ba6` (Plane Mode picker + Parent Portal redesign) di-push + deploy production (`vercel --prod --yes`, alias `jagoankelas.fun`)**, diverifikasi curl 200.

**Watermark ikon + entrance animation di card `PickSubject.jsx` (2026-08-11)** -- user kirim video promo AI-generated ("Pomelli ad", ekstrak frame pake `ffmpeg -vf fps=4` buat liat isinya beneran, bukan nebak dari nama file) yang nunjukin ikon translucent morphing + maskot hopping + confetti ikon random di card pelajaran, minta ditiru vibe-nya buat halaman pilih-pelajaran. Setelah diinvestigasi: (1) maskot "jalan-jalan di halaman" UDAH ADA (`RoamingCarDino`, malah lebih canggih -- physics chase, bukan cuma hop statis), (2) confetti ikon random di video gak nyambung ke subjeknya (PAI dapet ikon gembok) jadi SENGAJA di-skip (resiko balik ke keluhan "murahan" yang baru dibenerin), (3) yang genuinely kepake: **watermark ikon translucent per card**.

Implementasi (`GameCard.jsx` + `PickSubject.jsx`):
- **Watermark**: reuse ikon subject yang UDAH ADA (`SubjectIcons.jsx`, bukan bikin ikon/blob baru) di-render gede (92px) di ruang kosong kanan card, `filter: brightness(0) invert(1)` (jadi silhouette putih rata -- pola YANG SAMA kayak "highlight glossy putih transparan" yang emang udah dipake DI DALEM tiap SubjectIcons SVG sendiri, jadi konsisten sama bahasa visual yang udah ada) + opacity 0.16. Animasi bob **6.5 detik** infinite loop (nyamain timing `PageDecor`'s `jkPageDecorBob`), gak pernah "diem" total tapi amplitudonya kecil (±6px) jadi gak ganggu baca teks card.
- **Entrance stagger**: tiap card fade+geser-naik pas halaman dibuka, **0.35 detik** durasi, **60ms** stagger antar-card (9 card total: Math Race, 6 subject, Fokus, Ninja -- selesai semua dalam ~0.83 detik), abis itu diem total (one-time per page-load/navigasi, `animation-fill-mode:backwards` biar gak kedip state akhir sebelum delay-nya jalan).
- `GameCard.jsx` dapet 2 prop baru opsional: `watermark` (node ikon) & `entranceDelay` (ms) -- kalau gak dikasih, card render PERSIS kayak sebelumnya (dipake juga di komponen lain lewat props default gak berubah).
- Diverifikasi lewat `getComputedStyle()` di browser (bukan cuma screenshot, karena entrance animation 0.35s kelewat cepet buat ke-capture manual): `animationName`/`animationDuration`/`animationDelay` di semua 3 card pertama match exact (`0s`/`0.06s`/`0.12s`, duration `0.35s`), watermark `opacity:0.16`+`filter:brightness(0) invert(1)`+`animationName:jkCardWatermarkBob`+duration `6.5s` kebukti nempel di card Math Race SAMPE Ninja (card terakhir). Klik card (IPAS) masih navigate bener ke `/kelas/1/ipas` -- `position:relative`+`overflow:hidden` baru gak ngerusak `onClick`. Nol console error. Build production clean.
