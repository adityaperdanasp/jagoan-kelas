# Jagoan Kelas — Project Notes

Aplikasi edukasi buat anak SD Indonesia kelas 1-6, ngikutin Kurikulum Merdeka. Tagline: "Belajar, tapi Gak Ngebosenin". Dibuat buat kelas anaknya Adit (sama konteksnya kayak BrainBox), tapi **infrastruktur 100% terpisah** dari BrainBox (`~/Documents/al-idrisi-games`) — repo GitHub beda, Firebase project beda, Vercel project beda, domain beda. BrainBox cuma dipakai sebagai REFERENSI POLA KODE (baca-only, gak pernah diedit dari sesi kerja project ini).

## Infra

- **Repo**: [github.com/adityaperdanasp/jagoan-kelas](https://github.com/adityaperdanasp/jagoan-kelas)
- **Hosting**: Vercel project `ellilo/jagoan-kelas` → [jagoan-kelas.vercel.app](https://jagoan-kelas.vercel.app), `vercel --prod --yes` buat deploy manual (belum ada custom domain)
- **Backend**: Firebase project `jagoan-kelas` (Firestore Database, region `asia-southeast2`/Jakarta, mode test). Config di `src/firebase.js` — `apiKey` itu public client identifier, aman di-commit (security lewat Firestore Rules, bukan kerahasiaan key), sama kayak pola `firebase.js` BrainBox.
- **Stack**: Vite + React + React Router (SPA, bukan Next.js). BrainBox pakai vanilla HTML/CSS/JS; project ini sengaja pakai React karena butuh handle 42 kombinasi kelas×pelajaran dengan komponen reusable, bukan reuse langsung kode BrainBox.
- **Auth ke GitHub**: SSH key (`~/.ssh/id_ed25519`) udah terhubung ke akun `adityaperdanasp`, dipakai buat push — bukan `gh auth login` (belum pernah login, `gh` CLI ke-install tapi nganggur).
- **Deploy history penting**: sesi kerja 2026-08-04 s/d 2026-08-05 numpuk 30+ file perubahan (konten 3 subject baru, semua fitur di CLAUDE.md ini) TANPA di-commit sama sekali sampai akhirnya di-commit+push+`vercel --prod --yes` sekaligus di penghujung 2026-08-05 — kalau nemu commit gede "Add Bahasa Inggris/Indonesia/PAI content, Drive/Plane advanced features, BGM, encourage messages, AI Tutor", itu sebabnya. **Pelajaran buat serverless function baru**: `api/generate-hint.js` sempet gagal deploy (`FUNCTION_INVOCATION_FAILED`) gara-gara pakai `module.exports` (CommonJS) padahal `package.json` app ini `"type":"module"` — WAJIB pakai `export default` (ESM) buat file apapun di `api/`.

## Desain — WAJIB ikutin wireframe persis, jangan improvisasi

Sumber: `~/Documents/jagoan-kelas/design_handoff_kids_game_wireframe/` (`README.md` + `Kids Game Wireframe.dc.html` + `_ds/` design tokens). Ini SPEC, bukan inspirasi.

- Token warna/font/radius/shadow di `src/styles/tokens.css` — nilai persis dari `_ds/tokens/*.css`, **jangan approximate**.
- Komponen di `src/components/ds/` (Avatar, Badge, Chip, Button, GameCard, ProgressXP, Input, SegmentedToggle) — di-port **1:1** dari source JSX yang ada di `_ds_bundle.js` (bundle itu nyimpen source component asli BrainBox design system, bukan cuma compiled CSS).
- 4 layar wireframe: Landing (`Landing.jsx`) → Pilih Kelas (`PickGrade.jsx`, path 6 node winding) → Pilih Pelajaran (`PickSubject.jsx`, 7→6 card) → Detail Pelajaran (`SubjectDetail.jsx`, list topik + status).
- **Deviasi yang UDAH disepakati sama user** (jangan revert tanpa nanya): wireframe aslinya nampilin **7 mata pelajaran** (Matematika/IPA/IPS/PPKn/PAI/B.Indo/B.Inggris terpisah), tapi diubah jadi **6** — IPA+IPS digabung jadi satu card "IPAS" (`src/data/content.js`) karena Kurikulum Merdeka asli emang udah merger keduanya jadi satu buku sejak kelas 3 (kelas 1-2 tematik). Kalau nanti ada permintaan "kok cuma 6 pelajaran" — ini bukan bug, ini keputusan sadar.

## Auth — pola disamain persis sama BrainBox

- **Sign Up / Sign In** pakai nama + PIN 4 digit (`src/screens/Auth.jsx`, `SegmentedToggle`), BUKAN email/password formal — sama filosofi kayak BrainBox (`al-idrisi-games/index.html` layar `#sc-screen-select`).
- Skema data: `players/{nameKey}` → `{name, pin, xp, createdAt}` (`src/data/authService.js`). `nameKey` = nama di-lowercase + non-alfanumerik jadi `-`, identik sama fungsi `nameKeyOf`/sanitasi BrainBox punya `testerAccounts/{nameKey}`.
  - Sign Up: tolak kalau `nameKey` udah ada dokumennya, langsung login abis create.
  - Sign In: cocokin `pin` plaintext ke yang tersimpan. **Plaintext PIN itu SENGAJA** (bukan lupa hash) — sama persis pola BrainBox, karena threat model-nya rendah (device fisik anak SD di kelas, bukan aplikasi publik dengan data sensitif).
- Avatar warna: `hashColor(seed ?? name)` di `Avatar.jsx` — fungsi hash-nya **disalin persis** dari BrainBox punya (PALETTE array yang sama, algoritma hash yang sama), bukan reimplementasi.
- Session: `localStorage` key `jagoan_kelas_player` nyimpen `{id, name, pin, xp, createdAt}` (`src/data/PlayerContext.jsx`) — analog `player.js` BrainBox (`aig_player`), tapi lebih simpel (BrainBox punya konsep `role: parent/child` yang gak dipakai di sini, gak ada kebutuhan itu).
- Proteksi rute: `RequireAuth` wrapper di `App.jsx` — semua rute selain `/masuk` redirect ke situ kalau `player` null.

### Beda sengaja dari BrainBox (dicatat biar transparan, bukan silent divergence)
- **Firestore, bukan Realtime Database.** BrainBox pakai RTDB (`al-idrisi-games` project) terutama karena butuh sinkronisasi real-time buat multiplayer (Math Race, DinoRace race state). Jagoan Kelas belum ada kebutuhan multiplayer real-time — Firestore lebih pas buat model data per-dokumen (progress per pelajaran) dan udah kepasang duluan pas setup awal. **DinoRace beneran (2-player) belum di-porting justru karena butuh RTDB** buat live pairing (lihat bagian Games di bawah) — kalau itu dikerjain, project Firebase yang sama bisa ditambahin RTDB juga (satu project bisa punya keduanya).
- Skema progress (`players/{id}.progress.{subject}.{grade}.{babKey}`) **flat nested-map di 1 dokumen**, BUKAN subcollection kayak BrainBox punya `players/{id}/badges/{gameId}/...`/`topicStats/{gameId}/{topicKey}/...` (2 path terpisah). Digabung jadi satu struktur di sini karena scope-nya lebih kecil per topik (status+stars+xp+correct+wrong sekaligus, bukan 2 concern terpisah) — lihat bagian "Progress Tracking + Focus Round" di bawah buat detail lengkap.

## Konten soal (content-pipeline)

Folder terpisah `~/Documents/jagoan-kelas/content-pipeline/` (di luar `app/`, cuma script Python + output JSON, gak ikut ke-deploy). Restrukturisasi bank soal mentah (`soal_matematika_1000.json`, `soal_ipas_1_6.json`) jadi format per-bab sesuai struktur Kurikulum Merdeka resmi (`input/struktur_bab_kemdikbud.md`, hasil riset web + verifikasi ulang).

- `bab_struktur.py` — struktur bab resmi per kelas per mapel (hardcoded dari riset, termasuk 8-bab tematik IPAS kelas 1 dari modul ajar Kemdikbud, BUKAN Buku Siswa resmi karena emang gak ada buku IPAS buat kelas 1-2).
- `classify_matematika.py` / `classify_ipas.py` — mapping soal existing ke bab pakai kombinasi topic-name langsung + keyword-scoring (buat topik generik kayak "Pengetahuan Alam & Sosial Tambahan"). Soal yang gak match apapun masuk `belum_terpetakan`, TIDAK dipaksa ke bab yang salah.
- `generators_matematika.py` — generator soal prosedural (gaya MathVille `generators.js`, jawaban dihitung via kode) buat ngisi bab yang kosong/tipis. Target 15 soal/bab.
- `draft_ipas.py` — soal IPAS ditulis manual (BUKAN generate acak, karena harus faktual akurat) buat 14 bab yang kosong, ditandai `"sumber": "draft_ai_perlu_review"` — **WAJIB direview manusia sebelum dianggap final**, khususnya sains (cahaya/bunyi, tubuh manusia, bumi) dan sejarah/geografi. Kelas 6 Bab 8 "Proyek Akhir IPAS" SENGAJA gak dibikinin soal (itu bab proyek, bukan materi kuis).
- Output di `content-pipeline/output/{matematika,ipas}/kelas_{1-6}.json`, di-copy manual ke `app/src/data/content/{subject}/kelas_{n}.json` (bukan symlink/build-step — kalau content-pipeline di-rerun, HARUS re-copy manual ke app).
- **PPKn**: soal per-bab (`soal_ppkn_per_bab.json`) dikasih user langsung udah dalam format final, di-split jadi per-kelas via script inline (bukan file terpisah, one-off) ke `app/src/data/content/ppkn/kelas_{n}.json`.
- **PAI**: konten paling sensitif, WAJIB direview ustadz/guru agama sebelum di-generate ulang/ditambah — udah SELESAI & AKTIF (lihat bagian "PAI" di bawah), dicatat di sini biar gak ada yang asal nge-generate soal baru tanpa research yang sama ketatnya. IPS udah gabung ke IPAS (bukan gap).

### Mismatch kurikulum — SUDAH diselesaikan (2026-08-04)
Semua 421 soal `belum_terpetakan` (341 Matematika + 80 IPAS) udah direklasifikasi berdasarkan riset ulang struktur bab resmi Kemdikbud (web search, cross-check beberapa sumber independen). Keputusan detail + alasan ada di docstring `content-pipeline/reclassify_cross_grade.py` (Matematika) dan `content-pipeline/reclassify_ipas.py` (IPAS) — **baca itu sebelum re-run pipeline dari awal**, karena `restructure_*.py` bakal reset ke state belum-direklasifikasi (reclassify script harus dijalanin lagi setelahnya, urutannya: restructure → fill → reclassify).

### Bahasa Inggris & Bahasa Indonesia — konten baru (2026-08-04)
Beda dari Matematika/IPAS: gak ada bank soal existing buat direstrukturisasi, jadi dibangun langsung dari nol (struktur bab + soal, keduanya ORIGINAL, sumber `"draft_ai_perlu_review"` — **WAJIB direview guru** sebelum dianggap final, terutama akurasi kosakata Inggris & tata bahasa Indonesia).

- **`INGGRIS_BAB`/`BINDO_BAB`** di `bab_struktur.py` — struktur bab DIRANCANG SENDIRI (bukan port literal buku resmi), lihat komentar panjang di atas tiap dict buat alasannya: B.Inggris statusnya mulok pilihan Kurikulum Merdeka (gak ada 1 buku baku nasional), B.Indonesia strukturnya per-SKILL bukan per-cerita (app belum punya komponen buat nampilin bacaan panjang).
- **`draft_inggris.py`** — soal 2 arah (EN→ID, ID→EN) di-assemble programatis dari bank kosakata tematik per bab (`vocab_pair_questions()`), bukan generator prosedural kayak Matematika (kosakata tetep ditulis manual).
- **`draft_bindo.py`** — soal tata bahasa/kosakata Indonesia (jenis kalimat, sinonim-antonim, kata baku, jenis teks, dll), ditulis manual per bab.
- **`build_inggris.py`/`build_bindo.py`** — assemble langsung dari struktur+draft (gak ada tahap restructure/classify kayak Matematika/IPAS, karena emang gak ada bank buat diklasifikasi).
- Output di `content-pipeline/output/{inggris,bindo}/kelas_{1-6}.json`, **di-copy manual ke folder app yang NAMANYA BEDA**: `inggris` → `app/src/data/content/binggris/`, `bindo` → `app/src/data/content/bindo/` (harus cocok sama `SUBJECTS` id di `content.js` / `SUBJECTS_WITH_CONTENT` di `contentLoader.js`, JANGAN asal copy nama folder pipeline).
- 526 soal B.Inggris, 282 soal B.Indonesia. Diverifikasi jalan end-to-end di browser (register → pilih kelas 4 → buka kedua subject → jawab soal → feedback benar muncul).

Ringkasan keputusan kunci:
- Matematika: perkalian/pembagian ternyata materi resmi **kelas 3** (bukan kelas 2 — Kurikulum Merdeka nunda ke kelas 3). Keliling bangun datar → kelas 5. Volume bangun ruang (rumus kubus/balok) → kelas 6 bab "Kubus dan Balok". Perbandingan&skala → kelas 6 bab "Rasio". Statistika (modus) → kelas 5 bab "Data".
- **1 topik dibuang** (bukan dipindah): "Bilangan Bulat" kelas 6 (28 soal) ternyata cuma soal penjumlahan/pengurangan level kelas 1 yang salah label generik, gak ada nilai pedagogis di kelas 6 — diputuskan dibuang.
- IPAS: semua 80 soal dapet rumah (gak ada yang dibuang), kebanyakan soal generik "Pengetahuan Alam & Sosial (Tambahan)" yang gagal keyword-matching ternyata cuma butuh assignment manual per-soal (bukan per-topik) karena 1 topik generik nyampur beberapa tema beda kelas.
- Kelas 6 Matematika dikonfirmasi CUMA 4 bab resmi (Pecahan&Desimal/Rasio/Kubus&Balok/Peluang) via 2 sumber independen — bukan struktur yang salah/kurang lengkap.

## BGM + Encourage Message (2026-08-05)

**Musik latar per pelajaran** (`src/data/bgm.js`) — di-port dari pola BrainBox `mathville/bgm.js` APA ADANYA (baca komentar di file itu buat detail iOS Safari workarounds yang disalin, bukan preferensi gaya): volume lewat Web Audio `GainNode` (BUKAN `<audio>.volume` — iOS Safari ngabaikan itu total), unlock-on-gesture yang RETRY tiap gesture (AudioContext bisa nyangkut "suspended" di iOS walau `resume()` udah dipanggil), `switchTrack()` ganti `.src` di `<audio>` element yang SAMA dengan crossfade 400ms, no-op kalau src-nya sama (biar gak glitch restart).

- **File audio REUSE dari BrainBox** (`al-idrisi-games`), bukan bikin baru — di-copy manual ke `public/audio/bgm/` (matematika.mp3 dari mathville, ipas.mp3 dari azkauniverse, bindo.mp3/binggris.mp3 dari azkacraft menu/game, drive.mp3 dari multipleazka, plane.mp3 dari mathville plane-bgm, hub.mp3 buat landing/ppkn/pai). **Kalau BrainBox nambah/ganti track-nya, gak otomatis kesinkron ke sini** — harus di-copy ulang manual, sama kayak pola copy konten soal di `content-pipeline`.
- **`TRACK_BY_SUBJECT`** map subject id → path, dipake `SubjectDetail.jsx` (`useBgmTrack(TRACK_BY_SUBJECT[subject])`). PPKn & PAI numpang track yang sama kayak Drive/hub (gak ada track BrainBox yang tematik pas buat itu) — bukan bug, keputusan sadar karena keterbatasan aset yang di-reuse.
- **`useBgmTrack(src)`** — hook kecil, `useEffect` yang manggil `switchTrack()` tiap `src` berubah. Dipasang di `Landing.jsx` (hub), `SubjectDetail.jsx` (per-subject), `DriveMode.jsx`, `PlaneMode.jsx`. `PickGrade`/`PickSubject` gak perlu pasang sendiri — track hub dari Landing otomatis kebawa lewat (gak ada yang override sampai `SubjectDetail` mount).
- **`initBgmUnlock()`** dipanggil sekali di `App.jsx` (`useEffect` deps `[]`) — daftarin listener gesture (`pointerdown`/`touchend`/`click`/`keydown`) di `document`.
- **Tombol mute** (🔊/🔇) di header `Landing.jsx`, persisten `localStorage` (`jk_bgm_muted`) — BrainBox sendiri gak punya UI mute eksplisit, ini tambahan sadar karena konteks kelas (26 anak main bareng, guru mungkin butuh matiin).
- **Verifikasi**: `new Audio()` gak nempel ke DOM (kayak `new Image()`), jadi `document.querySelectorAll('audio')` gak nemuin apa-apa — dites via `window.__jkBgmDebug` (expose dev-only, `import.meta.env.DEV` guard, aman ada di kode produksi karena gak pernah ke-set di build). Diverifikasi genuinely: `paused:false`, `currentTime` maju beneran, `src` ganti bener dari hub.mp3 → matematika.mp3 pas masuk `/kelas/4/matematika`.

**Encourage message tanpa nama** (`src/data/encouragement.js`) — pool pesan semangat random (bukan AI-generated), dipilih berdasar accuracy tier (HIGH ≥80%/MID ≥50%/LOW di bawah itu), SENGAJA gak pakai nama anak (beda dari "Kirim Pesan" Parent Portal yang personal). Dipasang di layar hasil `TopicQuiz.jsx` dan `FocusRoundQuiz.jsx`, di-generate SEKALI pas `setResult()` (bukan tiap render) biar gak berubah-ubah pas user liat hasil.

## AI Tutor (2026-08-05) — AKTIF & VERIFIED DI PRODUCTION

Di-port dari BrainBox `api/generate-hint.js` (baca komentar di file asli buat kenapa desainnya gitu), disederhanain ke **single-shot** (BrainBox versi awal juga single-shot sebelum di-upgrade ke chat interaktif multi-turn — lihat CLAUDE.md BrainBox "Bo (maskot AI)" kalau nanti mau upgrade ke situ juga).

- **`api/generate-hint.js`** (Vercel serverless function, folder `api/` di root `app/` — otomatis ke-detect Vercel, gak perlu config tambahan di `vercel.json` karena filesystem routes menang duluan sebelum SPA rewrite catch-all). Model `claude-haiku-4-5` (sama kayak BrainBox, murah+cepat). System prompt **Bahasa Indonesia** (beda penting dari BrainBox yang Inggris — konten Jagoan Kelas semua buat anak SD Indonesia).
- **Tombol "🤖 Jelasin Lagi"** di `QuizRunner.jsx`, muncul CUMA pas jawaban salah, ON-DEMAND (bukan otomatis tiap salah) — biar biaya API kepake pas beneran diminta anak. Gagal (network/API key belum ke-set) = tombol ilang diem-diem lewat `hintFailed` state, kuis TETEP jalan normal — sama filosofi "never blocks the game" kayak BrainBox.
- **Context yang dikirim**: `subjectName`/`gradeLabel`/`topicTitle` di-pass dari `TopicQuiz.jsx` (subject asli + `Kelas N` + judul bab) dan `FocusRoundQuiz.jsx` (generic "Fokus Latihan"/"Topik campuran" karena soalnya kecampur macem-macem topik). Drive Mode & Plane Mode **SENGAJA gak dikasih tombol ini** — quiz kilat di situ bukan tempatnya hint mendalam, sama persis alasan BrainBox ("shown on post-activity screen, never live mid-question").
- **⚠️ Bug ketemu & difix pas aktivasi (2026-08-05)**: `api/generate-hint.js` awalnya ditulis pakai `module.exports = async (req, res) => {...}` (CommonJS) — tapi `package.json` app ini punya `"type": "module"`, jadi Vercel Node runtime treat semua `.js` di `api/` sebagai ESM, dan `module.exports` throw `ReferenceError` pas runtime (`FUNCTION_INVOCATION_FAILED`). Fix: ganti ke `export default async function handler(req, res) {...}`. **Kalau nambah endpoint serverless baru di `api/` ke depannya, WAJIB pakai ESM `export default`, jangan `module.exports`.**
- **ANTHROPIC_API_KEY** udah di-set user sendiri di Vercel project env vars (2026-08-05) — biaya per-pemakaian ditanggung akun Anthropic user, sama kayak pola BrainBox.
- **Verified end-to-end di PRODUCTION** (bukan cuma dev): `curl` langsung ke `https://jagoan-kelas.vercel.app/api/generate-hint` dengan soal matematika asli → respons AI Bahasa Indonesia yang bener (jelasin proses pinjam-meminjam pengurangan pakai angka soal aslinya) → dites lagi lewat UI asli (browser, akun test, jawab salah, klik "🤖 Jelasin Lagi") → kartu hint muncul dengan penjelasan yang sama kualitasnya. Akun test dihapus abis verifikasi.

## Games (Drive Mode / Plane Mode / DinoRace easter egg)

Folder `src/games/` — `drive/`, `plane/`, `dinorace/`, `shared/`. Drive & Plane Mode sekarang punya **hampir semua fitur lanjutan BrainBox** (di-port 2026-08-05, baca kode aslinya di `al-idrisi-games/mathville/script.js`, cari komentar "Drive Mode"/"PLANE MODE") — Drive: nitro ✅, water gun ✅, vehicle skin ✅; Plane: power-up ✅, boss ✅, respawn gauntlet ✅, endless+ramp ✅, high score ✅. Cuma `dinorace/` (2-player racing beneran) yang masih belum sama sekali (scope beda, butuh Firebase RTDB). Pola porting yang dipake: baca constant/logic asli di BrainBox, adaptasi value-nya APA ADANYA ke React (ref buat data per-frame, state React cuma buat hal yang jarang berubah) — jangan re-desain dari nol.

- **`shared/quickQuestion.js`** — generator soal kilat grade-aware (beda dari `generators_matematika.py` yang per-bab kurikulum). Rentang angka per kelas 1-6, dipakai Drive Mode & Plane Mode dua-duanya. BrainBox punya bank soal FIXED grade-4 (dari MathVille) karena mathville cuma 1 kelas; kita generate langsung per grade karena app-nya 6 kelas.
- **`shared/useJoystick.js` + `Joystick.jsx`** — analog stick, pola sama kayak BrainBox (drag dari tengah, vector -1..1). Posisi visual nub lewat React state, tapi `vecRef` yang dibaca game loop tiap frame TIDAK lewat re-render (biar gak lag) — persis alasan BrainBox manipulasi DOM langsung.
- **`drive/DriveMode.jsx`** — mobil dodge-obstacle + dino chase + quiz kilat pas nabrak obstacle. Entry: tombol "🚗 Drive" di `SubjectDetail.jsx` (khusus subject Matematika), route `/kelas/:grade/matematika/drive`.
  - Ada: 1 dino ngejar (bukan 2 di Hard), obstacle dodge, quiz-on-hit, lives/bite-immunity, difficulty picker, win/lose, **nitro boost + water gun + vehicle skin picker (2026-08-05, lihat di bawah)**.
  - **Nitro boost** — di-port dari BrainBox (`DRIVE_NITRO_*` constants di `mathville/script.js`), nilai SAMA PERSIS: hold buat 1.6x speed (`NITRO_MULT`), abis dalam 2.5 detik boost terus-terusan, isi ulang penuh 6 detik pas gak dipake. Tombol ⚡ + fuel bar **dipindah ke header atas** (2026-08-05, sebelumnya kanan-bawah) buat ngasih tempat aim-joystick water gun. Frame-based drain/regen (asumsi ~60fps, bukan real delta-time) — sama simplifikasi kayak `CAR_SPEED`/`DINO_SPEED` yang emang udah gitu dari awal di file ini. State-nya nempel di `gameRef.current.nitroFuel` (bukan React state, biar gak re-render tiap frame) + `boostingRef` buat status tombol lagi ditekan/enggak.
  - **Water gun (2026-08-05)** — di-port dari BrainBox (`DRIVE_WATER_*`), joystick kanan-bawah terpisah (`aimJoystick`, komponen `Joystick` yang sama di-reuse) buat aim 360°, di luar deadzone (`WATER_AIM_DEADZONE=0.3`) = nembak jet 💦. Range 128px, cone ±18° (`WATER_CONE_RAD`), tangki abis 1.5 detik nonstop lalu cooldown 1.5 detik. Kena dino TOTAL 3 detik (akumulatif lintas semburan, `dinoWetFrames`) trigger slow 2 detik -30% speed (`dinoSlowFrames`/`WATER_SLOW_MULT`) yang di-apply ke `DINO_SPEED` bareng `dinoDiffMult`. Visual jet (`waterJetRef`) di-rotate ke arah aim tiap frame, sama pola ref-DOM-langsung kayak mobil/dino.
  - **Vehicle skin picker (2026-08-05)** — beda dari BrainBox yang custom SVG per skin, di sini cuma swap emoji (`VEHICLE_SKINS`, 5 pilihan: Blaze🚗/Comet🚕/Turbo🚙/Sunburst🚓/Nova🏎️) karena app ini emoji-based dari awal, bukan custom art. Persisten `localStorage` (`jk_drive_vehicle_skin`), murni kosmetik (gak ngaruh ke gameplay/collision).
  - **Verifikasi teknis penting**: `requestAnimationFrame` **gak jalan sama sekali** di browser-pane pas tab background/gak fokus (limitation tooling yang sama persis kayak yang dicatat BrainBox soal Plane Mode testing) — dicoba paksa `document.hidden=false`+`visibilitychange` event, tetep gak ngaruh. Semua fitur di atas diverifikasi lewat REF ASLI (`gameRef`/`boostingRef`/vecRef joystick) langsung dari React fiber component (`__reactFiber$` key di DOM node), BUKAN simulasi terpisah:
    - Nitro: konfirmasi tombol ⚡ beneran nyetel `boostingRef.current=true`, formula drain/regen dijalanin manual di atas OBJEK REF ASLI 200+400 kali (simulasi ~3.3s boost + ~6.7s idle) — hasil abis ke 0 lalu balik penuh ke 100 persis target.
    - Water gun: set `aimJoystick.vecRef` langsung ke arah dino (dino sengaja didekatin ke mobil dulu biar dalam range 128px), trigger beberapa real frame tick lewat interaksi asli (drag) — `waterStreamFrames` dan `dinoWetFrames` naik BARENG persis 5=5, membuktikan deteksi range+cone+akumulasi kerja bareng logic firing yang sama.
    - Vehicle skin: dipilih via UI asli (klik "Nova"), persist ke `localStorage`, dan ke-apply beneran ke sprite in-game (bukan cuma di picker) — diverifikasi via screenshot.
  - **Masih BELUM ada** (fitur BrainBox lain yang sengaja di-skip): city-markers-ke-chapter, dino obstacle-avoidance steering (dino kita jalan lurus ke arah mobil, gak belok-belok ngindarin rintangan kayak BrainBox `dinoSteerAngle`). Kalau mau lanjutin salah satu ini, pola portingnya sama kayak nitro/water-gun: baca constant/logic asli di `al-idrisi-games/mathville/script.js` (cari komentar section-nya), adaptasi ke React (ref buat data per-frame, state React cuma buat hal yang jarang berubah).
- **`plane/PlaneMode.jsx`** — shmup vertikal, auto-fire, musuh turun+nembak balik, quiz berkala = bomb semua musuh. Entry: tombol "✈️ Plane" di sebelah Drive, route `/kelas/:grade/matematika/plane`.
  - Ada: ship+joystick, auto-fire, 1 tipe musuh (+variasi sine drift), musuh nembak balik (aimed, ada spread error), lives+invuln flash, ledakan visual, quiz berkala = bomb, **power-up, boss fight, respawn gauntlet, endless mode + ramp, high score persisten (2026-08-05, lihat di bawah)**.
  - **Fase lanjutan (2026-08-05)** — di-port dari BrainBox `mathville/script.js` (Fase 5/6 + v2), nilai diadaptasi (bukan endless BrainBox yang murni, kita batasi skala biar cocok sama app 6-kelas):
    - **Power-up**: musuh biasa mati → 22% (`POWERUP_DROP_CHANCE`) drop ⚡ rapid-fire (8 detik, 2x fire rate) atau 🛡️ shield (6 detik, nyerap 1 hit), jatuh pelan, dipungut nabrak badan pesawat.
    - **Boss fight**: skor nyampe threshold (mulai 15, +15 tiap boss berikutnya) → musuh biasa berhenti spawn, boss 🐉 muncul (HP 8, gerak kiri-kanan mantul, nembak lebih sering). Jawaban benar = bom musuh **+ damage 3 ke boss** (`BOSS_QUESTION_DAMAGE`), bukan cuma bom musuh biasa. Boss kalah = +3 skor bonus, density musuh naik 1.2x (compounding), lanjut main (BUKAN menang-lalu-selesai).
    - **Respawn gauntlet**: nyawa habis TIDAK langsung game over — kalau `respawnsUsed < 3`, buka overlay "💔 Kesempatan Terakhir!" (beda dari overlay soal biasa), jawab 3 soal benar BERTURUT-TURUT (salah cuma reroll, progress gak turun) buat balik main full nyawa. Abis 3x kesempatan respawn, baru beneran game over.
    - **Endless + high score**: gak ada lagi "skor target buat menang" — main terus sampe nyawa+respawn abis. High score per-kelas kesimpen `localStorage` (`jk_plane_highscore_kelas{N}`, BUKAN Firestore — ini rekor lokal per device, beda dari XP yang kesimpen ke akun).
    - **2 bug KETEMU & DIFIX pas ngerjain ini** (baik buat diinget kalau ubah game loop lain yang mirip):
      1. Awalnya `useEffect` game loop punya `score` di dependency array (buat baca skor terbaru pas cek boss threshold) — ternyata itu bikin loop RESTART (+ `cleanupWorld()` ke-panggil, world ke-cleanup) tiap kali skor berubah (tiap musuh mati!). Fix: skor "beneran" dipindah ke `gRef.current.score` (dibaca sinkron di frame loop), React state `score` cuma buat display, `bumpScore()` helper update dua-duanya, deps balik ke `[phase]` doang.
      2. Respawn-gauntlet awalnya nulis logic keberhasilan (`setRespawning(false)`, `setLives(...)`, dst) DI DALAM parameter functional-updater `setRespawnProgress(p => {...})` — StrictMode dev me-double-invoke updater function kayak gitu, jadi side-effect-nya bisa kepanggil 2x. Fix: progress asli dipindah ke `respawnProgressRef` (ref biasa), functional-updater gak dipake lagi buat percabangan logic.
    - **Verifikasi**: sama kayak nitro, `requestAnimationFrame` gak reliable di tab background browser-pane. Kali ini malah lebih beruntung — abis beberapa tool-call (screenshot/interaksi/tunggu) `frame()` sempat jalan beneran cukup banyak kali, jadi bisa diverifikasi END-TO-END LEWAT GAMEPLAY ASLI (bukan simulasi manual kayak nitro): set `g.score=15` via ref asli → boss beneran spawn (`g.boss.hp=8`) → jawab soal benar via tombol asli → `hp` 8→5, skor 15→17 (persis `+2`) → set `hp=2` → jawab benar lagi → boss KALAH (`boss:null`), skor 17→22 (`+2+3` bonus persis), `bossesDefeated:1`, `bossThreshold` 15→30, `enemyDensity` 1→1.2 — SEMUA angka cocok pas sama formula. Respawn gauntlet juga diverifikasi lewat 3x klik jawaban benar asli: `respawnProgressRef` 0→1→2→3, abis itu `respawnsUsed:1`, `paused:false`, nyawa balik `❤️❤️❤️`. Power-up pickup CUMA direview kode (gak sempet ke-tick pas di-test, tapi pattern-nya identik sama bullet-vs-boss collision yang udah kebukti jalan).
  - Elemen bullet/musuh/power-up dimanipulasi langsung ke DOM (`document.createElement` + `style.left/top` tiap frame, BUKAN React state) — SAMA PERSIS pola performa BrainBox, krusial buat FPS yang smooth pas banyak bullet di layar.
- **`dinorace/`** — CUMA unlock mechanism yang jadi, game beneran BELUM. `useSecretTap.js`: tap 6 kuadran (bagi hero image Landing jadi TL/TR/BL/BR) sesuai urutan rahasia `TL,TR,TL,TR,BL,BR` dalam window 2.5 detik/tap, salah urutan atau kelamaan diem = reset progress. Berhasil → navigate ke `/rahasia/dinorace` (`DinoRaceUnlock.jsx`), yang JUJUR nampilin "segera hadir", bukan pura-pura ada game-nya. **Urutan tap sequence-nya SENGAJA gak didokumentasiin di UI/README publik manapun** (biar tetep rahasia) — cuma ada di kode ini.
  - DinoRace ASLI (2-player racing, sumber di `~/Documents/dinorace` atau `al-idrisi-games/dinorace/`) belum di-porting sama sekali. Butuh: Firebase RTDB (bukan Firestore) buat room pairing real-time, UI race track + joystick kontrol mobil, sistem 2-player sync. Scope besar terpisah, belum mulai dikerjain.

## Parent Portal (`/parents`)

Pola disamain **persis** sama BrainBox `parents/index.html`/`script.js` (baca dulu itu kalau mau ubah sesuatu di sini). Poin kunci yang WAJIB dipertahankan kalau diubah:

- **Auth pakai PIN ANAK**, bukan PIN guru/dashboard terpisah — `signInAsChild()` di `authService.js` cuma alias ke `signIn()` yang sama dipakai anak login sendiri. Ini keputusan privacy yang sengaja: orang tua cuma bisa lihat data anaknya sendiri (butuh tau PIN anaknya), bukan semua murid. **Jangan pernah bikin 1 PIN admin buat semua data** — itu privacy bug persis yang dihindarin BrainBox.
- **Route `/parents` TIDAK di-wrap `RequireAuth`** — beda sesi dari login player utama, biar orang tua gak harus login sebagai anak dulu di app utama.
- **Semua 3 section udah jalan pakai data asli** (per 2026-08-04, lihat bagian "Progress Tracking + Focus Round"):
  - **"Kirim Pesan"** — 1-arah (`players/{id}.parentMessage: {text, sentAt, read}`, nimpa pesan lama, bukan thread), muncul sebagai popup `OverlayCard` di Landing anak pas dibuka lagi, ditandain `read:true` begitu di-dismiss.
  - **"Fokus Minggu Ini"** (Assign) — `TopicPicker` (shared component, sama yang dipake `FocusRoundPicker`) nulis ke `players/{id}.assignedTopics` (array topicId `subject:grade:babKey`, max 8). Anak liat ini pre-checked pas buka `/fokus` sendiri.
  - **"Perlu Latihan Lagi"** (Needs Practice) — `computeWeakTopics()` di `progressService.js`, formula SAMA PERSIS BrainBox (`≥3 percobaan && akurasi<70%`, constants `MIN_ATTEMPTS`/`WEAK_ACCURACY`), baca dari `progress` map yang sama dipake locked/current/done. Title di-resolve dari `loadAllFocusTopics()` (gak didenormalisasi ke Firestore).
  - **XP per Pelajaran** (2026-08-04) — section baru di atas "Kirim Pesan", breakdown `computeXpBySubject()` (`progressService.js`) yang jumlahin field `xp` per bab di seluruh `progress` map per subject, dibandingin ke `SUBJECTS` (`content.js`) buat nama/emoji, bar proporsional ke subject XP tertinggi. Fix penting: `recordFocusRoundAttempt()` sebelumnya cuma nulis XP ke top-level `player.xp`, gak ke `progress.{subject}.{grade}.{babKey}.xp` — sekarang ditambahin juga biar breakdown ini akurat termasuk kontribusi Focus Round, bukan cuma practice biasa. Diverifikasi di browser: akun test 20 XP Matematika + 10 XP PAI → breakdown nampilin proporsi bar yang bener, total cocok 30 XP.
- Entry point: link kecil "Untuk orang tua →" di footer Landing (`Landing.jsx`), sengaja gak menonjol — sama alasan BrainBox: biar anak gak notice/ke-klik gak sengaja.

## Progress Tracking + Quiz Engine + Focus Round (selesai 2026-08-04)

Fondasi ini bikin app-nya "berfungsi" beneran (gak cuma nampilin daftar topik doang) — anak beneran ngerjain soal, progress kesimpen, XP naik, bab kebuka satu-satu.

**Schema** — `players/{id}.progress.{subject}.{grade}.{babKey} = {status, stars, xp, correct, wrong, lastAt}` (`progressService.js`). `status` CUMA pernah ditulis `"done"` — locked/current dihitung ULANG tiap baca (`computeStatuses()`: bab pertama yang belum `done` = `"current"`, sisanya `"locked"`), jadi gak ada 2 sumber kebenaran yang bisa out-of-sync kayak kalau "unlock next" ditulis eksplisit pas save.

- **`games/quiz/QuizRunner.jsx`** — komponen quiz reusable, dipake DUA jalur (practice per-topik normal DAN Focus Round). 1 soal/layar, handle `multiple_choice` (tombol) & `short_answer` (input teks, `normalizeAnswer.js` buat trim+lowercase compare). `onFinish({correct,wrong,total,results})` — `results` = `[{id,correct}]` per soal (pake `useRef`, bukan state, biar gak kena race batching pas dibaca tepat di titik finish), dipake FocusRoundQuiz buat atribusi correct/wrong balik ke topik asal soal.
- **`screens/TopicQuiz.jsx`** (`/kelas/:grade/:subject/topik/:babKey`) — practice normal, sample 8 soal acak dari `bab.soal`, `+10 XP`/jawaban benar, `recordTopicResult()` nulis `status:"done"` (INI yang bikin bab berikutnya kebuka).
- **`screens/FocusRoundPicker.jsx`** (`/fokus`) + **`FocusRoundQuiz.jsx`** (`/fokus/main`) — anak pilih max 8 topik lintas SEMUA subject+kelas (`loadAllFocusTopics()` di `focusTopics.js`, load semua kombinasi subject×grade yang ada konten dari `SUBJECTS_WITH_CONTENT` — otomatis nambah pas subject baru diaktifin, jangan hardcode angka di sini), pre-checked dari `assignedTopics` (yang di-set orang tua). Quiz-nya campur 3 soal/topik terpilih, `recordFocusRoundAttempt()` nulis correct/wrong+xp per topik TAPI **sengaja gak nyentuh `status`** (Focus Round nyumbang ke akurasi/weak-topic-calc doang, gak ganggu urutan locked/unlocked linear SubjectDetail) — sama filosofi kayak BrainBox misahin chapter-progress dari topicStats.
- **`components/TopicPicker.jsx`** — checklist topik shared, dipake `FocusRoundPicker` DAN `ParentPortal` (Assign section) biar UI-nya konsisten.
- Entry point Focus Round: tombol "🎯 Fokus Latihan" di Landing, di bawah "Ayo Main!".

`SubjectDetail.jsx` sekarang baca progress ASLI (`getSubjectProgress` + `computeStatuses`), bukan hardcoded lagi — topik current/done bisa diklik → `TopicQuiz`, "Lanjut Belajar" langsung lompat ke topik current.

`contentLoader.js` — `loadRawTopics()` (ganti nama dari `loadTopics` lama) sekarang include SOAL PENUH per bab (dibutuhin quiz), bukan cuma metadata. Masih lazy-load per kelas pakai `import.meta.glob`. `hasContent(subjectId)` (`SUBJECTS_WITH_CONTENT`) sekarang `true` buat SEMUA 6 subject (`matematika`/`ipas`/`ppkn`/`binggris`/`bindo`/`pai`) — gak ada lagi yang fallback "coming soon".

**Verified end-to-end** (browser, bukan cuma baca kode): selesain quiz bab-1 Matematika kelas 4 → bab-1 jadi "Selesai", bab-2 kebuka, XP naik — dicek ulang abis reload (baca Firestore fresh, bukan state lokal doang). Focus Round 3-topik → round 9 soal campur → tulis ke 3 topik terpisah → muncul bener di "Perlu Latihan Lagi" Parent Portal. Assign topik dari Parent Portal → muncul pre-checked di picker anak.

**Minor gap yang tersisa**: `stars` masih dihitung dari akurasi single-round doang (≥90%→3, ≥70%→2, ≥40%→1) — data-nya aman (`Math.max(stars, prev.stars)`), UI-nya udah ada tombol "Ulangi buat Naikin Bintang" (2026-08-04, lihat TODO #7) buat naikin lewat retry manual, tapi gak ada logic otomatis apa pun. XP breakdown per-subject dan tombol retry-bintang, dua-duanya UDAH kelar — lihat bagian Parent Portal & TODO list, bukan gap lagi.

## Gaya kerja user (sama kayak BrainBox punya, disalin langsung)
- Komunikasi campur Indonesia-Inggris.
- **Diskusi dulu sebelum eksekusi** kalau ada keputusan arsitektur/scope — baru "gas"/"lanjut" abis sepakat. Tapi task yang udah dikasih instruksi detail (misal restrukturisasi soal dengan spec lengkap) boleh langsung dikerjain tanpa nanya ulang tiap step.
- **Ikutin spec/wireframe persis**, jangan improvisasi kalau ada yang ambigu — tanya, jangan nebak (kejadian: subject IPA/IPS vs IPAS, ditanya dulu baru dieksekusi).
- Testing di real device krusial buat bug yang gak muncul di desktop — belum pernah dites di device asli sama sekali buat project ini (masih full browser-pane testing).
- **Aksi yang butuh login interaktif (GitHub OAuth, Firebase Console) gak bisa diwakilin** — kasih instruksi jelas step-by-step, user yang jalanin sendiri, baru lanjut.

## Yang masih perlu ditindaklanjuti
1. ~~Konten IPS/PAI/B.Indo/B.Inggris belum ada sama sekali~~ — **B.Inggris (526 soal) & B.Indonesia (282 soal) selesai dibikin & AKTIF di app 2026-08-04**, lihat bagian "Bahasa Inggris & Bahasa Indonesia — konten baru". **PAI: 168 soal draft selesai ditulis 2026-08-04 TAPI SENGAJA BELUM diaktifin di app** — lihat bagian "PAI — draft konten (belum aktif)" di bawah, WAJIB direview ustadz dulu. IPS udah gabung ke IPAS (bukan gap).
2. ~~80 soal IPAS + 341 soal Matematika belum_terpetakan~~ — **selesai direklasifikasi 2026-08-04**, lihat bagian "Mismatch kurikulum — SUDAH diselesaikan".
3. ~~1 akun test nyangkut di Firestore production~~ — **dihapus 2026-08-04**.
4. ~~Hero illustration di Landing masih placeholder emoji~~ — **diganti SVG ilustrasi anak baca buku 2026-08-04** (`components/ds/HeroIllustration.jsx`), lihat bagian "Hero Illustration" di bawah.
5. ~~Drive Mode & Plane Mode masih v1/core-engine~~ — **SELESAI 2026-08-05**: Plane Mode (power-up/boss/respawn-gauntlet/endless/high-score) dan Drive Mode (nitro/water-gun/vehicle-skin) sekarang punya hampir semua fitur lanjutan BrainBox, full verified via gameplay asli (lihat bagian Games). **DinoRace beneran (2-player racing) masih belum di-porting** — cuma unlock mechanism-nya yang jadi; butuh Firebase RTDB + race track UI, scope besar terpisah (beda dari Drive/Plane, ini emang belum mulai dikerjain sama sekali).
6. ~~Parent Portal XP breakdown masih total doang~~ — **selesai 2026-08-04**, section "XP per Pelajaran" baru, lihat bagian Parent Portal.
7. ~~Stars/XP di TopicQuiz cuma dihitung dari 1 round, gak ada tombol ulang~~ — **tombol "Ulangi buat Naikin Bintang" ditambahin 2026-08-04** ke `TopicQuiz.jsx`, `progressService.starsFor()` di-export biar dipakai UI juga. Diverifikasi di browser: run 1/8 benar → 0⭐ dim, retry 7/8 benar → 2⭐ terang.
8. **Firebase Authentication belum diaktifin di console** (cuma Firestore) — belum masalah karena auth sistem sendiri (name+PIN) gak pakai Firebase Auth SDK, tapi kalau nanti butuh Google Sign-In/dst buat orang tua, perlu diaktifin.
9. **Belum ada dual-deploy pattern** kayak BrainBox (standalone domain per produk) — wajar karena masih 1 domain (`jagoan-kelas.vercel.app`), belum relevan sampai ada kebutuhan domain kustom.

## Hero Illustration (2026-08-04)
`Landing.jsx` hero slot (220x160, div yang sama dipake buat tap-quadrant DinoRace easter egg) sekarang render `<HeroIllustration />` (`components/ds/HeroIllustration.jsx`) — SVG flat-style anak duduk baca buku + tas ransel + tumpukan buku, pakai warna dari `tokens.css` (bukan approximate). Bukan foto/aset AI-gen, hand-coded SVG paths. `useSecretTap` tetap jalan karena quadrant dihitung dari `getBoundingClientRect()` div pembungkus, bukan dari isi di dalamnya — ganti konten anak div aman gak ganggu easter egg (diverifikasi via JS click event, gak ada error console).

## PAI — konten AKTIF, user udah konfirmasi review (2026-08-04)
168 soal (6 kelas x 6 bab) ditulis (`content-pipeline/draft_pai.py` + `build_pai.py`, output `content-pipeline/output/pai/kelas_{1-6}.json`), lalu **di-copy ke `app/src/data/content/pai/` dan ditambahin ke `SUBJECTS_WITH_CONTENT`** di `contentLoader.js` setelah user eksplisit bilang "oke konfirmasi gas" — dianggap sebagai konfirmasi review, subject PAI SEKARANG AKTIF & tampil ke anak (udah diverifikasi jalan di browser, kelas 4 bab "Kisah Nabi Musa & Nabi Isa" render bener).

- Struktur bab (`PAI_BAB` di `bab_struktur.py`) DIRANCANG SENDIRI (bukan port 10-bab Buku Siswa resmi kelas 1 yang ditemukan riset), diringkas 6 bab/kelas biar konsisten sama subject lain.
- Konten SENGAJA dibatasi ke materi universal disepakati lintas ormas/mazhab umum di Indonesia (rukun Iman/Islam, nama 25 nabi & rasul, nama kitab, asmaul husna & kalimat thayyibah paling umum, akhlak/adab sehari-hari, kisah nabi versi umum) — **SENGAJA MENGHINDARI** fiqih yang berpotensi beda pendapat (detail rakaat sholat, nisab zakat, dst). Karena itu beberapa bab lebih tipis (4-7 soal) dibanding target subject lain — kualitas/keamanan konten diutamakan di atas kuantitas buat topik sesensitif ini.
- Semua soal masih tertandai `"sumber": "draft_ai_perlu_review"` di data-nya (gak diubah pas aktivasi) — kalau user minta tambahan/revisi materi PAI nanti, tetep perlakukan sehati-hati draft awal (jangan asal generate soal baru tanpa research yang sama ketatnya).
